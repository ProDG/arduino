---
phase: 04-wagtail-backend-skeleton-contract-match-dockerized
plan: 08
type: execute
wave: 7
depends_on: [01, 02, 03, 04, 05, 06, 07]
files_modified:
  - docs/force-en-audit.md
  - .planning/ROADMAP.md
autonomous: false
requirements:
  - WAGTAIL-08
tags:
  - phase-exit
  - audit
  - security
  - documentation

must_haves:
  truths:
    - "Force-en audit row appended to docs/force-en-audit.md covering Wagtail admin + /preview/* rendered pages"
    - "gitleaks pre-commit hook is verified to block a contrived `.env`-shaped commit"
    - "Fresh-laptop walkthrough proves `docker compose -f compose.yml -f compose.dev.yml up -d` brings up the full BE stack from a clean checkout"
    - "ROADMAP.md amended to reflect D-BUMP-01 (7.4 LTS bump deferred out of P4 entirely; recorded as a follow-up entry, NOT a P4 plan)"
    - "All seven ROADMAP P4 success criteria are mapped to evidence artifacts produced by Plans 01-07"
  artifacts:
    - path: "docs/force-en-audit.md"
      provides: "P4 row appended (Wagtail admin Ukrainian, preview pages <html lang=uk>, no English month/locale leakage in API responses)"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 4 entry amended re D-BUMP-01 (7.4 bump deferred); Phase 4.1 entry inserted OR a clear note appended"
  key_links:
    - from: "docs/force-en-audit.md"
      to: "P1/P2/P3 row pattern (existing format)"
      via: "append a Phase 4 row using the same column structure"
      pattern: "\\| Phase 4 \\|"
    - from: ".planning/ROADMAP.md Phase 4 section"
      to: "D-BUMP-01 deferral decision"
      via: "amendment line + cross-reference to 04-CONTEXT.md decisions"
      pattern: "deferred"
---

<objective>
Run the formal phase-exit verification: (1) gitleaks pre-commit gate proven to block secret-shaped commits; (2) force-en audit row appended to `docs/force-en-audit.md` covering Wagtail admin Ukrainian load + `/preview/*` rendered pages; (3) fresh-laptop checkout walkthrough proves the dev stack comes up from `compose.yml + compose.dev.yml` alone; (4) ROADMAP.md amended for D-BUMP-01 (7.4 LTS bump deferred out of P4 entirely — becomes a follow-up entry, NOT a Plan inside P4 per CONTEXT.md `<deferred>`); (5) the seven ROADMAP P4 success criteria are walked one-by-one against evidence from Plans 01-07.

Purpose: WAGTAIL-08 closure is conditional on the gitleaks gate working. UKR-06 (force-en audit) is a cross-cutting requirement that runs at every phase exit. The 7.4 deferral amendment is the agreed exit-step for D-BUMP-01. The success-criteria walk is the verifier's input.

Output: Updated `docs/force-en-audit.md`, updated `.planning/ROADMAP.md`, summary capturing all evidence + the user-approved phase-close verdict.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-wagtail-backend-skeleton-contract-match-dockerized/04-CONTEXT.md
@.planning/phases/04-wagtail-backend-skeleton-contract-match-dockerized/04-01-SUMMARY.md
@.planning/phases/04-wagtail-backend-skeleton-contract-match-dockerized/04-02-SUMMARY.md
@.planning/phases/04-wagtail-backend-skeleton-contract-match-dockerized/04-03-SUMMARY.md
@.planning/phases/04-wagtail-backend-skeleton-contract-match-dockerized/04-04-SUMMARY.md
@.planning/phases/04-wagtail-backend-skeleton-contract-match-dockerized/04-05-SUMMARY.md
@.planning/phases/04-wagtail-backend-skeleton-contract-match-dockerized/04-06-SUMMARY.md
@.planning/phases/04-wagtail-backend-skeleton-contract-match-dockerized/04-07-SUMMARY.md
@docs/force-en-audit.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: gitleaks gate verification + force-en audit row + ROADMAP amendment for D-BUMP-01</name>
  <files>docs/force-en-audit.md, .planning/ROADMAP.md</files>
  <read_first>
    - docs/force-en-audit.md (existing P1, P2, P3 rows — match format/columns)
    - .planning/ROADMAP.md (current Phase 4 section + Progress table)
    - .planning/phases/04-wagtail-backend-skeleton-contract-match-dockerized/04-CONTEXT.md §"7.3 → 7.4 LTS Bump (Deferred)" (D-BUMP-01..03), §"Day-Zero Security & Locale" (D-SEC-01..04), §"<deferred>" block (verify the bump is on the deferred list)
    - .pre-commit-config.yaml (gitleaks v8.30.1 already installed per Plan 01)
    - All Plan 01-07 SUMMARYs (evidence sources for the success-criteria walk)
  </read_first>
  <action>
    **Step A — gitleaks gate verification (synthetic trigger):**

    Create a contrived secret-shaped file in a temporary path the hook covers, attempt to commit, expect blocking, then clean up:
    ```bash
    cat > /tmp/synthetic-secret-test.env <<'EOF'
    AWS_ACCESS_KEY_ID=AKIAIOSFODNN7EXAMPLE
    AWS_SECRET_ACCESS_KEY=wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY
    EOF
    cp /tmp/synthetic-secret-test.env ./synthetic-secret-test.env
    git add ./synthetic-secret-test.env
    pre-commit run gitleaks --all-files 2>&1 | tee /tmp/gitleaks-trigger.log
    GITLEAKS_BLOCKED=$?
    git rm --cached ./synthetic-secret-test.env
    rm -f ./synthetic-secret-test.env /tmp/synthetic-secret-test.env
    test $GITLEAKS_BLOCKED -ne 0  # gitleaks MUST exit non-zero (blocked)
    ```
    Capture the gitleaks output line citing the AKIA-pattern detection in the SUMMARY.

    Also re-confirm:
    ```bash
    git check-ignore -q .env && echo "OK: .env still gitignored"
    git ls-files .env  # must be empty
    git ls-files .env.example  # must list .env.example
    grep -F "DEBUG = False" backend/wagtail_arduino/settings/prod.py
    grep -F "ALLOWED_HOSTS = " backend/wagtail_arduino/settings/base.py
    ```

    **Step B — Force-en audit row in `docs/force-en-audit.md`:**

    Read the existing P1/P2/P3 row format. Append a P4 row matching the same columns. Specific P4 checks:

    1. Wagtail admin loads at `http://arduino.localhost/admin/login/` with browser locale forced to `en-US` — the admin UI labels remain Ukrainian per `LANGUAGE_CODE='uk'` (Wagtail respects the server-side LANGUAGE_CODE; admin doesn't follow Accept-Language by default).
    2. `/api/v2/pages/?type=lessons.LessonPage&slug=pershyi-blymayuchyi-svitlodiod&fields=*` response — search for English month names (`January..December`), English locale strings, etc. — none present (dates are ISO-8601, content is Ukrainian).
    3. `/preview/lesson/<token>` rendered HTML view-source contains `<html lang="uk">` (preserved from `index.html` shell).
    4. Wagtail admin's stock English strings present (e.g., button labels not yet translated by upstream Wagtail) — note as P6 polish, NOT a phase blocker per CONTEXT.md `<deferred>`.

    Append the row using the existing column shape. Mark each check PASS/FAIL/NOTE with a one-line evidence pointer.

    **Step C — Fresh-laptop walkthrough proof:**

    Either (a) physically check out the repo to a fresh path and run the bring-up, OR (b) simulate fresh-laptop semantics by `docker compose down -v` (removes volumes) and re-`up`. Either way, capture stdout + final `docker compose ps` + the smoke `curl http://arduino.localhost/admin/login/` returning 200.

    ```bash
    # Option B: simulated fresh laptop
    docker compose -f compose.yml -f compose.dev.yml down -v
    docker compose -f compose.yml -f compose.dev.yml up -d --build
    sleep 10
    docker compose exec wagtail python manage.py migrate --noinput
    docker compose exec wagtail python manage.py seed_fixtures
    curl -s -o /dev/null -w 'admin: %{http_code}\n' http://arduino.localhost/admin/login/
    curl -s -o /dev/null -w 'api:   %{http_code}\n' http://arduino.localhost/api/v2/pages/
    pnpm contract:diff | tail -3
    docker compose ps
    ```
    Expected: `admin: 200`, `api: 200`, `7/7 PASS`, `docker compose ps` shows all services healthy.

    **Step D — Amend ROADMAP.md for D-BUMP-01:**

    Locate the Phase 4 section in `.planning/ROADMAP.md`. The current text (line 14 + line 83) says "Plan a one-line bump to Wagtail 7.4 LTS on its 2026-05-04 release." Replace this with a deferral note:

    > **Note (amended at P4 phase exit per D-BUMP-01):** The 7.3→7.4 LTS bump was deferred out of Phase 4. Phase 4 ships and exits on Wagtail 7.3.x. The bump is tracked as a follow-up effort (proposed Phase 4.1 — single-task: pin bump + re-run P4 verification gates per D-BUMP-02). Rationale: bundling the major-version bump into the contract-locking phase concentrated two large risk surfaces; deferral isolates them.

    Apply the amendment to BOTH the Phase 4 description line (around line 14) and the inline "Plans" subsection (line 83). Update the Progress table at the bottom: change Phase 4 status from "Unblocked" to "**Complete**" (pending verifier sign-off — this plan executes BEFORE the verifier; if verifier rejects, leave the table edit as a deviation noted in SUMMARY).

    Optionally insert a Phase 4.1 stub after Phase 4:
    ```markdown
    ### Phase 4.1: Wagtail 7.4 LTS Bump (deferred from P4 per D-BUMP-01)
    **Goal**: Pin Wagtail to 7.4 LTS; re-run all P4 verification gates per D-BUMP-02; rollback strategy per D-BUMP-03.
    **Depends on**: Phase 4
    **Requirements**: (none new — all WAGTAIL-* IDs were closed in P4)
    **Success Criteria** (from D-BUMP-02): full P4 verification re-run = contract diff (all 7 fixtures PASS) + preview flow walkthrough + MinIO upload smoke + force-en audit row clean + manual Wagtail admin smoke
    **Plans**: TBD (single-task plan likely)
    ```

    **Step E — Map seven ROADMAP P4 success criteria to evidence:**

    Compose a table in the SUMMARY mapping each of the 7 success criteria (from ROADMAP.md Phase 4 §"Success Criteria") to a specific evidence artifact (Plan number, SUMMARY path, grep target, or curl output) produced by Plans 01-07. This table is the verifier's primary input. Example row format:
    ```
    | # | Criterion (abridged) | Evidence |
    |---|----------------------|----------|
    | 1 | Wagtail 7.3 + Django 5.2 + … in Compose; page models match FE TS contract | Plan 01 (uv.lock, compose.yml), Plan 03 (page models + grep -F "class LessonPage(Page)" backend/apps/lessons/models.py); 04-01-SUMMARY + 04-03-SUMMARY |
    | 2 | REST v2 byte-compatible; expand_db_html; renditions via MinIO | Plan 06 (`pnpm contract:diff` → 7/7 PASS); Plan 02 (rendition URL grep + curl 200) |
    | 3 | django-storages[s3] + boto3 against MinIO; `mc ls` shows uploads | Plan 02 SUMMARY (mc ls output) |
    | 4 | Editor preview flow works | Plan 07 Task 3 walkthrough notes |
    | 5 | Env flag flips Mock → Wagtail | Plan 05 (provideContentApi factory + `pnpm build` succeeds in both flag states) |
    | 6 | Day-zero security: .env gitignored, gitleaks blocks, DEBUG=False prod, LANGUAGE_CODE='uk', etc. | Plan 01 + this Plan Task 1 Step A (gitleaks synthetic trigger) |
    | 7 | `docker compose -f compose.yml -f compose.dev.yml up -d` works on fresh laptop | this Plan Task 1 Step C (down -v + up walkthrough) |
    ```
  </action>
  <verify>
    <automated>grep -E "\|\s*Phase 4\s*\|" docs/force-en-audit.md && grep -F "deferred" .planning/ROADMAP.md | grep -i "7\.4\|bump" && curl -s -o /dev/null -w '%{http_code}' http://arduino.localhost/admin/login/ | grep -q 200 && pnpm contract:diff 2>&1 | grep -F "7/7 PASS"</automated>
  </verify>
  <acceptance_criteria>
    - gitleaks synthetic trigger: `pre-commit run gitleaks --all-files` against the AKIA-pattern fixture exits non-zero AND stdout contains the AKIA detection message
    - `git check-ignore -q .env` → exit 0; `git ls-files .env` → empty
    - `grep -E "\|\s*Phase 4\s*\|" docs/force-en-audit.md` → matches (P4 row appended)
    - The P4 row in `docs/force-en-audit.md` mentions: Wagtail admin Ukrainian, /preview/* `<html lang=uk>`, API responses no-English-month-leakage
    - `.planning/ROADMAP.md` Phase 4 section contains a deferral note for the 7.4 bump: `grep -F "deferred" .planning/ROADMAP.md | grep -i "7\.4\|bump"` → matches
    - `.planning/ROADMAP.md` Progress table updated for Phase 4 (status reflects completion or pending-verifier per executor judgement)
    - Fresh-laptop simulation: after `docker compose down -v && up -d`, `curl http://arduino.localhost/admin/login/` → 200
    - `pnpm contract:diff` after fresh-laptop bring-up still exits 0 with `7/7 PASS`
    - `docker compose ps --format json | jq -r '.[].State' | sort -u` → contains only `running` (or `exited` for the one-shot `mc` sidecar)
    - The seven ROADMAP success-criteria mapping table appears in the plan SUMMARY
  </acceptance_criteria>
  <done>gitleaks gate verified blocking; P4 force-en audit row appended; ROADMAP amended for the deferred 7.4 bump; fresh-laptop walkthrough re-establishes the full stack with seed + contract diff PASS; success-criteria mapping table ready for verifier.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Phase 4 close — user reviews evidence and gives verdict (approve / known-debt / re-open)</name>
  <what-built>
    Plan 04-08 has executed:
    - **Synthetic gitleaks trigger** — proved the pre-commit hook blocks AKIA-pattern secrets (verbatim output in 04-08-SUMMARY).
    - **Force-en audit row appended** to `docs/force-en-audit.md` — Wagtail admin loads in Ukrainian under forced en browser locale; preview pages keep `<html lang="uk">`; no English month/locale leakage in API responses; any residual upstream-Wagtail English strings are noted as P6 polish (NOT a P4 blocker per CONTEXT.md `<deferred>`).
    - **ROADMAP.md amended** — Phase 4 description and inline plans subsection both note D-BUMP-01 (7.4 LTS bump deferred out of P4 entirely; tracked as a separate follow-up — proposed Phase 4.1 stub inserted).
    - **Fresh-laptop walkthrough** — `docker compose down -v` + `up -d` from a clean state, ran migrations + seed + contract diff: all green; admin/login + api/v2/pages 200.
    - **Success-criteria mapping table** — all 7 ROADMAP P4 success criteria mapped to specific evidence in 04-08-SUMMARY.

    All requirements WAGTAIL-01 through WAGTAIL-10 closed across Plans 01-07; this plan closes WAGTAIL-08 verification half (gitleaks proven) and the cross-cutting UKR-06 force-en row.
  </what-built>
  <how-to-verify>
    Read the seven plan SUMMARYs (04-01-SUMMARY.md through 04-07-SUMMARY.md) plus 04-08-SUMMARY.md. Walk the success-criteria table. Spot-check two or three of the most contract-critical claims by hand:

    1. `pnpm contract:diff` — should print `7/7 PASS`.
    2. Open `http://arduino.localhost/admin/login/` — admin in Ukrainian.
    3. `docker compose ps` — all services healthy / `mc` exited 0.
    4. `git log --oneline -20` — Phase 4 commits visible.
    5. `grep -F "image_src" backend/` — exit 1 (rename complete).
    6. `grep -F "note = blocks.RichTextBlock" backend/apps/blocks/code.py` — exit 1 (rename complete).
    7. `grep -F "DEBUG = False" backend/wagtail_arduino/settings/prod.py` — matches.
    8. The amended ROADMAP.md Phase 4 entry mentions the 7.4 deferral.

    Then issue one of three verdicts:
    - **APPROVE** — Phase 4 closes clean, no debt; STATE.md will be updated to mark P4 complete and ready for P5 planning.
    - **KNOWN DEBT (option C)** — close with documented carry-over (similar to P3); list specific items to roll into milestone-end review (this is the path P3 took — P4 deserves a clean close ideally, but option C remains available).
    - **RE-OPEN** — name a specific gap that must be remediated before close (the planner will switch to gap-closure mode and add a Plan 09).
  </how-to-verify>
  <resume-signal>Type "approved" + brief note, OR "known-debt: <list>", OR "re-open: <gap>" with details so the planner can act.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pre-commit hook → developer working tree | Trusted local enforcement; defense-in-depth alongside server-side review |
| Synthetic test fixture → staged area | Deliberately invokes gitleaks to prove gate; fixture file deleted before plan finishes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-44 | Information Disclosure | Synthetic AKIA fixture accidentally committed | mitigate | Test sequence: create file → `git add` → run hook (expect block) → `git rm --cached` + `rm` → verify no trace; acceptance criterion confirms non-zero exit so the file was NEVER committed |
| T-04-45 | Tampering | gitleaks rules disabled / weakened during P4 | mitigate | `.pre-commit-config.yaml` rev pinned at v8.30.1 (set in P1); P4 did not touch the gitleaks repo entry — only appended ruff/mypy hooks; grep verify in the synthetic-trigger step output |
| T-04-46 | Information Disclosure | ROADMAP amendment exposing internal decision context | accept | ROADMAP.md is committed to repo; OSS-public posture; D-BUMP-01 rationale is non-sensitive |
| T-04-47 | Spoofing | Force-en audit run from a browser locale that doesn't actually force EN | mitigate | Audit instructions specify the exact browser-flag invocation (Chrome `--lang=en-US` or DevTools sensors); evidence row notes the exact method used |
</threat_model>

<verification>
- All grep + curl + contract:diff checks pass
- gitleaks synthetic trigger documented (verbatim output)
- Force-en audit row populated
- ROADMAP amended
- Fresh-laptop walkthrough produces all-green
- User issues approve / known-debt / re-open verdict
</verification>

<success_criteria>
- WAGTAIL-08 verification half closed: gitleaks gate proven; .env still gitignored; DEBUG=False prod; ALLOWED_HOSTS explicit; SECRET_KEY env-driven (set in Plans 01 + this plan)
- UKR-06 (cross-cutting): P4 force-en audit row appended to docs/force-en-audit.md
- ROADMAP.md amended to reflect D-BUMP-01 (7.4 deferral)
- Phase 4 ROADMAP success criteria all mapped to evidence
- User verdict obtained for phase close
</success_criteria>

<output>
After completion, create `.planning/phases/04-wagtail-backend-skeleton-contract-match-dockerized/04-08-SUMMARY.md` capturing: gitleaks synthetic-trigger verbatim output, the appended force-en audit row, the ROADMAP amendment diff, the fresh-laptop walkthrough output, the seven-criterion mapping table, and the user's phase-close verdict.
</output>
</content>
</invoke>
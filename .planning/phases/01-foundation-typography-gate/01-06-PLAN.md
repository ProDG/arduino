---
phase: 01-foundation-typography-gate
plan: 06
type: execute
wave: 3
depends_on: [01-01, 01-03, 01-04, 01-05]
files_modified:
  - .pre-commit-config.yaml
  - .gitleaks.toml
  - .github/workflows/ci.yml
  - docs/force-en-audit.md
  - docs/typography-checklist.md
  - .planning/phases/01-foundation-typography-gate/font-swap-dry-run.md
  - package.json
autonomous: false

requirements:
  - TYPE-03
  - TYPE-06
  - TYPE-10
  - UKR-05
  - UKR-06

must_haves:
  truths:
    - ".env is gitignored (PLAN 01) AND gitleaks pre-commit hook is installed and tested"
    - "GitHub Actions CI runs: install --frozen-lockfile, lint, lint:verify-rule, test, build, gitleaks-scan"
    - "docs/force-en-audit.md exists, is filled-in (NOT a stub), AND has been run once with results recorded"
    - "docs/typography-checklist.md exists and is filled-in (NOT a stub)"
    - "TYPE-06 verified: a font-pairing dry-run swap is recorded showing only _typography.scss was edited"
    - "Europe/Kyiv set wherever times render (UKR-05) — already baked into src/lib/intl.ts in PLAN 04; verify no additional time-rendering site escapes the facade"
  artifacts:
    - path: ".pre-commit-config.yaml"
      provides: "gitleaks + prettier-check + eslint-on-changed + stylelint-on-changed hooks"
      contains: "gitleaks"
    - path: ".github/workflows/ci.yml"
      provides: "GitHub Actions: lint + lint:verify-rule + test + build + gitleaks scan"
    - path: "docs/force-en-audit.md"
      provides: "Locale-leak audit checklist; recorded run result"
      contains: "/dev/glyph-audit"
    - path: "docs/typography-checklist.md"
      provides: "Phase-exit gate checklist for re-verifying glyph + specimen + locale on a font swap or upgrade"
    - path: ".planning/phases/01-foundation-typography-gate/font-swap-dry-run.md"
      provides: "Evidence that TYPE-06 holds — single-file font swap proven"
  key_links:
    - from: ".pre-commit-config.yaml"
      to: "gitleaks"
      via: "repo: https://github.com/gitleaks/gitleaks"
      pattern: "gitleaks/gitleaks"
    - from: ".github/workflows/ci.yml"
      to: "pnpm lint && pnpm lint:verify-rule && pnpm test --run && pnpm build"
      via: "CI step list"
      pattern: "pnpm build"
---

<objective>
Close out Phase 1: install day-zero security (gitleaks pre-commit + CI scan), wire GitHub Actions to run lint + synthetic-violation lint check + tests + build + gitleaks scan on every push, fill in the two phase-exit doc files (`docs/force-en-audit.md` and `docs/typography-checklist.md`) with real content (NOT stubs) and run the force-en audit once recording results, prove TYPE-06 with a font-pairing dry-run swap recorded as evidence, and verify no time-rendering site bypasses `src/lib/intl.ts` (UKR-05).

Purpose: This plan is the phase-exit gate. After it passes, Phase 1 is shippable: foundation is locked AND structurally protected against regression. The two doc files become the templates the rest of the project's phases re-run.
Output: CI green, pre-commit hooks installed, both docs filled in and audited, dry-run swap evidence committed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/01-foundation-typography-gate/01-CONTEXT.md
@.planning/phases/01-foundation-typography-gate/01-UI-SPEC.md
@.planning/phases/01-foundation-typography-gate/01-03-SUMMARY.md
@.planning/phases/01-foundation-typography-gate/01-04-SUMMARY.md
@.planning/phases/01-foundation-typography-gate/01-05-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Install pre-commit + gitleaks + GitHub Actions CI</name>
  <read_first>
    - .planning/phases/01-foundation-typography-gate/01-CONTEXT.md (D-29 CI runs `pnpm lint && pnpm test --run && pnpm build`; D-31 pre-commit hooks: gitleaks + prettier-check + eslint + stylelint)
    - .gitignore (PLAN 01 — confirm .env is already there)
    - package.json (PLAN 01 + PLAN 04 scripts)
    - eslint.config.js (PLAN 04 — has the no-bare-toLocale rule + ignores synthetic fixture)
  </read_first>
  <action>
    1. **Create `.pre-commit-config.yaml`** (D-31):
       ```yaml
       repos:
         - repo: https://github.com/gitleaks/gitleaks
           rev: v8.21.0
           hooks:
             - id: gitleaks
         - repo: local
           hooks:
             - id: prettier-check
               name: prettier-check
               entry: pnpm exec prettier --check
               language: system
               files: '\.(ts|html|scss|json|md|yaml|yml)$'
               pass_filenames: true
             - id: eslint-changed
               name: eslint changed ts
               entry: pnpm exec eslint
               language: system
               files: '\.ts$'
               exclude: '^src/__synthetic__/'
               pass_filenames: true
             - id: stylelint-changed
               name: stylelint changed scss
               entry: pnpm exec stylelint
               language: system
               files: '\.scss$'
               pass_filenames: true
       ```

       Pin gitleaks to a recent stable tag (resolve at execution time — pin in the file, do NOT use `master`). Add a `.gitleaks.toml` minimally configured if any false positives surface (likely none for a fresh Angular scaffold).

    2. **Install pre-commit framework** on the dev machine: `pip install pre-commit` or `uv tool install pre-commit`. Then `pre-commit install` from the repo root. Then `pre-commit run --all-files` to verify all hooks pass on the existing tree.

    3. **Test gitleaks fires:** create a temp file `tmp-secret-test.txt` with content `aws_secret_access_key=AKIAIOSFODNN7EXAMPLE/wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY` (the AWS canary — gitleaks recognizes it). Try `git add tmp-secret-test.txt && git commit -m "test"` → MUST be blocked by gitleaks. Then `rm tmp-secret-test.txt` and `git reset HEAD .` to clean up. Record this test in the SUMMARY.

    4. **Create `.github/workflows/ci.yml`** (D-29):
       ```yaml
       name: CI
       on:
         push:
           branches: [main]
         pull_request:
       jobs:
         build:
           runs-on: ubuntu-24.04
           steps:
             - uses: actions/checkout@v4
               with:
                 fetch-depth: 0  # gitleaks needs full history

             - uses: pnpm/action-setup@v4
               with:
                 version: 10

             - uses: actions/setup-node@v4
               with:
                 node-version: '22'
                 cache: 'pnpm'

             - run: pnpm install --frozen-lockfile

             - run: pnpm lint
             - run: pnpm lint:verify-rule
             - run: pnpm test --run
             - run: pnpm build

             - name: gitleaks scan
               uses: gitleaks/gitleaks-action@v2
               env:
                 GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
       ```

    5. **Verify** the workflow file is YAML-valid and references all four scripts (`lint`, `lint:verify-rule`, `test`, `build`).

    Reference D-29, D-31. Note the executor cannot exercise GitHub Actions without a remote — verifying YAML validity + step list is sufficient for this task; the live first-CI-run check happens after first push, outside this phase.
  </action>
  <verify>
    <automated>test -f .pre-commit-config.yaml && grep -q "gitleaks/gitleaks" .pre-commit-config.yaml && grep -q "rev: v" .pre-commit-config.yaml && test -f .github/workflows/ci.yml && grep -q "pnpm lint" .github/workflows/ci.yml && grep -q "pnpm lint:verify-rule" .github/workflows/ci.yml && grep -q "pnpm test --run" .github/workflows/ci.yml && grep -q "pnpm build" .github/workflows/ci.yml && grep -q "gitleaks-action" .github/workflows/ci.yml && pre-commit run --all-files</automated>
  </verify>
  <acceptance_criteria>
    - `.pre-commit-config.yaml` references `gitleaks/gitleaks` repo with a pinned `rev:` (not `master`)
    - `.pre-commit-config.yaml` includes hooks for `prettier-check`, `eslint`, `stylelint`
    - `pre-commit install` succeeds; `.git/hooks/pre-commit` exists
    - `pre-commit run --all-files` exits 0 (every hook passes on the current tree)
    - Manual gitleaks test (commit a fake AWS key) is BLOCKED — verified during execution and cleaned up
    - `.github/workflows/ci.yml` is valid YAML
    - CI step list includes: install --frozen-lockfile, lint, lint:verify-rule, test --run, build, gitleaks-action
  </acceptance_criteria>
  <done>Day-zero security wired: gitleaks blocks secret commits via pre-commit, GitHub Actions enforces the same on push. Toolchain (lint/test/build) runs in CI on every push.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Fill in docs/force-en-audit.md and docs/typography-checklist.md (NOT stubs)</name>
  <read_first>
    - .planning/phases/01-foundation-typography-gate/01-CONTEXT.md (D-32 — both docs filled in, NOT stubs)
    - .planning/phases/01-foundation-typography-gate/01-UI-SPEC.md §"Section 3 — Locale demo" (audit footer note text + force-en procedure)
    - .planning/phases/01-foundation-typography-gate/01-05-SUMMARY.md (visual checks performed in PLAN 05 — many of those translate into the audit checklist)
    - CLAUDE.md ("Force-en browser audit at every phase exit")
  </read_first>
  <action>
    Both docs are project-wide artifacts re-run at every phase exit per CLAUDE.md and ROADMAP.md "Cross-Cutting Conventions" section. They MUST be substantive checklists — not "TODO".

    1. **`docs/force-en-audit.md`** — UKR-06 + Phase 1 success criterion #5. Structure:
       - **Title:** "Force-EN Locale Audit — Project Checklist"
       - **Purpose:** One-paragraph explanation: why locale leakage is the #1 risk for a Ukrainian-only site; this checklist runs at every phase exit.
       - **Procedure:**
         1. Build static site: `pnpm build`.
         2. Serve locally: `pnpm dlx http-server dist/*/browser -p 4300`.
         3. Open Chrome → DevTools → "Sensors" panel → set Locale to `en-US`.
         4. Reload the site. Walk every page in the current scope.
         5. For each user-visible string, confirm it stays Ukrainian. Any English regression is a FAIL.
       - **Phase 1 scope checklist** (rendered as Markdown checklist):
         - [ ] `/` (root page) — H1 `Arduino UA`, lede, body, link text all Ukrainian
         - [ ] `/dev/glyph-audit` Section 1 — page heading + lede Ukrainian; verification string Ukrainian (sanity)
         - [ ] `/dev/glyph-audit` Section 2 — H1 `Перший крок: світлодіод, що блимає`, lede, body, code-comment, figure caption, aside all Ukrainian
         - [ ] `/dev/glyph-audit` Section 3 — Date renders e.g. `30 квітня 2026 р.` (Ukrainian month + `р.` suffix), NOT `April 30, 2026`
         - [ ] `/dev/glyph-audit` Section 3 — Number renders `1 234 567,89` (NBSP thousand separator, comma decimal), NOT `1,234,567.89`
         - [ ] `/dev/glyph-audit` Section 3 — Sort order is Ukrainian alphabet (`ґніт` after `г` words; `їжак` after `і` words)
         - [ ] `<html lang="uk">` is set on every page (View Source)
         - [ ] No `April`, `May`, `Mon`, `Tue`, etc. anywhere in DOM (Chrome DevTools → Elements → Search)
         - [ ] `grep -rnE 'new Intl\.[A-Z][A-Za-z]*' src/ --include='*.ts'` returns ONLY hits inside `src/lib/intl.ts` (no Intl class instantiation escapes the wrapper)
       - **Intl wrapper policy** (project-wide rule, embedded in this doc per W4): Any `new Intl.*` use outside `src/lib/intl.ts` is a violation. If a future feature legitimately needs `Intl.PluralRules` / `Intl.RelativeTimeFormat` / `Intl.ListFormat` / `Intl.Segmenter` / `Intl.DisplayNames`, add a wrapper to `src/lib/intl.ts` and use that wrapper everywhere — never the bare class. Update this doc's checklist when a new wrapper is added.
       - **Future-phase placeholders:** Markdown headings for "Phase 2 scope", "Phase 3 scope", etc., each with a one-line note `(Add checklist items as primitives / templates land.)`. So the doc is the cumulative project-wide audit, growing per phase, not phase-1-only.
       - **Run-once recording for Phase 1:** A subsection at the bottom titled `## Run record — Phase 1 (YYYY-MM-DD)`. List PASS/FAIL for each Phase 1 checkbox. Sign with date and "Phase 1 audit run by: <executor>". This is the substantive run-record proving the checklist was actually walked once for Phase 1 (success criterion #5).

    2. **`docs/typography-checklist.md`** — D-32 + UI-SPEC §"Checker Sign-Off" + the Section 1 visual gates from PLAN 05. Structure:
       - **Title:** "Typography Checklist — Run after font swap or font release bump"
       - **Purpose:** One paragraph. The phase-exit gate for re-verifying that all six woff2 outputs still cover Ukrainian critical glyphs across regular/italic/bold/bold-italic.
       - **When to run:**
         - On a font-release bump (rerun `pnpm fonts:subset` then this checklist)
         - On a font-pairing swap (edit `_typography.scss` then this checklist)
         - At every phase exit for any phase that touches `_typography.scss` or `_base.scss`
       - **Procedure:**
         1. `pnpm fonts:subset` (only if font release bumped; otherwise skip).
         2. `pnpm fonts:metrics` (only if metric-affecting bump).
         3. `pnpm build` and serve locally.
         4. Visit `/dev/glyph-audit`.
       - **Glyph matrix checks** (12 cells × 3 sub-checks each):
         - [ ] Source Serif 4 / regular / 400 — full string renders, ґ and Ґ visible, no tofu
         - [ ] Source Serif 4 / italic / 400 — italic Cyrillic looks REAL (cursive а/г/и), not synthesized slant
         - [ ] Source Serif 4 / bold / 600 — bolder weight visible, ґ Ґ remain correct shape
         - [ ] Source Serif 4 / bold-italic / 600 — bolder + real italic, ґ Ґ correct
         - [ ] Inter / regular / 400
         - [ ] Inter / italic / 400 — REAL italic from Inter italic woff2
         - [ ] Inter / bold / 600
         - [ ] Inter / bold-italic / 600
         - [ ] JetBrains Mono / regular / 400 — Cyrillic comment renders correctly in mono
         - [ ] JetBrains Mono / italic / 400 — Cyrillic italic in mono visible
         - [ ] JetBrains Mono / bold / 600
         - [ ] JetBrains Mono / bold-italic / 600
       - **Isolated glyph row checks** (per cell, 96px row):
         - [ ] `і` — dotted-i, single dot
         - [ ] `ї` — i with diaeresis (two dots)
         - [ ] `є` — backwards-e, no fallback
         - [ ] `ґ` — ge with upper-comma stroke (NOT a tofu, NOT plain `г`)
         - [ ] `Ї` — capital with diaeresis
         - [ ] `Є` — capital backwards-e
         - [ ] `Ґ` — capital ge with comma
         - [ ] `ʼ` — modifier-letter apostrophe (raised, comma-shaped), NOT a vertical mark
       - **Specimen checks** (Phase 1 success criterion #2):
         - [ ] Body measure ~62ch on desktop
         - [ ] Ragged-right (no justified body)
         - [ ] No `hyphens: auto` engagement (no broken Ukrainian words)
         - [ ] Inline `<code>` (`pin 13`) reads as JetBrains Mono with warm-grey pill bg
         - [ ] Code block has Arduino-teal hairline border (#007A80)
         - [ ] `Рис. 1` figure-num is Arduino-teal
         - [ ] Aside has 2px Arduino-teal left border
         - [ ] No FOIT (text visible immediately, swaps to real font)
         - [ ] CLS < 0.05 measured in DevTools Performance
       - **Locale demo checks** (Phase 1 success criterion #4):
         - [ ] Date in Ukrainian month + `р.` suffix
         - [ ] Number with NBSP thousands + comma decimal
         - [ ] Sort order matches Ukrainian alphabet
       - **Run record subsection:** `## Run record — Phase 1 (YYYY-MM-DD)`. PASS/FAIL per item.

    3. **Run both checklists** against the live site once for Phase 1. Record results inline in each doc's "Run record" subsection. If any item FAILS, fix in collaboration with PLAN 05's checkpoint (Task 3 of PLAN 05). Both checklists' run-records must be PASS-only before this task closes.

    Reference D-32 (both docs filled, NOT stubs).
  </action>
  <verify>
    <automated>test -f docs/force-en-audit.md && wc -l docs/force-en-audit.md | awk '$1 > 50' | grep -q . && grep -q "Run record" docs/force-en-audit.md && grep -q "/dev/glyph-audit" docs/force-en-audit.md && grep -q "квітня\|Ukrainian month" docs/force-en-audit.md && test -f docs/typography-checklist.md && wc -l docs/typography-checklist.md | awk '$1 > 80' | grep -q . && grep -q "Run record" docs/typography-checklist.md && grep -q "Source Serif 4" docs/typography-checklist.md && grep -q "ґ" docs/typography-checklist.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/force-en-audit.md` exists, has > 50 lines (substantive — not a stub)
    - `docs/force-en-audit.md` contains a Phase 1 scope checklist with at least 6 checkboxes referencing `/dev/glyph-audit`, `/`, `<html lang="uk">`
    - `docs/force-en-audit.md` contains a `## Run record — Phase 1` section with PASS/FAIL recorded
    - `docs/typography-checklist.md` exists, has > 80 lines
    - `docs/typography-checklist.md` references all 12 matrix cells (3 families × 4 styles)
    - `docs/typography-checklist.md` contains a `## Run record — Phase 1` section with PASS/FAIL for matrix, isolated glyphs, specimen, locale demo
    - Both run records dated and signed
    - All Phase 1 items in both run records are PASS (any FAIL would block phase exit and be fixed)
  </acceptance_criteria>
  <done>Both phase-exit docs filled in (NOT stubs), and both have been run once with results recorded. The structural prevention layer (lint + gitleaks) is paired with the visual prevention layer (these checklists).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: TYPE-06 dry-run swap evidence + UKR-05 time-zone audit</name>
  <read_first>
    - .planning/phases/01-foundation-typography-gate/01-CONTEXT.md (D-03 — _typography.scss is single-file swap target; UKR-05 — Europe/Kyiv wherever times rendered)
    - src/styles/tokens/_typography.scss (PLAN 03 — current state)
    - src/lib/intl.ts (PLAN 04 — Europe/Kyiv baked in)
    - .planning/ROADMAP.md §"Phase 1 Success Criteria" criterion #3
  </read_first>
  <action>
    1. **TYPE-06 dry-run swap (Phase 1 success criterion #3).** Prove that changing the font pairing requires editing exactly ONE file (`src/styles/tokens/_typography.scss`).

       Procedure (perform on a scratch branch or local-only changes — DO NOT commit the swap):
       a. Make a temporary edit ONLY to `src/styles/tokens/_typography.scss`:
          - Change `--font-body` family-stack first entry from `'Source Serif 4'` to `'Literata'` (or any other serif name).
          - Optional: comment out two of the Source Serif 4 `@font-face` blocks.
       b. Run `pnpm build`. Build MUST succeed.
       c. Confirm the only modified file is `_typography.scss`: `git status` shows ONE modified file under `src/styles/tokens/`.
       d. Visit `/dev/glyph-audit` (served from `dist/`). The body type now falls back through the family stack to Georgia (since Literata isn't loaded). The page renders, the harness still works, the matrix labels still match the cell font (Source Serif 4 cells now show their fallback). NO other component, NO other token file, NO base CSS needed editing.
       e. Document the procedure and outcome in `.planning/phases/01-foundation-typography-gate/font-swap-dry-run.md`:
          - Date, executor name
          - Edit performed (paste the diff)
          - Files modified per `git status` (confirm: 1 file)
          - Build result (succeeded)
          - Visual confirmation (screenshot path or text description)
          - **Conclusion: TYPE-06 holds. Single-file font swap proven.**
       f. Revert the edit: `git checkout -- src/styles/tokens/_typography.scss`. Re-run `pnpm build` to confirm baseline restored.

    2. **UKR-05 time-zone audit.** `Europe/Kyiv` was baked into `src/lib/intl.ts` in PLAN 04. Confirm no other code paths render time/date without going through the facade.

       Procedure:
       a. `grep -rnE 'new Intl\.[A-Z][A-Za-z]*' src/ --include='*.ts'` — should return ONLY hits inside `src/lib/intl.ts`. Any other hit is a violation. The widened regex matches ALL `Intl.X` constructors (`DateTimeFormat`, `NumberFormat`, `Collator`, `PluralRules`, `RelativeTimeFormat`, `ListFormat`, `Segmenter`, `DisplayNames`) — not just the three originally enumerated. **Add a note in `docs/force-en-audit.md` (whose content is locked in PLAN 06 Task 2):** "Any `new Intl.*` use outside `src/lib/intl.ts` is a violation; if a future feature legitimately needs `Intl.PluralRules` / `Intl.RelativeTimeFormat` / `Intl.ListFormat` / `Intl.Segmenter` / `Intl.DisplayNames`, add a wrapper to `src/lib/intl.ts` and use that wrapper everywhere — never the bare class."
       b. `grep -rn "toLocaleDateString\|toLocaleString\|toLocaleTimeString" src/ --include='*.ts'` — should return ONLY hits inside `src/__synthetic__/eslint-violation-fixture.ts` (the test fixture). The lint rule from PLAN 04 already enforces this; this audit is the manual second check.
       c. `grep -rn "Date.now()\|new Date()" src/ --include='*.ts'` — review every hit. Each call must either (a) feed into `formatDateUk(...)`, or (b) be timezone-agnostic (e.g., used as a number for cache keys or trace IDs). No raw `new Date()` should be `.toString()`-rendered into the DOM.

       Record findings in `font-swap-dry-run.md` under a `## UKR-05 audit` section: list of hits per grep, classification (allowed / violation), and conclusion.

    Reference D-03 (single-file swap target), UKR-05 (Europe/Kyiv wherever times rendered).
  </action>
  <verify>
    <automated>test -f .planning/phases/01-foundation-typography-gate/font-swap-dry-run.md && grep -q "TYPE-06 holds" .planning/phases/01-foundation-typography-gate/font-swap-dry-run.md && grep -q "UKR-05 audit" .planning/phases/01-foundation-typography-gate/font-swap-dry-run.md && grep -q "Single-file font swap proven" .planning/phases/01-foundation-typography-gate/font-swap-dry-run.md && pnpm build && (grep -rnE 'new Intl\.[A-Z][A-Za-z]*' src/ --include='*.ts' | grep -v 'src/lib/intl.ts' | wc -l | grep -q '^0$')</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/01-foundation-typography-gate/font-swap-dry-run.md` exists
    - File contains a recorded edit, the resulting `git status` showing exactly 1 file modified, and the conclusion `TYPE-06 holds`
    - File contains a `## UKR-05 audit` section listing findings
    - The Intl audit reports zero `new Intl.*` calls outside `src/lib/intl.ts` (verified by grep returning empty for non-intl.ts paths)
    - The bare-toLocale audit reports zero violations outside `src/__synthetic__/`
    - The current `_typography.scss` is reverted to its post-PLAN-03 state (no Literata leftover; build succeeds with Source Serif 4 stack intact)
  </acceptance_criteria>
  <done>TYPE-06 proven — single-file font swap is the only change needed for a font-pairing change. UKR-05 audited — Europe/Kyiv is the only TZ in use; no escape paths. Evidence committed as a markdown record.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Phase 1 exit verification — all five success criteria green</name>
  <what-built>
    Phase 1 is complete. This checkpoint confirms all five Phase 1 success criteria from `ROADMAP.md` are PASS:
    1. Glyph audit page renders critical Ukrainian glyphs in 3×4 family-style matrix
    2. Real-prose specimen shows ~62ch measure, ragged-right, no FOIT, no measurable CLS
    3. Font pairing swap is a single-file edit (`_typography.scss`) — proven by dry-run
    4. `<html lang="uk">`, `LOCALE_ID = 'uk-UA'`, `Intl.DateTimeFormat('uk-UA')` produces Ukrainian under force-en
    5. `.env` gitignored, gitleaks pre-commit hook installed and tested, `docs/force-en-audit.md` exists and run once
  </what-built>
  <how-to-verify>
    1. **Success criterion 1 (TYPE-01, TYPE-02, TYPE-04):** Confirm PLAN 05's checkpoint (Task 3) PASS recorded in `01-05-SUMMARY.md`. Re-walk if needed.
    2. **Success criterion 2 (TYPE-07):** Confirm `docs/typography-checklist.md` "Specimen checks" all PASS in the run record.
    3. **Success criterion 3 (TYPE-06):** Read `.planning/phases/01-foundation-typography-gate/font-swap-dry-run.md` and confirm `TYPE-06 holds` is recorded.
    4. **Success criterion 4 (UKR-01, UKR-04, UKR-06):** Confirm `docs/force-en-audit.md` "Run record — Phase 1" shows all checkboxes PASS, including the force-en walk under DevTools locale = `en-US`.
    5. **Success criterion 5 (UKR-05, UKR-06, TYPE-10):**
       - `git check-ignore .env` returns `.env` (it IS gitignored).
       - `cat .git/hooks/pre-commit` exists (pre-commit installed).
       - Test gitleaks: `echo "aws_secret_access_key=AKIAIOSFODNN7EXAMPLE/wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY" > /tmp/sec && cd "$REPO_ROOT" && cp /tmp/sec .gitleaks-test && git add .gitleaks-test && git commit -m "should fail"` — MUST fail with gitleaks error. Then `git rm --cached .gitleaks-test && rm .gitleaks-test`.
       - `docs/force-en-audit.md` exists and the Phase 1 run record exists.
    6. **CI sanity:** `pnpm install --frozen-lockfile && pnpm lint && pnpm lint:verify-rule && pnpm test --run && pnpm build` all exit 0.
    7. **Pre-commit sanity:** `pre-commit run --all-files` exits 0.
    8. **Manual rule-removal counter-test (B3 direction 2 from PLAN 04):**
       **Precondition:** Abort if `git status --short eslint.config.js` is non-empty before starting (the working tree for this file MUST be clean — otherwise the restore step cannot reliably revert to the post-PLAN-04 state).
       Temporarily comment out the `no-restricted-syntax` block in `eslint.config.js`. Run `pnpm lint:verify-rule`. It MUST exit 1 with stdout containing `FAIL: rule did not fire on synthetic violation`. Restore the rule (uncomment).
       **Post-restore guard:** Run `git diff eslint.config.js` and confirm output is empty (zero diff vs. the pre-test state). If `git diff` shows any residual change, the restore was incomplete — manually `git checkout -- eslint.config.js` and re-run the verification.
       Re-run `pnpm lint:verify-rule` — it MUST exit 0 with `PASS: rule fired on synthetic violation`. This proves the rule is the actual signal (not a false-positive PASS from a buggy script). Record both runs in `01-06-SUMMARY.md`.
    9. **Open hand-off — first-push CI verification:** the `.github/workflows/ci.yml` and gitleaks-action workflow cannot be exercised without pushing to GitHub. Record in `01-06-SUMMARY.md` (and mirror into `.planning/STATE.md` under "Open hand-offs"): "First push to GitHub MUST show green CI on the `lint && lint:verify-rule && test --run && build` workflow AND on the gitleaks-action workflow. If red on first push, reopen PLAN 06." This converts the unverifiable-locally gap into a tracked open commitment that surfaces on the next `/gsd-progress` run.

    Report PASS or FAIL on each criterion with evidence path.
  </how-to-verify>
  <resume-signal>Type "phase 1 approved" if all five criteria are PASS, or list FAILs with required fix.</resume-signal>
  <acceptance_criteria>
    - All 5 success criteria from ROADMAP.md §"Phase 1" verified PASS with evidence path
    - `pnpm install --frozen-lockfile && pnpm lint && pnpm lint:verify-rule && pnpm test --run && pnpm build` all exit 0
    - `pre-commit run --all-files` exits 0
    - gitleaks blocks a synthetic AWS-key commit attempt during the human verification
  </acceptance_criteria>
  <done>Phase 1 closed. Foundation locked. Hand-off to Phase 2 (Primitives).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev workstation → repo | Pre-commit gate intercepts secret leaks before commit |
| repo → CI (GitHub Actions) | CI re-runs gitleaks against full history; secondary gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-18 | Information disclosure | Secrets committed via `.env` or arbitrary file | mitigate | `.env` gitignored (PLAN 01); gitleaks pre-commit (this plan); gitleaks-action in CI (this plan); manual fake-AWS-key test verifies hook fires |
| T-01-19 | Tampering | A future contributor disables the pre-commit hook locally (`git commit --no-verify`) | mitigate | CI gitleaks-action runs on every push, catches what local hook bypasses |
| T-01-20 | Denial of service | Pre-commit slow on large diffs (developer disables it) | accept | Hooks scoped to staged files; gitleaks scan is fast; revisit if pain emerges |
| T-01-21 | Elevation of privilege | GitHub Actions workflow uses excessive permissions | mitigate | Default `GITHUB_TOKEN` permissions inherited (read-only by default in modern repo settings); no custom secrets defined in this plan |
</threat_model>

<verification>
- `pre-commit run --all-files` exits 0
- gitleaks pre-commit blocks a synthetic AWS-key commit
- `.github/workflows/ci.yml` runs all four pnpm scripts plus gitleaks-action
- Both docs (`force-en-audit.md`, `typography-checklist.md`) exist with > 50 / > 80 lines and have run records
- `font-swap-dry-run.md` records TYPE-06 evidence
- Phase 1 exit checkpoint reports PASS on all 5 success criteria
</verification>

<success_criteria>
- TYPE-03: FOUT/CLS regression gates re-run against the live site via `docs/typography-checklist.md` "Specimen checks" run record (no FOIT, CLS < 0.05)
- TYPE-06: Single-file font-pairing swap proven (font-swap-dry-run.md)
- TYPE-10: SCSS architecture (global tokens + base; component co-location) finalized; pre-commit + CI guard the conventions
- UKR-05: Europe/Kyiv audit confirms no escape paths from `src/lib/intl.ts`
- UKR-06: Force-en audit doc filled in AND run once for Phase 1 (run record committed)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-typography-gate/01-06-SUMMARY.md` summarizing:
- Pre-commit hooks installed (list)
- gitleaks test result (BLOCKED a fake AWS key commit — confirmed)
- CI workflow steps + first-push readiness
- Both docs' line counts + run-record results (PASS/FAIL counts)
- TYPE-06 dry-run swap conclusion
- UKR-05 audit conclusion
- Phase 1 exit checkpoint outcome (all five success criteria)
- Hand-off note to Phase 2: foundation locked, primitives can build against `var(--…)` tokens; the harness page (`/dev/glyph-audit`) remains the regression check that primitives will be tested against next.
</output>
</output>

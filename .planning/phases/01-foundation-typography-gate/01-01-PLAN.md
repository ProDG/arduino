---
phase: 01-foundation-typography-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  # Files manually edited beyond ng-new defaults. Pure scaffolded artifacts
  # (tsconfig.*, vitest.config.ts, pnpm-lock.yaml) are generated, not authored,
  # and are intentionally excluded from this list.
  - package.json
  - .gitignore
  - .npmrc
  - angular.json
  - eslint.config.js
  - .stylelintrc.json
  - .prettierrc.json
  - .prettierignore
  - src/index.html
  - src/main.ts
  - src/app/app.config.ts
  - src/app/app.routes.ts
  - src/app/app.component.ts
  - src/app/app.component.html
  - src/app/app.component.scss
  - src/app/pages/home/home.component.ts
  - src/app/pages/home/home.component.html
  - src/app/pages/home/home.component.scss
  - src/app/pages/glyph-audit/glyph-audit.component.ts
  - src/app/pages/glyph-audit/glyph-audit.component.html
  - src/app/pages/glyph-audit/glyph-audit.component.scss
  - src/styles/main.scss
  - README.md
autonomous: true
requirements:
  - TYPE-10
user_setup: []

must_haves:
  truths:
    - "Running `pnpm install --frozen-lockfile` from a clean clone succeeds (Angular 21.2.x, zoneless, Vitest installed deterministically)"
    - "Running `pnpm build` from a clean clone produces a static `dist/.../browser/` (no Node SSR runtime artifacts)"
    - "Visiting `/` and `/dev/glyph-audit` in the built site returns prerendered HTML pages"
    - "Running `pnpm lint` and `pnpm format:check` exit 0 on the scaffolded tree"
    - "Running `pnpm test` finds and executes Vitest with zero failures on the scaffold"
  artifacts:
    - path: "angular.json"
      contains: '"outputMode": "static"'
    - path: "package.json"
      contains: '"@angular/core"'
    - path: "pnpm-lock.yaml"
      provides: "pnpm 10 dependency lock"
    - path: "eslint.config.js"
      provides: "ESLint 9 flat config"
    - path: ".stylelintrc.json"
      provides: "Stylelint 16 standard-scss config"
    - path: ".gitignore"
      contains: ".env"
    - path: "src/app/app.routes.ts"
      contains: "dev/glyph-audit"
    - path: "src/styles/main.scss"
      provides: "Aggregator entry — @forwards tokens + base (placeholders for now)"
  key_links:
    - from: "src/app/app.config.ts"
      to: "provideRouter(routes)"
      via: "Angular 21 standalone bootstrap"
      pattern: "provideRouter"
    - from: "angular.json"
      to: "src/styles/main.scss"
      via: "global styles entry"
      pattern: 'main\.scss'
---

<objective>
Scaffold the Angular 21.2.x app with `outputMode: "static"`, register two routes (`/` and `/dev/glyph-audit`) with placeholder components, and wire pnpm 10 + ESLint 9 (flat) + Stylelint 16 + Prettier 3. Establish the directory layout that PLANs 03/04/05 will populate. NO font-face, NO tokens, NO base CSS in this plan — those land in PLAN 03. NO locale registration — that lands in PLAN 04.

Purpose: Lock the toolchain and project skeleton so downstream plans (font pipeline, tokens, locale, harness) can land in parallel waves without bikeshedding the scaffold.
Output: A buildable, lintable Angular app skeleton with two empty routes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/01-foundation-typography-gate/01-CONTEXT.md
@.planning/phases/01-foundation-typography-gate/01-UI-SPEC.md
@.planning/research/STACK.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Scaffold Angular 21 app with pnpm 10 + outputMode static</name>
  <read_first>
    - .planning/phases/01-foundation-typography-gate/01-CONTEXT.md (D-22, D-23, D-24, D-26, D-29, D-30 — exact scaffold scope)
    - .planning/research/STACK.md §2 (Angular 21 SSG) and §5 (Tooling versions)
    - CLAUDE.md (no Tailwind, no Node SSR runtime, FE-first)
  </read_first>
  <action>
    **Executor note (high file-modification count):** Most files in `files_modified` are auto-generated by `ng new`; executor should run `ng new` first then incrementally apply config edits, committing after each logical group (scaffold / pnpm / lint / test / locale).

    Run `pnpm dlx @angular/cli@21 new arduino-hub --style=scss --ssr=true --routing=true --package-manager=pnpm --skip-git` from a temp dir, then move generated files to repo root (do NOT nest in `arduino-hub/`). Reconfigure for SSG-only:

    1. In `angular.json` under `projects.arduino-hub.architect.build.options`, set `"outputMode": "static"` and ensure `"prerender": true`. Remove any `server` target if Angular CLI generated one specific to Node SSR runtime; keep prerender config only. Set `"styles": ["src/styles/main.scss"]`.
    2. `package.json`:
       - `"packageManager": "pnpm@10.0.0"` (or latest 10.x).
       - Scripts: `"dev": "ng serve"`, `"build": "ng build"`, `"test": "vitest run"`, `"lint": "eslint . && stylelint 'src/**/*.scss'"`, `"format": "prettier --write ."`, `"format:check": "prettier --check ."`.
       - DevDeps: `eslint@^9`, `angular-eslint@^21`, `typescript-eslint@^8`, `stylelint@^16`, `stylelint-config-standard-scss@^14`, `prettier@^3`, `prettier-plugin-organize-imports@^4`, `vitest@^2`, `@analogjs/vitest-angular` (Angular 21 Vitest preset) per STACK.md §2.
    3. `eslint.config.js` (flat) — extends angular-eslint recommended + typescript-eslint recommended. Add `no-restricted-syntax` rule placeholder (the actual rule body for `toLocaleString`/`toLocaleDateString` ban is added in PLAN 04 — leave a TODO comment referencing PLAN 04).
    4. `.stylelintrc.json` — `{ "extends": ["stylelint-config-standard-scss"] }`.
    5. `.prettierrc.json` — `{ "singleQuote": true, "trailingComma": "all", "printWidth": 100, "plugins": ["prettier-plugin-organize-imports"] }`.
    6. `.prettierignore` — `dist/`, `node_modules/`, `pnpm-lock.yaml`, `public/fonts/`, `fonts-source/`.
    7. `.npmrc` — `engine-strict=true`, `auto-install-peers=true`.
    8. `.gitignore` — append: `.env`, `.env.*`, `!.env.example`, `dist/`, `node_modules/`, `coverage/`, `.angular/`, `fonts-source/` (per D-08 — gitignore if heavy), `*.log`.
    9. Create directories (with `.gitkeep` if empty): `src/styles/tokens/`, `src/styles/base/`, `src/lib/`, `public/fonts/`, `scripts/fonts/`, `docs/`, `fonts-source/`.
    10. Run `pnpm install` to generate `pnpm-lock.yaml`. Commit lockfile.

    Reference D-22 (single Angular app, no core-ui in P1), D-23 (folder layout), D-29 (`pnpm build` → `dist/browser/`), D-30 (tooling versions).
  </action>
  <verify>
    <automated>pnpm install --frozen-lockfile && pnpm build && test -d dist/arduino-hub/browser -o -d dist/browser && grep -q '"outputMode": "static"' angular.json && grep -q '\.env' .gitignore</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` declares `"packageManager": "pnpm@10.x"` (grep `pnpm@10`)
    - `angular.json` contains `"outputMode": "static"` (grep)
    - `pnpm-lock.yaml` exists at repo root and is committed
    - `eslint.config.js` exists and is flat-config (uses `export default [...]`)
    - `.stylelintrc.json` exists with `stylelint-config-standard-scss` extension
    - `.gitignore` contains `.env`, `dist/`, `node_modules/`, `fonts-source/`
    - Directories exist: `src/styles/tokens/`, `src/styles/base/`, `src/lib/`, `public/fonts/`, `scripts/fonts/`, `docs/`
    - `pnpm install --frozen-lockfile` succeeds
    - `pnpm build` succeeds and produces `dist/.../browser/index.html`
    - No `arduino-ssr.service` referenced anywhere; no `server.ts` runtime entry (prerender config only)
  </acceptance_criteria>
  <done>Angular 21 SSG app scaffolded; `pnpm build` produces a static `dist/.../browser/` folder; toolchain (ESLint/Stylelint/Prettier/Vitest) wired and runnable; lockfile committed.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Register two routes (/, /dev/glyph-audit) with placeholder components and main.scss aggregator</name>
  <read_first>
    - .planning/phases/01-foundation-typography-gate/01-UI-SPEC.md §"The Placeholder Root Page — /" and §"The Harness Page — /dev/glyph-audit" (locked Ukrainian copy, page chrome rules)
    - .planning/phases/01-foundation-typography-gate/01-CONTEXT.md (D-25 — two routes only; D-24 — main.scss aggregates tokens + base; D-26 — provideRouter, no provideClientHydration)
    - src/app/app.config.ts (current scaffold output to know what to modify)
    - src/index.html (current scaffold output)
  </read_first>
  <action>
    1. `src/index.html`:
       - Set `<html lang="uk">` (D-27).
       - `<title>Arduino UA</title>`.
       - Add `<meta charset="utf-8">`, `<meta name="viewport" content="width=device-width, initial-scale=1">`.
       - Leave `<head>` slots for font preloads (added in PLAN 03) — add HTML comment `<!-- font preloads injected in PLAN 03 -->`.
    2. `src/app/app.routes.ts` — export `routes: Routes = [...]` with:
       ```ts
       { path: '', loadComponent: () => import('./pages/home/home.component').then(m => m.HomeComponent), title: 'Arduino UA' },
       { path: 'dev/glyph-audit', loadComponent: () => import('./pages/glyph-audit/glyph-audit.component').then(m => m.GlyphAuditComponent), title: 'Гліф-аудит — Arduino UA' },
       ```
    3. `src/app/app.config.ts` — `ApplicationConfig` exporting `providers: [provideRouter(routes), provideZonelessChangeDetection()]`. **Do NOT add** `provideClientHydration` (D-26: SSG only, no hydration). The `LOCALE_ID` provider and `registerLocaleData` call are added in PLAN 04 — leave a TODO comment `// LOCALE_ID + registerLocaleData added in PLAN 04 (UKR-01)`.
    4. `src/app/app.component.ts/html/scss` — minimal shell: standalone component with `<router-outlet />` only. No header, no footer (D-25).
    5. `src/app/pages/home/home.component.ts/html/scss`:
       - Standalone component, selector `app-home`.
       - Template per UI-SPEC §"The Placeholder Root Page" — exact Ukrainian copy:
         ```html
         <article class="home">
           <h1>Arduino UA</h1>
           <p class="lede">Українська онлайн-книга про Arduino. У розробці.</p>
           <p>Фундамент готовий: типографіка, шрифти, локаль, токени.
              Подальші розділи з'являться у наступних фазах.</p>
           <p>Перевірка типографіки та кириличного покриття:
              <a href="/dev/glyph-audit">/dev/glyph-audit</a>.</p>
         </article>
         ```
       - SCSS: minimal — leave classes only; styling will pick up from base CSS in PLAN 03 (`.home { min-height: 100vh; display: grid; place-content: center; }` is OK to add now).
    6. `src/app/pages/glyph-audit/glyph-audit.component.ts/html/scss`:
       - Standalone component, selector `app-glyph-audit`.
       - Template: `<article class="glyph-audit"><h1>Гліф-аудит</h1><p>Розмітка матриці гліфів — деталі у PLAN 05.</p></article>` (placeholder; PLAN 05 fills sections 1/2/3).
       - SCSS file empty (PLAN 05 fills it).
       - Add Angular `Meta` service in component constructor to set `<meta name="robots" content="noindex">` (D-15) — use `inject(Meta).addTag({ name: 'robots', content: 'noindex' })` in `ngOnInit` or constructor.
    7. `src/styles/main.scss` — aggregator:
       ```scss
       // Tokens are added in PLAN 03
       // @use './tokens';
       // Base CSS is added in PLAN 03
       // @use './base/base';
       ```
       Active commented placeholders are intentional — PLAN 03 uncomments and fills.
    8. `src/main.ts` — `bootstrapApplication(AppComponent, appConfig)`.
    9. `README.md` (project root, brief, Ukrainian-aware): one paragraph English describing the project, plus a "Local dev" section: `pnpm install`, `pnpm dev`, `pnpm build`, `pnpm lint`, `pnpm test`, `pnpm format`. Link to `.planning/ROADMAP.md`.

    Reference D-25 (only two routes), D-26 (no hydration), D-27 (`<html lang="uk">`).
  </action>
  <verify>
    <automated>pnpm build && grep -q '<html lang="uk">' src/index.html && grep -q "dev/glyph-audit" src/app/app.routes.ts && grep -q "provideRouter" src/app/app.config.ts && ! grep -q "provideClientHydration" src/app/app.config.ts && grep -q "Гліф-аудит" src/app/pages/glyph-audit/glyph-audit.component.html</automated>
  </verify>
  <acceptance_criteria>
    - `src/index.html` contains `<html lang="uk">` and `<title>Arduino UA</title>` (grep)
    - `src/app/app.routes.ts` registers exactly two paths: `''` and `'dev/glyph-audit'` (grep both literals)
    - `src/app/app.config.ts` calls `provideRouter(routes)` and `provideZonelessChangeDetection()` (grep both)
    - `src/app/app.config.ts` does NOT contain `provideClientHydration` (grep -v)
    - `src/app/pages/home/home.component.html` contains the four locked Ukrainian copy strings (grep `Українська онлайн-книга`, `Фундамент готовий`)
    - `src/app/pages/glyph-audit/glyph-audit.component.ts` adds `<meta name="robots" content="noindex">` via Angular `Meta` service (grep `noindex`)
    - `src/styles/main.scss` exists with commented placeholders for tokens and base (grep `// @use './tokens'`)
    - `pnpm build` succeeds and `dist/.../browser/index.html` contains `lang="uk"` (grep)
    - `dist/.../browser/dev/glyph-audit/index.html` exists (prerender produced it)
  </acceptance_criteria>
  <done>Two routes prerendered. `<html lang="uk">` set. Glyph audit page emits `noindex` meta. main.scss aggregator placeholder ready for PLAN 03 to fill.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev machine → repo | Untrusted code/configs land via dependency installs and CLI scaffolding |
| repo → CI/build | Built artifacts shipped from this codebase run on the dev machine and (later) production |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information disclosure | `.env` accidentally committed | mitigate | `.env`, `.env.*` patterns added to `.gitignore` in this plan; gitleaks pre-commit hook in PLAN 06 enforces |
| T-01-02 | Tampering | Unpinned dependency versions | mitigate | `pnpm-lock.yaml` committed; `--frozen-lockfile` enforced in CI (PLAN 06) |
| T-01-03 | Tampering | Angular CLI scaffold introduces unwanted Node SSR runtime | mitigate | Verify no `server.ts` runtime entry; `outputMode: "static"` in angular.json; acceptance criterion checks |
| T-01-04 | Spoofing | Malicious npm package published under typosquat | accept | Trusting npm registry + `pnpm` integrity hashes via lockfile; revisit if incident |
</threat_model>

<verification>
- `pnpm install --frozen-lockfile` succeeds from a clean clone
- `pnpm build` produces `dist/.../browser/` with prerendered `index.html` and `dev/glyph-audit/index.html`
- `pnpm lint` runs without errors (rules may be permissive; PLAN 04 tightens)
- No `provideClientHydration` registered, no `server.ts` runtime entry
</verification>

<success_criteria>
- Buildable Angular 21 SSG skeleton committed
- Two prerendered routes (`/`, `/dev/glyph-audit`) ship Ukrainian copy in HTML
- Toolchain (pnpm 10, ESLint 9, Stylelint 16, Prettier 3, Vitest) wired and runnable
- `<html lang="uk">` + `noindex` on harness page in place
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-typography-gate/01-01-SUMMARY.md` summarizing:
- Angular version + zoneless confirmation
- Tooling versions (pnpm/ESLint/Stylelint/Prettier/Vitest)
- Folder layout established
- Routes registered
- Open hand-offs to PLAN 02 (font pipeline), PLAN 03 (tokens + base), PLAN 04 (locale), PLAN 05 (harness sections)
</output>
</output>

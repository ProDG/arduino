---
phase: 01-foundation-typography-gate
plan: 05
type: execute
wave: 3
depends_on: [01-02, 01-03, 01-04]
files_modified:
  - src/app/pages/glyph-audit/glyph-audit.component.ts
  - src/app/pages/glyph-audit/glyph-audit.component.html
  - src/app/pages/glyph-audit/glyph-audit.component.scss
  - src/app/pages/glyph-audit/verification-string.const.ts
  - src/app/pages/glyph-audit/specimen-prose.const.ts
  - src/app/app.routes.ts
  - public/robots.txt
autonomous: false

requirements:
  - TYPE-01
  - TYPE-02
  - TYPE-03
  - TYPE-04
  - TYPE-07

must_haves:
  truths:
    - "/dev/glyph-audit renders a 12-cell matrix (3 families × 4 styles) with the canonical verification string at body and 64px display sizes"
    - "Each matrix cell shows isolated critical glyphs `і ї є ґ Ї Є Ґ ʼ` at 96px"
    - "Real-prose specimen page (Ukrainian Arduino content, NOT Lorem Ipsum) renders at ~62ch, ragged-right"
    - "Locale demo renders date, number, sorted list using formatDateUk / formatNumberUk / collatorUk"
    - "Page emits <meta name='robots' content='noindex'> and is excluded from sitemap (robots.txt)"
  artifacts:
    - path: "src/app/pages/glyph-audit/glyph-audit.component.html"
      provides: "Three sections: glyph matrix, real-prose specimen, locale demo"
    - path: "src/app/pages/glyph-audit/verification-string.const.ts"
      provides: "Canonical verification string (D-17) as exported constant — single source of truth"
      contains: "Ґаздиня"
    - path: "src/app/pages/glyph-audit/specimen-prose.const.ts"
      provides: "Real Ukrainian Arduino prose specimen as structured constants"
      contains: "ATmega328P"
    - path: "public/robots.txt"
      provides: "Disallow: /dev/"
  key_links:
    - from: "glyph-audit.component.ts"
      to: "src/lib/intl"
      via: "imports formatDateUk, formatNumberUk, collatorUk"
      pattern: "from ['\"].*lib/intl"
    - from: "glyph-audit.component.html"
      to: "verification-string.const"
      via: "renders the canonical string in 12 cells"
      pattern: "VERIFICATION_STRING|verificationString"
---

<objective>
Build the `/dev/glyph-audit` triple-duty harness page (D-15): Section 1 (12-cell glyph matrix), Section 2 (real Ukrainian Arduino prose specimen at 62ch), Section 3 (locale demo using `src/lib/intl.ts` from PLAN 04). The page is the executable phase-exit gate for TYPE-01, TYPE-02, TYPE-03, TYPE-04, TYPE-07. End-of-plan checkpoint: human walks the page through `docs/typography-checklist.md` (the doc itself ships in PLAN 06).

Purpose: One page proves the entire foundation. If `ґ` doesn't render, we see it here. If italic Cyrillic looks synthesized, we see it here. If `Intl.DateTimeFormat('uk-UA')` returns English, we see it here. If FOIT or measurable CLS regress, we see it here. This page exists to fail loudly on regression — including after a font swap or font release bump.
Output: The harness page rendering all three sections with real content; checkpoint for visual verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/01-foundation-typography-gate/01-CONTEXT.md
@.planning/phases/01-foundation-typography-gate/01-UI-SPEC.md
@.planning/phases/01-foundation-typography-gate/01-03-SUMMARY.md
@.planning/phases/01-foundation-typography-gate/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author canonical constants (verification string + specimen prose)</name>
  <read_first>
    - .planning/phases/01-foundation-typography-gate/01-CONTEXT.md (D-17 — canonical verification string verbatim; Claude's discretion on specimen prose wording but must include ґ ї є і, code block, italic, bold, em-dash, en-dash, «…»)
    - .planning/phases/01-foundation-typography-gate/01-UI-SPEC.md §"Section 2 — Real-prose specimen" (locked H1, lede, H2, comment, figure caption strings; locked content sequence)
    - .planning/phases/01-foundation-typography-gate/01-UI-SPEC.md §"Copywriting Contract" (every locked Ukrainian string)
  </read_first>
  <action>
    1. **Create `src/app/pages/glyph-audit/verification-string.const.ts`** — single source of truth for the canonical string (D-17):
       ```ts
       /**
        * The canonical Ukrainian verification string. D-17 in 01-CONTEXT.md.
        * Exercises: ґ Ґ ї Ї є і ' (apostrophe ʼ U+02BC), em-dash —, «…» quotes, "…" inner quotes,
        * Latin (Arduino model names) interleaved with Cyrillic.
        */
       export const VERIFICATION_STRING =
         'Ґаздиня їсть її їжу — є ґедзь, ґніт, ґанок. Цей рядок має бути ідеально набраним. ATmega328P, INPUT_PULLUP. «Лапки» „вкладені" — апостроф ʼ.';

       export const CRITICAL_GLYPHS = 'і ї є ґ Ї Є Ґ ʼ';
       ```
       The apostrophe character MUST be U+02BC (`ʼ`), NOT U+0027 (`'`). Verify by reading the file back through a hex tool or `python3 -c "import sys; print(hex(ord(sys.stdin.read().strip()[-2])))"` against the last `ʼ`.

    2. **Create `src/app/pages/glyph-audit/specimen-prose.const.ts`** — the real Ukrainian Arduino prose specimen, locked from UI-SPEC §"Suggested specimen prose":
       ```ts
       export const SPECIMEN_H1 = 'Перший крок: світлодіод, що блимає';

       export const SPECIMEN_LEDE =
         'Це найпростіша і водночас найважливіша вправа в роботі з Arduino — ваш «Hello, World!» у світі електроніки.';

       export const SPECIMEN_BODY_PARAS = [
         'Плата Arduino Uno побудована на мікроконтролері <strong>ATmega328P</strong>. Вбудований світлодіод підʼєднано до цифрового виходу <code>pin 13</code> через резистор — саме тому ми можемо керувати ним <em>без жодних додаткових компонентів</em>. Достатньо завантажити скетч і спостерігати, як ґніт цифрового життя загорається й гасне з інтервалом 5–7 разів на секунду.',
       ];

       export const SPECIMEN_H2 = 'Налаштування виходу';

       export const SPECIMEN_AFTER_H2 =
         'У функції <code>setup()</code> ми один раз повідомляємо мікроконтролеру, що <code>pin 13</code> має працювати у режимі виходу. Ґаздиня цифрового світу — функція <code>pinMode()</code> — приймає номер ніжки та режим: <code>OUTPUT</code> або <code>INPUT_PULLUP</code>.';

       export const SPECIMEN_CODE = `void setup() {
  pinMode(LED_BUILTIN, OUTPUT);
}

void loop() {
  // блимаємо світлодіодом
  digitalWrite(LED_BUILTIN, HIGH);
  delay(500);
  digitalWrite(LED_BUILTIN, LOW);
  delay(500);
}`;

       export const SPECIMEN_FIGURE_CAPTION =
         'Електрична схема ілюструє, як струм проходить через вбудований резистор плати до світлодіода й назад до спільної землі.';

       export const SPECIMEN_H3 = 'Що далі';

       export const SPECIMEN_AFTER_H3 =
         'У наступному розділі ми навчимося читати стан кнопки через цифровий вхід і керувати світлодіодом за допомогою функції <code>digitalRead()</code>.';

       export const SPECIMEN_ASIDE =
         'У режимі <code>INPUT_PULLUP</code> мікроконтролер активує внутрішній резистор підтяжки до живлення — це позбавляє схему від зайвої деталі та робить кнопку логічно інверсною.';
       ```
       Apostrophe (`ʼ` U+02BC) used as `ʼ` to avoid editor encoding drift. Em-dash `—` U+2014, en-dash `–` U+2013, `«…»` U+00AB/U+00BB used verbatim.

       The HTML inside body strings (e.g., `<strong>`, `<em>`, `<code>`) is intentional — the component renders it via `[innerHTML]` (acceptable here because the prose is a hardcoded constant, NOT user input; no XSS surface).

    3. **Verify** apostrophe encoding: `python3 -c "with open('src/app/pages/glyph-audit/verification-string.const.ts') as f: s=f.read(); assert '\\u02BC' in s or 'ʼ' in s, 'apostrophe wrong codepoint'; assert ''' not in s.split('//')[0], 'straight apostrophe found'"` — or simpler: `grep -c $'\xca\xbc' src/app/pages/glyph-audit/verification-string.const.ts` returns ≥ 1 (UTF-8 bytes for U+02BC).

    Reference D-17 (verification string verbatim), UI-SPEC §"Copywriting Contract" (locked strings).
  </action>
  <verify>
    <automated>test -f src/app/pages/glyph-audit/verification-string.const.ts && grep -q "Ґаздиня їсть її їжу" src/app/pages/glyph-audit/verification-string.const.ts && grep -q "ATmega328P" src/app/pages/glyph-audit/verification-string.const.ts && python3 -c "f=open('src/app/pages/glyph-audit/verification-string.const.ts','rb').read(); assert b'\\xca\\xbc' in f, 'U+02BC apostrophe missing'; print('OK')" && test -f src/app/pages/glyph-audit/specimen-prose.const.ts && grep -q "Перший крок: світлодіод, що блимає" src/app/pages/glyph-audit/specimen-prose.const.ts && grep -q "блимаємо світлодіодом" src/app/pages/glyph-audit/specimen-prose.const.ts</automated>
  </verify>
  <acceptance_criteria>
    - `verification-string.const.ts` exports `VERIFICATION_STRING` containing `Ґаздиня`, `їсть її їжу`, `ґедзь, ґніт, ґанок`, `ATmega328P`, `INPUT_PULLUP`, `«Лапки» „вкладені"`, and the typographic apostrophe `ʼ` U+02BC (verified by UTF-8 byte sequence `0xCA 0xBC`)
    - `verification-string.const.ts` exports `CRITICAL_GLYPHS = 'і ї є ґ Ї Є Ґ ʼ'`
    - `specimen-prose.const.ts` exports H1, lede, body paragraphs, H2, code block, figure caption, H3, aside — every constant from the action's list
    - Specimen prose contains at least one `ґ` (grep), one `ї`, one `є`, one inline `<em>`, one `<strong>`, one `<code>`, em-dash `—`, en-dash `5–7`, `«Hello, World!»` (Ukrainian quotes around English phrase)
    - Code block contains `pinMode(LED_BUILTIN, OUTPUT)`, `digitalWrite`, and the Ukrainian comment `// блимаємо світлодіодом`
  </acceptance_criteria>
  <done>Canonical constants committed. The specimen content is real Ukrainian Arduino prose (NOT Lorem Ipsum). Apostrophe encoding verified.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Build the harness page component (matrix + specimen + locale demo) and exclude from sitemap</name>
  <read_first>
    - .planning/phases/01-foundation-typography-gate/01-UI-SPEC.md §"The Harness Page — `/dev/glyph-audit`" (full page chrome + section 1 grid layout + section 2 specimen sequence + section 3 locale demo + audit footer note)
    - .planning/phases/01-foundation-typography-gate/01-CONTEXT.md (D-15 noindex, D-16 12-cell matrix, D-18 specimen, D-19 locale demo, D-20 raw HTML elements not primitives)
    - src/app/pages/glyph-audit/glyph-audit.component.ts (current placeholder from PLAN 01)
    - src/lib/intl.ts (PLAN 04 — formatDateUk, formatNumberUk, collatorUk)
    - src/styles/tokens/_typography.scss (tokens consumed via var(--…))
  </read_first>
  <action>
    1. **`glyph-audit.component.ts`** — standalone Angular component with:
       - `Meta` service: ensure `<meta name="robots" content="noindex">` (already added in PLAN 01; verify still present after edits).
       - `Title` service: `'Гліф-аудит — Arduino UA'`.
       - Imports: `import { VERIFICATION_STRING, CRITICAL_GLYPHS } from './verification-string.const';`, all specimen constants, `import { formatDateUk, formatNumberUk, collatorUk } from '../../../lib/intl';`.
       - Component fields:
         - `families = [{ name: 'Source Serif 4', cssVar: 'var(--font-body)' }, { name: 'Inter', cssVar: 'var(--font-ui)' }, { name: 'JetBrains Mono', cssVar: 'var(--font-mono)' }];`
         - `styles = [{ label: 'regular', style: 'normal', weight: 400 }, { label: 'italic', style: 'italic', weight: 400 }, { label: 'bold', style: 'normal', weight: 600 }, { label: 'bold-italic', style: 'italic', weight: 600 }];`
         - `verificationString = VERIFICATION_STRING; criticalGlyphs = CRITICAL_GLYPHS;`
         - `today = new Date(); formattedDate = formatDateUk(this.today); fixedDate = new Date('2026-04-30T12:00:00Z'); formattedFixedDate = formatDateUk(this.fixedDate); formattedNumber = formatNumberUk(1234567.89); sortedWords = ['ялинка', 'абрикос', 'ґніт', 'їжак', 'буряк'].sort(collatorUk().compare);`
         - The fixed date `2026-04-30T12:00:00Z` is the deterministic year-round verification anchor — it MUST always render as `30 квітня 2026 р.` regardless of when the test runs. The live `today` is also rendered, but its month name varies and so it is NOT used in automated assertions.
       - Template wired to `glyph-audit.component.html`.

    2. **`glyph-audit.component.html`** — three sections:

       **Section 1 — Matrix (D-16, UI-SPEC §"Section 1 — Glyph matrix"):**
       ```html
       <article class="glyph-audit">
         <header class="page-header">
           <h1>Гліф-аудит</h1>
           <p class="lede">Перевірка кириличного покриття для Source Serif 4, Inter та JetBrains Mono — кожна вага, кожен стиль.</p>
         </header>

         <section class="matrix">
           @for (family of families; track family.name) {
             @for (style of styles; track style.label) {
               <div class="cell"
                    [style.font-family]="family.cssVar"
                    [style.font-style]="style.style"
                    [style.font-weight]="style.weight">
                 <div class="cell-label">{{ family.name }} · {{ style.label }} · {{ style.weight }}</div>
                 <div class="cell-body">{{ verificationString }}</div>
                 <div class="cell-display">{{ verificationString }}</div>
                 <hr>
                 <div class="cell-glyphs">{{ criticalGlyphs }}</div>
               </div>
             }
           }
         </section>

         <hr class="section-rule">

         <!-- Section 2 — specimen -->
         <section class="specimen">
           <h1>{{ specimenH1 }}</h1>
           <p class="lede">{{ specimenLede }}</p>
           @for (para of specimenBodyParas; track $index) {
             <p [innerHTML]="para"></p>
           }
           <h2>{{ specimenH2 }}</h2>
           <p [innerHTML]="specimenAfterH2"></p>
           <pre><code>{{ specimenCode }}</code></pre>
           <figure>
             <div class="figure-placeholder" aria-label="Електрична схема (плейсхолдер)"></div>
             <figcaption>
               <span class="figure-num">Рис. 1</span>{{ specimenFigureCaption }}
             </figcaption>
           </figure>
           <h3>{{ specimenH3 }}</h3>
           <p [innerHTML]="specimenAfterH3"></p>
           <aside [innerHTML]="specimenAside"></aside>
         </section>

         <hr class="section-rule">

         <!-- Section 3 — locale demo -->
         <section class="locale-demo">
           <h2>Локалізація</h2>
           <p><strong>Сьогодні (live):</strong> {{ formattedDate }}</p>
           <p><strong>Фіксована дата (для перевірки):</strong> <span data-testid="fixed-date">{{ formattedFixedDate }}</span></p>
           <p><strong>Число:</strong> {{ formattedNumber }}</p>
           <p><strong>Сортування:</strong></p>
           <ul>
             @for (word of sortedWords; track word) {
               <li>{{ word }}</li>
             }
           </ul>
           <p class="audit-note">
             Аудит: завантажте цю сторінку з налаштуваннями локалі браузера <code>en-US</code>.
             Дата, число та порядок сортування вище МАЮТЬ залишитись українськими.
             Якщо хоч щось перемкнулось на англійську — це регрес.
             Чек-ліст: <code>docs/force-en-audit.md</code>.
           </p>
         </section>
       </article>
       ```
       Add the specimen-prose constants as fields on the component class (`specimenH1`, `specimenLede`, `specimenBodyParas`, `specimenH2`, `specimenAfterH2`, `specimenCode`, `specimenFigureCaption`, `specimenH3`, `specimenAfterH3`, `specimenAside`) imported from `./specimen-prose.const`.

    3. **`glyph-audit.component.scss`** — co-located styles using `var(--…)` only (D-02). Per UI-SPEC §"Section 1" grid:
       ```scss
       .glyph-audit { padding: var(--space-6) var(--container-pad-mobile); max-width: var(--container-max); margin-inline: auto; }
       @media (min-width: 768px) { .glyph-audit { padding-inline: var(--container-pad-tablet); } }
       @media (min-width: 1200px) { .glyph-audit { padding-inline: var(--container-pad-desktop); } }

       .page-header { margin-block: var(--space-9) var(--space-7); }

       .matrix {
         display: grid;
         grid-template-columns: 1fr;
         row-gap: var(--space-8);
         column-gap: var(--space-6);
         margin-block: var(--space-7);
       }
       @media (min-width: 768px) { .matrix { grid-template-columns: repeat(2, 1fr); } }
       @media (min-width: 1200px) { .matrix { grid-template-columns: repeat(4, 1fr); } }

       .cell { padding: var(--space-5); }
       .cell-label { font-family: var(--font-ui); font-size: 13px; color: var(--color-ink-muted); margin-bottom: var(--space-3); }
       .cell-body { font-size: var(--text-body); line-height: 1.6; margin-bottom: var(--space-4); overflow-wrap: break-word; }
       .cell-display { font-size: var(--text-display-xl); line-height: 1.0; overflow: hidden; }
       .cell hr { margin: var(--space-3) 0; border: 0; border-top: 1px solid var(--color-rule); }
       .cell-glyphs { font-size: var(--text-glyph); line-height: 1.0; letter-spacing: 0.1em; }

       .section-rule { border: 0; border-top: 1px solid var(--color-rule); margin: var(--space-10) 0; }
       @media (max-width: 767px) { .section-rule { margin: var(--space-8) 0; } }

       .specimen { max-width: var(--measure-prose); margin-inline: auto; }
       .specimen h1 { margin-bottom: var(--space-5); }
       .specimen h2 { margin-block: var(--space-7) var(--space-4); }
       .specimen h3 { margin-block: var(--space-6) var(--space-4); }
       .specimen pre { margin-block: var(--space-6); }
       .specimen .figure-placeholder { width: 100%; aspect-ratio: 16/9; background: var(--color-rule); border-radius: var(--radius-sm); }

       .locale-demo { max-width: var(--measure-prose); margin-inline: auto; }
       .locale-demo h2 { margin-bottom: var(--space-5); }
       .locale-demo .audit-note {
         font-size: var(--text-caption); color: var(--color-ink-muted);
         border-left: 2px solid var(--color-accent); padding-left: var(--space-4);
         margin-top: var(--space-6);
       }
       ```

    4. **`public/robots.txt`** — exclude `/dev/` from indexing:
       ```
       User-agent: *
       Disallow: /dev/
       ```
       Verify Angular's build copies `public/robots.txt` into `dist/.../browser/robots.txt`. (Angular 21's `public/` directory is mapped to the build root by the `assets` config wired in PLAN 03 — no `src/robots.txt` is needed.)

    5. **Verify** `<meta name="robots" content="noindex">` is still in the rendered glyph-audit HTML after build.

    Reference D-15, D-16, D-18, D-19, D-20.
  </action>
  <verify>
    <automated>pnpm build && test -f dist/*/browser/dev/glyph-audit/index.html && grep -q 'name="robots" content="noindex"' dist/*/browser/dev/glyph-audit/index.html && grep -q "Гліф-аудит" dist/*/browser/dev/glyph-audit/index.html && grep -q "Ґаздиня" dist/*/browser/dev/glyph-audit/index.html && grep -q "Перший крок: світлодіод" dist/*/browser/dev/glyph-audit/index.html && grep -q "блимаємо світлодіодом" dist/*/browser/dev/glyph-audit/index.html && grep -q "Локалізація" dist/*/browser/dev/glyph-audit/index.html && grep -q "30 квітня 2026 р." dist/*/browser/dev/glyph-audit/index.html && grep -q "Фіксована дата" dist/*/browser/dev/glyph-audit/index.html && test -f dist/*/browser/robots.txt && grep -q "Disallow: /dev/" dist/*/browser/robots.txt</automated>
  </verify>
  <acceptance_criteria>
    - `glyph-audit.component.html` renders 12 cells via nested `@for` over 3 families × 4 styles (verify by counting `<div class="cell"` blocks in rendered HTML: 12)
    - Each cell binds `[style.font-family]`, `[style.font-style]`, `[style.font-weight]` (grep all three in component HTML)
    - Specimen section renders all 9 elements: H1, lede, body para, H2, after-H2 para, `<pre><code>`, `<figure>` with `<figcaption>`, H3, after-H3 para, `<aside>`
    - Locale demo imports `formatDateUk`, `formatNumberUk`, `collatorUk` from `src/lib/intl` (grep import path) — NO bare `toLocaleString` calls (`pnpm lint` passes)
    - Audit footer note references `docs/force-en-audit.md` (grep)
    - `public/robots.txt` exists with `Disallow: /dev/` (grep)
    - `pnpm build` succeeds; `dist/.../browser/dev/glyph-audit/index.html` exists
    - Built HTML contains: `Гліф-аудит`, `Ґаздиня`, `Перший крок: світлодіод`, `блимаємо світлодіодом`, `Локалізація`
    - Built HTML contains the deterministic fixed-date string `30 квітня 2026 р.` (anchored by the hardcoded `new Date('2026-04-30T12:00:00Z')` field; this assertion is year-round stable, NOT dependent on the current month)
    - Built HTML contains both the `Сьогодні (live):` and `Фіксована дата (для перевірки):` labels (grep both)
    - `dist/.../browser/robots.txt` contains `Disallow: /dev/`
    - `pnpm lint` exits 0 (component does not violate the no-bare-toLocale rule)
  </acceptance_criteria>
  <done>Glyph audit page renders all three sections from real prose + intl facade. Sitemap excludes /dev/. Build copies robots.txt to dist. Page is ready for visual verification checkpoint.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification — load /dev/glyph-audit and confirm all gates pass</name>
  <what-built>
    The `/dev/glyph-audit` page renders three sections backed by real woff2 fonts, real Ukrainian Arduino prose, and the `src/lib/intl.ts` facade. Every Phase 1 success criterion (1, 2, 4 of 5 — gates 3 and 5 are PLAN 06's responsibility) terminates here visually.
  </what-built>
  <how-to-verify>
    1. Run `pnpm build && pnpm dlx http-server dist/*/browser -p 4300` (or `pnpm dev` if dev-server SSR-equivalent SSG works).
    2. Open `http://localhost:4300/dev/glyph-audit` in a browser at viewport ≥1200px.
    3. **Section 1 — Glyph matrix (Phase 1 success criterion #1):**
       - 12 cells visible, arranged 3 rows × 4 columns at desktop.
       - Each cell label reads e.g. `Source Serif 4 · italic · 400`.
       - In every cell, the canonical string `Ґаздиня їсть її їжу — є ґедзь, ґніт, ґанок. ...` reads correctly.
       - **Critical:** in italic cells, italic Cyrillic letters look REAL (slanted with cursive forms — the `а` becomes a single-story italic, the `г` has a true italic form), NOT mechanically slanted upright glyphs.
       - **Critical:** every cell shows `ґ` (lowercase, with the upper-comma stroke) and `Ґ` (capital). NO tofu boxes (`□`). NO fallback rendering (the `ґ` shape must match the family — Source Serif 4 `ґ` differs from Inter `ґ`).
       - Isolated glyph row at 96px reveals diaeresis on `ї`/`Ї` is two distinct dots, the upper-comma on `ґ`/`Ґ` is correctly placed, and the typographic apostrophe `ʼ` is the modifier-letter shape (NOT a vertical line).
    4. **Section 2 — Real-prose specimen (Phase 1 success criterion #2; verifies TYPE-03 — FOUT not FOIT, no measurable CLS):**
       - Body text is in Source Serif 4, ~62ch line length on desktop (a paragraph wraps at roughly 62–65 Cyrillic characters per line).
       - Body is ragged-right (last word in each line stops at varying positions; NO `text-align: justify`).
       - **TYPE-03 FOUT check:** refresh with cache disabled (DevTools → Disable cache → Reload). Text should appear immediately in fallback (Georgia) and swap to Source Serif 4 within ~200ms. NO blank period where text is invisible (FOIT = regression).
       - **TYPE-03 CLS check:** in DevTools Performance, record reload — CLS should be 0 or <0.05 (Fontaine fallback metrics matched closely to Source Serif 4). Layout shift > 0.05 = Fontaine override drift = regression.
       - Inline `<code>` (`pin 13`, `INPUT_PULLUP`) renders in JetBrains Mono with a soft-warm-grey pill background.
       - `«Hello, World!»` Ukrainian quotes render correctly.
       - Code block `void setup() { ... }` renders in JetBrains Mono with the Arduino-teal hairline border. The Ukrainian comment `// блимаємо світлодіодом` renders correctly in mono — Cyrillic in mono is the regression risk.
       - `Рис. 1` figure-num is Arduino-teal `#007A80`.
    5. **Section 3 — Locale demo (Phase 1 success criterion #4):**
       - **Fixed date (for deterministic verification):** the line labeled `Фіксована дата (для перевірки):` reads exactly `30 квітня 2026 р.` (Ukrainian month name + Ukrainian "р." year suffix; day-month-year order; NOT `April 30, 2026`). This is the year-round-stable assertion anchored on `new Date('2026-04-30T12:00:00Z')`.
       - **Live date:** the line labeled `Сьогодні (live):` reads in Ukrainian — month name is in Ukrainian (e.g. `травня`, `червня`, etc., depending on when verified), the year suffix is `р.`, the order is day-month-year. NOT `April 30, 2026` and NOT any English month. This line is checked qualitatively (Ukrainian month name + `р.`), not against a specific literal string, because its value drifts month-to-month.
       - Number reads `1 234 567,89` with NBSP thousands separator and comma decimal.
       - Sort list order: `абрикос`, `буряк`, `ґніт`, `їжак`, `ялинка` (Ukrainian alphabet order — `ґ` after `б`/`в`/`г` words; here lands between `буряк` and `їжак` since no `г` words; `ї` after `і`-words; `ж`-words after `є`).
    6. **Force-en regression check (the live audit gate):**
       - In Chrome DevTools → Sensors → Locale → set to `en-US` → reload.
       - Repeat steps 4–5. The date, number, and sort order MUST stay Ukrainian. If they switch to English, the locale wiring is broken.
    7. **Mobile + tablet check:** resize browser to 375px and 900px. The matrix collapses to 1 column then 2 columns. The specimen and locale demo remain readable; no horizontal scroll.
    8. **HTML lang attribute:** `<html lang="uk">` is set (View Source).
    9. **Robots / sitemap:** `<meta name="robots" content="noindex">` is in the rendered head; `http://localhost:4300/robots.txt` returns `Disallow: /dev/`.

    Report PASS/FAIL for each numbered check above. If FAIL on any item, file the diagnosis (which font / which subset / which @font-face) so the executor can fix.
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe each failed check with diagnosis.</resume-signal>
  <acceptance_criteria>
    - All checks 3a–3i return PASS
    - No tofu boxes anywhere on the page
    - Italic Cyrillic looks real (cursive `а`, `г`, `и`)
    - `ґ` and `Ґ` render correctly in every family × style cell
    - Date / number / sort all stay Ukrainian under force-en browser locale
    - No CLS measurable on reload
  </acceptance_criteria>
  <done>Human-verified phase 1 success criteria 1, 2, 4 pass on real fonts + real prose + intl facade. Any FAIL is filed back to the executor for fix before phase exit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| component HTML constants → DOM via [innerHTML] | Pre-validated hardcoded prose (no user input); accept binding |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-15 | Information disclosure | Glyph-audit page indexed by search engines (internal namespace leaks) | mitigate | `<meta name="robots" content="noindex">` set in component; `public/robots.txt` Disallow: /dev/ |
| T-01-16 | Tampering | `[innerHTML]` binding XSS surface | accept | All bound HTML is hardcoded constants in `specimen-prose.const.ts` — zero user input; if future plans introduce user content here, reassess and switch to Angular DomSanitizer |
| T-01-17 | Tampering | Apostrophe encoding drifts to U+0027 in editor | mitigate | `verification-string.const.ts` uses `ʼ` and acceptance test verifies UTF-8 bytes 0xCA 0xBC |
</threat_model>

<verification>
- `pnpm build` produces `dist/.../browser/dev/glyph-audit/index.html` containing all three sections
- `dist/.../browser/robots.txt` excludes `/dev/`
- Human checkpoint passes 9 visual checks (matrix, specimen, locale demo, force-en regression, breakpoints, head metadata)
</verification>

<success_criteria>
- TYPE-01: Verified visually — Ukrainian glyph coverage (`і ї є ґ Ї Є Ґ ʼ`) renders in all 12 cells
- TYPE-02: Cyrillic-Ext effective — `ґ` renders in every family/style without fallback
- TYPE-03: Visually verified FOUT (no FOIT) and CLS < 0.05 on specimen reload — confirms PLAN 03's `font-display: swap` + Fontaine fallback metrics work end-to-end
- TYPE-04: Glyph audit harness page exists and visually verifies all critical glyphs across all weights/styles before any page templates are built
- TYPE-07: Type scale calibrated against real Ukrainian Arduino prose at ~62ch — verified on the specimen section
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-typography-gate/01-05-SUMMARY.md` summarizing:
- The three sections shipped + which Phase 1 success criterion each covers
- Checkpoint outcome (PASS or list of FAILs with diagnosis)
- Any calibration adjustments made (e.g., type scale shifted ±2px during execution)
- Hand-off note to PLAN 06: visual gates 1, 2, 4 passed; gates 3 (font-swap dry-run) and 5 (gitleaks + force-en audit doc) are PLAN 06's responsibility
</output>
</output>

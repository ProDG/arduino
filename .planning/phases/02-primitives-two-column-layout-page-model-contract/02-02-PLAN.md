---
phase: 02-primitives-two-column-layout-page-model-contract
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/content/models/block.ts
  - src/content/models/lesson.ts
  - src/content/models/article.ts
  - src/content/models/datasheet.ts
  - src/content/models/schematic.ts
  - src/content/models/index.ts
  - src/content/models/block.spec.ts
  - src/content/api/content-api.ts
  - src/content/api/mock-content-api.ts
  - src/content/api/mock-content-api.spec.ts
  - src/content/api/content-api.token.ts
  - src/assets/mock-data/lessons/pershyi-blymayuchyi-svitlodiod.json
  - src/assets/mock-data/lessons/knopka-ta-pidtyahuvalnyi-rezystor.json
  - src/assets/mock-data/lessons/analogovyi-vhid-ta-potentsiometr.json
  - src/assets/mock-data/articles/chomu-arduino.json
  - src/assets/mock-data/datasheets/atmega328p.json
  - src/assets/mock-data/datasheets/arduino-uno-r3.json
  - src/assets/mock-data/schematics/blymayuchyi-svitlodiod-shema.json
  - scripts/lint-fixtures.mjs
  - docs/copy-style-uk.md
  - package.json
autonomous: true
requirements: [CONTRACT-01, CONTRACT-03, CONTRACT-04, UKR-02, UKR-03]
must_haves:
  truths:
    - "TypeScript discriminated `Block` union with 10 variants is locked at `src/content/models/block.ts`"
    - "Page models `Lesson`, `Article`, `Datasheet`, `Schematic`, and `AnyPage` exist at `src/content/models/*.ts` matching UI-SPEC §Page models verbatim"
    - "`ContentApi` abstract class exists at `src/content/api/content-api.ts` with all 8 methods (4 get + 4 list)"
    - "`MockContentApi` reads `/assets/mock-data/{kind}/{slug}.json` via `fetch` and returns typed objects VERBATIM (no transformation)"
    - "`CONTENT_API` `InjectionToken` and `provideContentApi()` factory exist (consumed by Plan 06)"
    - "7 fixtures: 3 lessons, 1 article, 2 datasheets, 1 schematic — all real Ukrainian Arduino prose, slugs locked per D-MOCK-03"
    - "`scripts/lint-fixtures.mjs` warns on editorial smells AND enforces per-fixture content gates; wired into `pnpm lint`"
    - "`docs/copy-style-uk.md` exists and lists the typography forms required of authors/editors"
    - "Type-narrowing test in `block.spec.ts` proves `Block.type` discriminant correctly narrows fields per variant"
    - "`pnpm lint` passes (including the new fixture-lint script)"
  artifacts:
    - path: "src/content/models/block.ts"
      provides: "Locked `Block` discriminated union (10 variants)"
      contains: "type Block ="
    - path: "src/content/api/content-api.ts"
      provides: "Abstract `ContentApi` class"
      contains: "abstract class ContentApi"
    - path: "src/content/api/mock-content-api.ts"
      provides: "JSON fixture reader implementation"
      contains: "extends ContentApi"
    - path: "src/content/api/content-api.token.ts"
      provides: "`CONTENT_API` InjectionToken + `provideContentApi()` factory"
      contains: "InjectionToken<ContentApi>"
    - path: "scripts/lint-fixtures.mjs"
      provides: "Editorial-smell + content-gate fixture lint"
    - path: "docs/copy-style-uk.md"
      provides: "Author/editor typography style guide (UKR-02/UKR-03 reframing per D-PRE-03)"
  key_links:
    - from: "src/content/api/mock-content-api.ts"
      to: "src/assets/mock-data/lessons/*.json"
      via: "fetch('/assets/mock-data/lessons/' + slug + '.json')"
      pattern: "fetch.*assets/mock-data"
    - from: "package.json"
      to: "scripts/lint-fixtures.mjs"
      via: "lint script chain"
      pattern: "node scripts/lint-fixtures.mjs"
---

<objective>
Lock the TypeScript page-model contract that Wagtail must conform to in P4, deliver 7 real-Ukrainian-prose mock fixtures, and ship the editorial-smell + content-gate lint script. UKR-02 and UKR-03 are honored as authoring-contract requirements per CONTEXT D-PRE-01..05 (no transformation function — see superseded UI-SPEC §"Ukrainian Text Pre-processor").

Purpose: CONTRACT-01 + CONTRACT-03 + CONTRACT-04 + UKR-02/UKR-03 reframed.
Output: Locked content models, `ContentApi`/`MockContentApi`, 7 fixtures, lint script, style guide.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/phases/02-primitives-two-column-layout-page-model-contract/02-CONTEXT.md
@.planning/phases/02-primitives-two-column-layout-page-model-contract/02-UI-SPEC.md
@.planning/phases/02-primitives-two-column-layout-page-model-contract/02-PATTERNS.md
@scripts/fonts/subset.mjs
@src/lib/intl.ts
@src/app/pages/glyph-audit/specimen-prose.const.ts
@src/app/app.config.ts
</context>

<interfaces>
The locked `Block` union and page-model interfaces are reproduced verbatim from UI-SPEC §"Shared types" and §"Page models". Use these EXACTLY (field names byte-identical):

```ts
// src/content/models/block.ts
export type Block =
  | { type: 'heading'; level: 2 | 3; text: string; id?: string }
  | { type: 'paragraph'; html: string }
  | { type: 'lede'; html: string }
  | { type: 'aside'; variant: 'note' | 'warning' | 'fact'; html: string }
  | { type: 'sidenote'; number: number; html: string; anchorParagraphIndex: number }
  | { type: 'figure'; number?: number; src: string; alt: string; captionHtml?: string; fullBleed: boolean }
  | { type: 'code'; language: 'cpp' | 'arduino' | 'plaintext' | 'diff';
      code: string; filename?: string; showLineNumbers: boolean;
      highlightLines: number[]; diffMode: boolean;
      annotations: { line: number; html: string }[] }
  | { type: 'diff'; before: string; after: string }
  | { type: 'pinout'; src: string; alt: string;
      pins: { x: number; y: number; label: string; role: string }[] }
  | { type: 'parts-list'; items: { name: string; quantity: number; note?: string }[] };

export type BlockType = Block['type'];
```

`Lesson`, `Article`, `Datasheet`, `Schematic`, `AnyPage` — copy verbatim from UI-SPEC §"Page models".

`ContentApi` signature — copy verbatim from UI-SPEC §"`ContentApi` interface".
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock TypeScript content models + `ContentApi` abstract class + DI token + tests</name>
  <files>
    src/content/models/block.ts,
    src/content/models/lesson.ts,
    src/content/models/article.ts,
    src/content/models/datasheet.ts,
    src/content/models/schematic.ts,
    src/content/models/index.ts,
    src/content/models/block.spec.ts,
    src/content/api/content-api.ts,
    src/content/api/mock-content-api.ts,
    src/content/api/mock-content-api.spec.ts,
    src/content/api/content-api.token.ts
  </files>
  <read_first>
    - .planning/phases/02-primitives-two-column-layout-page-model-contract/02-UI-SPEC.md §"Shared types", §"Page models", §"`ContentApi` interface"
    - .planning/phases/02-primitives-two-column-layout-page-model-contract/02-CONTEXT.md §"Mock data authoring workflow" (D-MOCK-02, D-MOCK-05) and §"Pre-processor — descoped" (D-PRE-01, D-PRE-02 — VERBATIM, no transformation)
    - .planning/phases/02-primitives-two-column-layout-page-model-contract/02-PATTERNS.md §"`src/content/api/mock-content-api.ts`" and §"`src/content/api/content-api.token.ts`"
    - src/lib/intl.ts (pure-function module pattern — file shape)
    - src/lib/intl.spec.ts (Vitest setup pattern)
    - src/app/app.config.ts (provider registration site — DO NOT modify in this plan; Plan 06 wires `provideContentApi()` into `appConfig.providers`)
  </read_first>
  <behavior>
    - `Block` union has exactly 10 variants matching UI-SPEC §"Shared types" verbatim.
    - Type-narrowing test: `if (b.type === 'code') { /* b.annotations is `{ line: number; html: string }[]` */ }` — verified via Vitest's `expectTypeOf`.
    - `MockContentApi.getLesson('pershyi-blymayuchyi-svitlodiod')` resolves to a `Lesson` whose `slug === 'pershyi-blymayuchyi-svitlodiod'` (round-trip).
    - `MockContentApi.listLessons()` returns an array of 3 entries with the locked slugs.
    - All 4 `get*` methods + 4 `list*` methods exist and return typed promises.
    - `MockContentApi` performs NO transformation on returned JSON (per D-PRE-02). Strings come back byte-identical to fixture content.
    - `CONTENT_API` `InjectionToken<ContentApi>` exists; `provideContentApi(): Provider` returns `{ provide: CONTENT_API, useClass: MockContentApi }`.
  </behavior>
  <action>
**RED step — write tests first:**

1. Create `src/content/models/block.spec.ts` with type-narrowing tests using `expectTypeOf` (Vitest). One test per variant proves `Block.type` discriminant narrows to exactly the right field set. Example:
```ts
import { describe, it, expectTypeOf } from 'vitest';
import type { Block } from './block';
describe('Block discriminated union', () => {
  it('narrows `code` to its full field set', () => {
    const b = {} as Block;
    if (b.type === 'code') {
      expectTypeOf(b).toMatchTypeOf<{ language: 'cpp'|'arduino'|'plaintext'|'diff'; code: string; annotations: { line: number; html: string }[] }>();
    }
  });
  // ...one `it` per variant; minimum 10.
});
```

2. Create `src/content/api/mock-content-api.spec.ts`. Mock `globalThis.fetch` per test using `vi.spyOn(globalThis, 'fetch')` returning a `Response` whose `.json()` resolves to a representative fixture. One happy-path round-trip per page kind (4 tests). Verify the returned object's `slug` matches the requested slug and that NO string field has been mutated (compare a fixture with `«…»`, `—`, NBSP — assert byte-identical after fetch).

Run `pnpm test` — all new tests MUST FAIL (no source files yet).

**GREEN step — write source:**

3. Create `src/content/models/block.ts` with the Block union COPIED VERBATIM from the `<interfaces>` section above. Add `export type BlockType = Block['type'];`.

4. Create `src/content/models/lesson.ts`, `article.ts`, `datasheet.ts`, `schematic.ts` — each contains exactly the corresponding interface from UI-SPEC §"Page models" (verbatim). Each imports `Block` from `./block`.

5. Create `src/content/models/index.ts` as a barrel:
```ts
export * from './block';
export * from './lesson';
export * from './article';
export * from './datasheet';
export * from './schematic';
import type { Lesson } from './lesson';
import type { Article } from './article';
import type { Datasheet } from './datasheet';
import type { Schematic } from './schematic';
export type AnyPage = Lesson | Article | Datasheet | Schematic;
```

6. Create `src/content/api/content-api.ts` containing the `ContentApi` abstract class verbatim per UI-SPEC §"`ContentApi` interface".

7. Create `src/content/api/mock-content-api.ts`:
```ts
import { Injectable } from '@angular/core';
import { ContentApi } from './content-api';
import type { Lesson, Article, Datasheet, Schematic } from '../models';

@Injectable({ providedIn: 'root' })
export class MockContentApi extends ContentApi {
  private async readJson<T>(path: string): Promise<T> {
    const res = await fetch(path);
    if (!res.ok) throw new Error(`Mock fixture not found: ${path}`);
    return (await res.json()) as T;
  }
  override getLesson(slug: string): Promise<Lesson> {
    return this.readJson<Lesson>(`/assets/mock-data/lessons/${slug}.json`);
  }
  override async listLessons() {
    const slugs = ['pershyi-blymayuchyi-svitlodiod', 'knopka-ta-pidtyahuvalnyi-rezystor', 'analogovyi-vhid-ta-potentsiometr'];
    const lessons = await Promise.all(slugs.map((s) => this.getLesson(s)));
    return lessons.map(({ slug, title, deck, difficulty, estimatedMinutes, publishedAt }) => ({ slug, title, deck, difficulty, estimatedMinutes, publishedAt }));
  }
  override getArticle(slug: string): Promise<Article> { return this.readJson<Article>(`/assets/mock-data/articles/${slug}.json`); }
  override async listArticles() {
    const a = await this.getArticle('chomu-arduino');
    return [{ slug: a.slug, title: a.title, deck: a.deck, publishedAt: a.publishedAt }];
  }
  override getDatasheet(slug: string): Promise<Datasheet> { return this.readJson<Datasheet>(`/assets/mock-data/datasheets/${slug}.json`); }
  override async listDatasheets() {
    const slugs = ['atmega328p', 'arduino-uno-r3'];
    const sheets = await Promise.all(slugs.map((s) => this.getDatasheet(s)));
    return sheets.map(({ slug, title, manufacturer }) => ({ slug, title, manufacturer }));
  }
  override getSchematic(slug: string): Promise<Schematic> { return this.readJson<Schematic>(`/assets/mock-data/schematics/${slug}.json`); }
  override async listSchematics() {
    const s = await this.getSchematic('blymayuchyi-svitlodiod-shema');
    return [{ slug: s.slug, title: s.title }];
  }
}
```

NO transformation. NO call to any `processUkrainianText`. Per D-PRE-02 the JSON is returned verbatim.

8. Create `src/content/api/content-api.token.ts`:
```ts
import { InjectionToken, type Provider } from '@angular/core';
import { ContentApi } from './content-api';
import { MockContentApi } from './mock-content-api';

export const CONTENT_API = new InjectionToken<ContentApi>('ContentApi');

export function provideContentApi(): Provider {
  return { provide: CONTENT_API, useClass: MockContentApi };
}
```

DO NOT modify `src/app/app.config.ts` in this plan — Plan 06 wires `provideContentApi()` into `appConfig.providers` once `/dev/primitives` exists.

Run `pnpm test` — all model + api tests MUST PASS.
Run `pnpm tsc --noEmit` — MUST exit 0.
  </action>
  <verify>
    <automated>pnpm test --run src/content/ && pnpm tsc --noEmit && grep -q "type Block =" src/content/models/block.ts && grep -q "abstract class ContentApi" src/content/api/content-api.ts && grep -q "InjectionToken<ContentApi>" src/content/api/content-api.token.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/content/models/block.ts` defines `Block` with exactly 10 variants. Verifiable: `grep -c "| { type: '" src/content/models/block.ts` returns `10`.
    - `src/content/models/{lesson,article,datasheet,schematic,index}.ts` exist and export `Lesson`/`Article`/`Datasheet`/`Schematic`/`AnyPage`.
    - `src/content/api/content-api.ts` declares `abstract class ContentApi` with the 8 abstract methods from UI-SPEC.
    - `src/content/api/mock-content-api.ts` extends `ContentApi`, fetches from `/assets/mock-data/{kind}/{slug}.json`, and contains NO transformation calls (`grep -i 'processUkrainianText\|process_ukrainian\|transform\|replace(' src/content/api/mock-content-api.ts` returns no transformation hits — `replace` may legitimately not appear).
    - `src/content/api/content-api.token.ts` exports `CONTENT_API` and `provideContentApi`.
    - `pnpm test --run src/content/` exits 0.
    - `pnpm tsc --noEmit` exits 0.
    - `src/app/app.config.ts` is UNMODIFIED (Plan 06 wires it).
  </acceptance_criteria>
  <done>The page-model contract is locked, the abstract API + mock impl exist with no transformation, the DI token is ready for Plan 06, and tests prove the type narrowing + round-trip behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Author 7 real-Ukrainian-prose mock fixtures + style guide + fixture lint, wire into `pnpm lint`</name>
  <files>
    src/assets/mock-data/lessons/pershyi-blymayuchyi-svitlodiod.json,
    src/assets/mock-data/lessons/knopka-ta-pidtyahuvalnyi-rezystor.json,
    src/assets/mock-data/lessons/analogovyi-vhid-ta-potentsiometr.json,
    src/assets/mock-data/articles/chomu-arduino.json,
    src/assets/mock-data/datasheets/atmega328p.json,
    src/assets/mock-data/datasheets/arduino-uno-r3.json,
    src/assets/mock-data/schematics/blymayuchyi-svitlodiod-shema.json,
    scripts/lint-fixtures.mjs,
    docs/copy-style-uk.md,
    package.json
  </files>
  <read_first>
    - .planning/phases/02-primitives-two-column-layout-page-model-contract/02-CONTEXT.md §"Mock data authoring workflow" (D-MOCK-01..05) and §"Pre-processor — descoped" (D-PRE-03)
    - .planning/phases/02-primitives-two-column-layout-page-model-contract/02-UI-SPEC.md §"Mock data fixtures", §"Mock-data prose calibration rule", §"Copywriting Contract"
    - .planning/phases/02-primitives-two-column-layout-page-model-contract/02-PATTERNS.md §"`scripts/lint-fixtures.mjs`" and §"`src/assets/mock-data/**/*.json`"
    - src/app/pages/glyph-audit/specimen-prose.const.ts (the project's calibration target for real Ukrainian Arduino prose — copy the voice and the punctuation forms)
    - scripts/fonts/subset.mjs (Node CLI ESM analog — header pattern, sync FS, exit codes)
    - src/content/models/{block,lesson,article,datasheet,schematic}.ts (just created — fixtures must byte-match these shapes)
    - docs/typography-checklist.md (existing accumulating doc voice — terse, ASCII-table-friendly)
    - package.json (current `lint` script — to extend)
  </read_first>
  <action>
**Authoring discipline (per D-MOCK-01 + CLAUDE.md):** AI-drafted, real Ukrainian Arduino prose, calibrated against the `specimen-prose.const.ts` voice. Every fixture must hit the 6 prose calibration gates from UI-SPEC §"Mock-data prose calibration rule". Locked slugs per D-MOCK-03.

1. **Create 7 JSON fixtures** at `src/assets/mock-data/{lessons|articles|datasheets|schematics}/{slug}.json`. Each file is the serialized form of the matching TypeScript page model — field names byte-identical.

   **Lesson fixture skeleton (apply to all 3 lessons; titles per UI-SPEC §Copywriting Contract):**
   ```json
   {
     "type": "lesson",
     "slug": "pershyi-blymayuchyi-svitlodiod",
     "title": "Перший блимаючий світлодіод",
     "deck": "Найпростіша схема, з якої починається кожен курс — світлодіод і пара рядків коду.",
     "estimatedMinutes": 12,
     "difficulty": "beginner",
     "partsList": { "type": "parts-list", "items": [
       { "name": "Плата Arduino Uno R3", "quantity": 1 },
       { "name": "Світлодіод", "quantity": 1, "note": "будь-якого кольору" },
       { "name": "Резистор 220 Ом", "quantity": 1 },
       { "name": "Макетна плата", "quantity": 1 },
       { "name": "Дроти-перемички", "quantity": 2 }
     ]},
     "body": [
       { "type": "lede", "html": "Плата <strong>Arduino Uno</strong> побудована на мікроконтролері <strong>ATmega328P</strong>. Вбудований світлодіод підʼєднано до цифрового виходу <code>pin 13</code> через резистор — саме тому ми можемо керувати ним <em>без жодних додаткових компонентів</em>." },
       { "type": "heading", "level": 2, "text": "Що нам знадобиться", "id": "shcho-nam-znadobytsya" },
       { "type": "paragraph", "html": "У цьому уроці ми зберемо схему на макетній платі та запишемо першу програму. Знадобиться лише п'ять елементів зі стартового набору." },
       { "type": "sidenote", "number": 1, "anchorParagraphIndex": 2, "html": "Якщо в тебе ще немає набору — підійде будь-яка плата серії Uno: <strong>R3</strong>, <strong>R4 Minima</strong>, або клон. Інші плати потребуватимуть інших схем живлення." },
       { "type": "figure", "number": 1, "src": "/assets/mock-data/figures/blymayuchyi-shema.svg", "alt": "Схема: Arduino Uno, світлодіод, резистор 220 Ом", "captionHtml": "Підключення світлодіода до <code>pin 13</code> через обмежувальний резистор.", "fullBleed": false },
       { "type": "heading", "level": 2, "text": "Код", "id": "kod" },
       { "type": "code", "language": "arduino", "code": "void setup() {\n  pinMode(LED_BUILTIN, OUTPUT);\n}\n\nvoid loop() {\n  digitalWrite(LED_BUILTIN, HIGH);  // блимаємо світлодіодом\n  delay(1000);\n  digitalWrite(LED_BUILTIN, LOW);\n  delay(1000);\n}\n", "filename": "blink.ino", "showLineNumbers": true, "highlightLines": [7], "diffMode": false, "annotations": [
         { "line": 2, "html": "Налаштовуємо <code>pin 13</code> як вихід — це робиться один раз при старті." },
         { "line": 7, "html": "Затримка <code>1000 мс</code> — пауза в одну секунду між перемиканнями." }
       ]},
       { "type": "paragraph", "html": "Завантаж скетч на плату через <em>Arduino IDE</em> — світлодіод почне блимати з інтервалом 1–2 секунди." },
       { "type": "aside", "variant": "fact", "html": "Назва Arduino походить від кафе у місті Іврея, Італія, де у 2005-му зібралася команда засновників проєкту." }
     ],
     "prevSlug": null,
     "nextSlug": "knopka-ta-pidtyahuvalnyi-rezystor",
     "publishedAt": "2026-04-15T09:00:00+03:00",
     "updatedAt": "2026-04-22T11:30:00+03:00"
   }
   ```

   **Hard rules for ALL fixtures (per D-MOCK-04 calibration gates):**
   - At least one `ґ` in body prose (e.g., `ґрунтовно`, `ґатунок`, or `аґент`).
   - At least one `«…»` quote pair (use them in the prose where natural).
   - At least one em-dash `—` (with NBSP ` ` before, regular space after — TYPED INTO THE JSON literally; the lint script will check).
   - At least one en-dash range `\d+–\d+` (e.g., `5–7 секунд`, `1–2`, `220–330 Ом`).
   - At least one inline `<code>` reference (e.g., `<code>pin 13</code>`).
   - Lessons: at least one `sidenote`, one `figure`, one `code` block in `body[]`.
   - Use `ʼ` (U+02BC, MODIFIER LETTER APOSTROPHE) for Ukrainian apostrophe — never ASCII `'` between Cyrillic letters (e.g., `підʼєднано`, `пʼять`, `обʼєкт`).
   - NBSP (` `) after one-letter prepositions in body prose: `а в до за з і й на не о по та у` — write the literal NBSP into JSON strings.
   - Code blocks (`code.code`) ship VERBATIM — no transformation. Comments inside code use ASCII; Ukrainian comments allowed (`// блимаємо світлодіодом`).
   - Titles, decks, slugs locked per UI-SPEC §Copywriting Contract; do NOT invent new slugs.

   **Three lessons** (3 distinct fixtures, drafted to slug-implied topic):
   - `pershyi-blymayuchyi-svitlodiod` — `Перший блимаючий світлодіод` (skeleton above)
   - `knopka-ta-pidtyahuvalnyi-rezystor` — `Кнопка та підтягувальний резистор`
   - `analogovyi-vhid-ta-potentsiometr` — `Аналоговий вхід та потенціометр`

   **One article**: `chomu-arduino` → `Чому Arduino?` — at least 6 paragraphs, at least 2 asides, at least 1 sidenote. No `partsList`, no `prevSlug`/`nextSlug`.

   **Two datasheets** (`atmega328p`, `arduino-uno-r3`): include `manufacturer`, exactly one `pinout` block (with `src`, `alt`, and at least 14 `pins[]` entries with `x`/`y` percentages 0..100), `specifications: {label,value}[]` (≥6 entries each), `peripheralNotes: Block[]` with at least one paragraph + one aside.

   **One schematic** (`blymayuchyi-svitlodiod-shema`): `schematicImage` is exactly one `figure` block with `fullBleed: true`. `explanation: Block[]` ≥3 paragraphs. `downloadUrl` is a path under `/assets/mock-data/figures/...`.

   **Image asset shortcuts:** the `src` paths in `figure`/`pinout`/`schematic` may reference `/assets/mock-data/figures/<name>.svg` files that DO NOT EXIST YET (Plan 06 may add placeholder SVGs, or images can be added at content-population time). The fixtures are the contract; the rendering test in Plan 06 tolerates missing images via the standard `alt` text fallback. Per D-MOCK-02, JSON is the contract.

2. **Create `docs/copy-style-uk.md`** (~1 page, terse, matches `docs/typography-checklist.md` voice). Required sections:
   - Title + one-paragraph framing: "Authors and editors deliver typeset Ukrainian prose. Phase 2 ships no transformation function — see `02-CONTEXT.md` D-PRE-01..05."
   - Table of typography forms with examples (each row: form, glyph, when-to-use, example):
     - Outer quotes: `«…»`, e.g. `«Arduino — це відкрита платформа»`
     - Nested quotes: `„…"`
     - Em-dash: `—` (U+2014) with NBSP before, regular space after
     - En-dash: `–` (U+2013) for numeric ranges only
     - Apostrophe: `ʼ` (U+02BC) — never ASCII `'` between Cyrillic letters
     - NBSP: U+00A0 after one-letter prepositions `а в до за з і й на не о по та у`
   - "Enforcement" section: links to `scripts/lint-fixtures.mjs` and explains it warns (does not transform).
   - "Read-aloud gate" reminder per D-MOCK-01: every fixture must pass an author read-aloud check; commit message line `read-aloud: PASS` is the attestation.

3. **Create `scripts/lint-fixtures.mjs`** — Node ESM CLI matching `scripts/fonts/subset.mjs` shape:
   - Header imports: `readFileSync`, `readdirSync`, `statSync` from `node:fs`; `dirname`, `join`, `resolve` from `node:path`; `fileURLToPath` from `node:url`.
   - `REPO_ROOT = resolve(__dirname, '..')` (script lives at `scripts/`, not `scripts/fonts/`).
   - Walk `src/assets/mock-data/**/*.json` recursively via `readdirSync(dir, { recursive: true })`.
   - For each file:
     - `JSON.parse` it; on parse error, print `path: invalid JSON: <message>` and add to violations.
     - **Editorial-smell checks (warn-level — counted as violations):**
       1. Straight ASCII `"` inside any string-valued field that is NOT `code.code` (recurse the parsed object). Allowed where: a `Block.type === 'code'` `code` field, or where a `<code>` HTML tag attribute genuinely needs quotes (the regex must distinguish HTML attribute quotes from prose quotes — heuristic: a straight `"` adjacent to `=` is allowed; a straight `"` between Cyrillic letters or whitespace is flagged).
       2. `--` between word characters (`/\w--\w/` or `/\s--\s/`) — flag.
       3. ASCII `'` between two Cyrillic letters (`/[Ѐ-ӿ]'[Ѐ-ӿ]/`) — flag (use `ʼ` U+02BC).
       4. Regular space (`U+0020`) immediately after one-letter preposition tokens followed by a Cyrillic letter — pattern: `/(?:^|[\s.,;:«»\(\)])(в|у|і|й|з|а|о|та|не|на|до|за|по) [Ѐ-ӿ]/iu` — flag (require NBSP).
     - **Per-fixture content gates (per UI-SPEC §"Mock-data prose calibration rule"):**
       1. At least one `ґ` in any body string (rejection: file violation).
       2. At least one `«…»` quote pair somewhere in the parsed string fields.
       3. At least one `—` (em-dash) AND one `\d+–\d+` (en-dash range) in the body.
       4. At least one `<code>` HTML tag in any html-bearing field.
       5. For `lessons/*.json`: `body[]` MUST contain at least one block where `type === 'sidenote'`, one where `type === 'figure'`, one where `type === 'code'`.
   - Print findings as `path:line: <message>` (line-counting via `readFileSync` split on `\n` to map to source position; for parsed-object violations approximate line = the JSON `JSON.stringify` re-search index).
   - Exit `1` if any violations; exit `0` if clean.
   - Match `subset.mjs`'s flag/CLI ergonomics: support `--help` flag printing usage; default invocation walks the fixture tree.

4. **Wire into `package.json` `lint` script.** Existing line:
   ```
   "lint": "eslint . && stylelint \"src/**/*.scss\""
   ```
   Replace with:
   ```
   "lint": "eslint . && stylelint \"src/**/*.scss\" && node scripts/lint-fixtures.mjs"
   ```

5. **Run `node scripts/lint-fixtures.mjs`** against the 7 fixtures. The script must exit 0. If it flags anything, FIX THE FIXTURE (typeset the text correctly) — do not weaken the lint rules. The fixtures are the source of truth for what the lint protects.

6. **Run `pnpm lint`** — must exit 0 (the new fixture-lint step is now in the chain).

7. **Per-fixture commit attestation:** When this plan's commit is created, the commit message body MUST include one `read-aloud: PASS` line per fixture (D-MOCK-01). Author/Claude attestation; not script-checkable.
  </action>
  <verify>
    <automated>test -f src/assets/mock-data/lessons/pershyi-blymayuchyi-svitlodiod.json && test -f src/assets/mock-data/lessons/knopka-ta-pidtyahuvalnyi-rezystor.json && test -f src/assets/mock-data/lessons/analogovyi-vhid-ta-potentsiometr.json && test -f src/assets/mock-data/articles/chomu-arduino.json && test -f src/assets/mock-data/datasheets/atmega328p.json && test -f src/assets/mock-data/datasheets/arduino-uno-r3.json && test -f src/assets/mock-data/schematics/blymayuchyi-svitlodiod-shema.json && test -f scripts/lint-fixtures.mjs && test -f docs/copy-style-uk.md && grep -q "node scripts/lint-fixtures.mjs" package.json && node scripts/lint-fixtures.mjs && pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - All 7 fixture files exist at the locked paths.
    - Each fixture contains at least one `ґ` in a body string. Verifiable per file: `grep -l 'ґ' src/assets/mock-data/lessons/*.json` lists all 3 lessons.
    - Each fixture contains `«` AND `»`. Verifiable: `grep -l '«' src/assets/mock-data/{lessons,articles,datasheets,schematics}/*.json | wc -l` returns `7`.
    - Each fixture contains an em-dash `—` and an en-dash range. Verifiable: `grep -lP '\d+–\d+' src/assets/mock-data/**/*.json | wc -l` returns `7` (with `globstar` shell or via `find` equivalent).
    - Each lesson fixture contains `"type": "sidenote"`, `"type": "figure"`, `"type": "code"`. Verifiable: for each lesson file, `grep -c '"type": "sidenote"\|"type": "figure"\|"type": "code"' <file>` returns ≥3.
    - `scripts/lint-fixtures.mjs` exists, is ESM (uses `import` syntax), and runs to completion with exit 0 against the 7 fixtures.
    - `package.json` `lint` script ends with `&& node scripts/lint-fixtures.mjs`.
    - `docs/copy-style-uk.md` exists and contains the typography-forms table (verifiable: contains literal substrings `«…»`, `—`, `–`, `ʼ`, ` ` description).
    - `pnpm lint` exits 0.
    - Commit message includes 7 `read-aloud: PASS` lines (manual attestation per D-MOCK-01).
  </acceptance_criteria>
  <done>
The 7 mock fixtures are real Ukrainian Arduino prose, byte-shaped to the locked TS contract; the editorial-smell + content-gate lint protects them in CI; the author/editor style guide documents the authoring contract that replaces the descoped pre-processor; `pnpm lint` is green.
  </done>
</task>

</tasks>

<verification>
- `pnpm test --run src/content/` exits 0.
- `pnpm tsc --noEmit` exits 0.
- `node scripts/lint-fixtures.mjs` exits 0.
- `pnpm lint` exits 0.
- `MockContentApi` has zero transformation calls (D-PRE-02 enforced by inspection).
- All 7 fixture slugs match the locked list in CONTEXT D-MOCK-03.
</verification>

<success_criteria>
- The page-model contract is locked in TypeScript (CONTRACT-01) — Wagtail must conform.
- `ContentApi` + `MockContentApi` + DI token are ready for app wiring (CONTRACT-03).
- 7 real-Ukrainian-prose fixtures are committed and lint-clean (CONTRACT-04).
- UKR-02 / UKR-03 are honored as authoring-contract requirements via `docs/copy-style-uk.md` + `scripts/lint-fixtures.mjs` (per CONTEXT D-PRE-01..05 + ROADMAP SC#5 reframing).
</success_criteria>

<output>
Create `.planning/phases/02-primitives-two-column-layout-page-model-contract/02-02-SUMMARY.md`. Record:
- Resolved fixture line counts and grep evidence for each calibration gate per file.
- Any deviations from the model shapes (none expected) with rationale.
- Confirmation that `MockContentApi` returns JSON verbatim (D-PRE-02 evidence).
- The 7 `read-aloud: PASS` attestations.
</output>

---
phase: 02-primitives-two-column-layout-page-model-contract
plan: 06
type: execute
wave: 4
depends_on: [02-02, 02-03, 02-04, 02-05]
files_modified:
  - src/app/pages/dev-primitives/dev-primitives.component.ts
  - src/app/pages/dev-primitives/dev-primitives.component.html
  - src/app/pages/dev-primitives/dev-primitives.component.scss
  - src/app/app.routes.ts
  - src/app/app.routes.server.ts
  - src/app/app.config.ts
  - docs/typography-checklist.md
  - docs/force-en-audit.md
autonomous: false
requirements: [PRIM-01, PRIM-02, PRIM-03, PRIM-04, PRIM-05, PRIM-06, PRIM-07, PRIM-08, LAYOUT-01, LAYOUT-02, LAYOUT-03, LAYOUT-04, LAYOUT-05, UKR-02, UKR-03]
must_haves:
  truths:
    - "`/dev/primitives` route exists, registered in `app.routes.ts`, served by `pnpm dev`"
    - "`/dev/primitives` is excluded from the production prerender output (RenderMode.Client) so SSG `ng build` does NOT emit a static HTML file for it"
    - "`<meta name=\"robots\" content=\"noindex\">` is set on the page"
    - "`provideContentApi()` is wired into `app.config.ts` providers; `MockContentApi` resolves through DI"
    - "The showcase page renders all 14 P2 primitives (Heading, Body, Lede, Aside, Sidenote, SidenoteRef, Figure, FigureCaption, CodeBlock, Diff, Pinout, PageShell, TwoColumn, MarginRail) via the `@arduino/core-ui` public API only"
    - "The TwoColumn section anchors three sidenotes to paragraphs near top/middle/bottom of a tall section"
    - "Three CodeBlock instances exercise basic / diff / annotated flavors"
    - "Pinout renders with at least 14 pins in the static legend"
    - "`docs/typography-checklist.md` has a P2 section enumerating each primitive at each breakpoint"
    - "`docs/force-en-audit.md` has a P2 row covering `/dev/primitives`"
    - "Three-breakpoint manual walk completed at <768, 768-1199, >=1200; force-en audit completed; both ticked in checklist"
  artifacts:
    - path: "src/app/pages/dev-primitives/dev-primitives.component.html"
      provides: "Showcase markup composing all primitives"
      min_lines: 80
    - path: "src/app/app.config.ts"
      provides: "App config with provideContentApi() wired"
      contains: "provideContentApi"
  key_links:
    - from: "src/app/pages/dev-primitives/dev-primitives.component.ts"
      to: "@arduino/core-ui"
      via: "import { HeadingComponent, BodyComponent, ... } from '@arduino/core-ui'"
      pattern: "from '@arduino/core-ui'"
    - from: "src/app/app.config.ts"
      to: "src/content/api/content-api.token.ts"
      via: "provideContentApi() in providers array"
      pattern: "provideContentApi\\(\\)"
---

<objective>
Wire everything together: register the `/dev/primitives` route, exclude it from SSG prerender, hook `provideContentApi()` into the app, author the showcase template that exercises every P2 primitive on real Ukrainian content (drawn from the `pershyi-blymayuchyi-svitlodiod` lesson fixture where appropriate, hand-authored for showcase-specific sections otherwise), append the P2 sections to the two accumulating phase-exit docs, and walk the three-breakpoint manual verification + force-en locale audit.

Purpose: ROADMAP SC#1 (every primitive renders on a single showcase), SC#2 (three-breakpoint LAYOUT verification), SC#3 (CodeBlock demos basic/diff/annotated), SC#5 (force-en audit clean).
Output: Live `/dev/primitives` page; updated docs; phase-exit attestations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/phases/02-primitives-two-column-layout-page-model-contract/02-CONTEXT.md
@.planning/phases/02-primitives-two-column-layout-page-model-contract/02-UI-SPEC.md
@.planning/phases/02-primitives-two-column-layout-page-model-contract/02-PATTERNS.md
@src/app/pages/glyph-audit/glyph-audit.component.ts
@src/app/pages/glyph-audit/glyph-audit.component.html
@src/app/pages/glyph-audit/glyph-audit.component.scss
@src/app/app.config.ts
@src/app/app.routes.ts
@src/app/app.routes.server.ts
@docs/typography-checklist.md
@docs/force-en-audit.md
@src/content/api/content-api.token.ts
@src/assets/mock-data/lessons/pershyi-blymayuchyi-svitlodiod.json
@projects/core-ui/src/public-api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire DI + route + prerender exclusion + showcase page composition</name>
  <files>
    src/app/pages/dev-primitives/dev-primitives.component.ts,
    src/app/pages/dev-primitives/dev-primitives.component.html,
    src/app/pages/dev-primitives/dev-primitives.component.scss,
    src/app/app.routes.ts,
    src/app/app.routes.server.ts,
    src/app/app.config.ts
  </files>
  <read_first>
    - src/app/pages/glyph-audit/glyph-audit.component.ts (exact analog — copy decorator + `OnInit`/Title/Meta pattern)
    - src/app/pages/glyph-audit/glyph-audit.component.html (template structure to mirror)
    - src/app/pages/glyph-audit/glyph-audit.component.scss (container + breakpoint padding pattern)
    - src/app/app.routes.ts (existing `/dev/glyph-audit` route — append new entry following same shape)
    - src/app/app.routes.server.ts (current prerender config — add explicit `dev/primitives` -> `RenderMode.Client` per CONTEXT D-WIRE-02)
    - src/app/app.config.ts (existing providers array — append `provideContentApi()`)
    - src/content/api/content-api.token.ts (the factory to invoke)
    - src/assets/mock-data/lessons/pershyi-blymayuchyi-svitlodiod.json (showcase pulls real lede + paragraph + code excerpts from this fixture)
    - projects/core-ui/src/public-api.ts (exports list — confirm 14 primitives + measure helpers are accessible)
    - .planning/phases/02-primitives-two-column-layout-page-model-contract/02-UI-SPEC.md §"The Showcase Page — `/dev/primitives`" (sections 1-11 — the locked structure)
    - .planning/phases/02-primitives-two-column-layout-page-model-contract/02-CONTEXT.md §"Showcase page wiring" (D-WIRE-01..04)
  </read_first>
  <action>

**Step A — wire `provideContentApi()` into `app.config.ts`:**

Append `provideContentApi()` to the `providers` array. Final shape:
```ts
import { ApplicationConfig, LOCALE_ID, provideZonelessChangeDetection } from '@angular/core';
import { provideRouter } from '@angular/router';
import { routes } from './app.routes';
import { provideContentApi } from '../content/api/content-api.token';

export const appConfig: ApplicationConfig = {
  providers: [
    provideZonelessChangeDetection(),
    provideRouter(routes),
    { provide: LOCALE_ID, useValue: 'uk-UA' },
    provideContentApi(),
  ],
};
```

**Step B — register the route in `app.routes.ts`:**

Append (after the `/dev/glyph-audit` entry) the new route, following the exact shape of the analog:
```ts
{
  path: 'dev/primitives',
  loadComponent: () =>
    import('./pages/dev-primitives/dev-primitives.component').then((m) => m.DevPrimitivesComponent),
  title: 'Примітиви — Arduino UA',
},
```

**Step C — exclude `/dev/primitives` from prerender** in `app.routes.server.ts` (CONTEXT D-WIRE-02). The current file uses a global `**` rule with `RenderMode.Prerender`. Add an explicit earlier-matching entry for `dev/primitives` with `RenderMode.Client` so the path ships as a CSR-only route in the static bundle (consistent with the SSG-only / no-Node-SSR-ever constraint — `RenderMode.Client` does NOT introduce a Node runtime; it just emits a stub HTML that the client hydrates).

The exact pattern (copy this verbatim, place BEFORE the `**` catch-all):
```ts
import { RenderMode, ServerRoute } from '@angular/ssr';

export const serverRoutes: ServerRoute[] = [
  { path: 'dev/primitives', renderMode: RenderMode.Client },
  { path: '**', renderMode: RenderMode.Prerender },
];
```
If the existing file uses different syntax, preserve its style and just insert the new explicit route ahead of the catch-all.

**Step D — create the showcase component:**

`src/app/pages/dev-primitives/dev-primitives.component.ts`:
```ts
import { ChangeDetectionStrategy, Component, OnInit, inject, signal } from '@angular/core';
import { Meta, Title } from '@angular/platform-browser';
import {
  HeadingComponent,
  BodyComponent,
  LedeComponent,
  AsideComponent,
  SidenoteComponent,
  SidenoteRefComponent,
  FigureComponent,
  FigureCaptionComponent,
  CodeBlockComponent,
  DiffComponent,
  PinoutComponent,
  PageShellComponent,
  TwoColumnComponent,
  MarginRailComponent,
} from '@arduino/core-ui';
import { CONTENT_API } from '../../../content/api/content-api.token';
import type { Lesson } from '../../../content/models';

@Component({
  selector: 'app-dev-primitives',
  standalone: true,
  imports: [
    HeadingComponent, BodyComponent, LedeComponent, AsideComponent,
    SidenoteComponent, SidenoteRefComponent, FigureComponent, FigureCaptionComponent,
    CodeBlockComponent, DiffComponent, PinoutComponent,
    PageShellComponent, TwoColumnComponent, MarginRailComponent,
  ],
  templateUrl: './dev-primitives.component.html',
  styleUrl: './dev-primitives.component.scss',
  changeDetection: ChangeDetectionStrategy.OnPush,
})
export class DevPrimitivesComponent implements OnInit {
  private readonly meta = inject(Meta);
  private readonly title = inject(Title);
  private readonly contentApi = inject(CONTENT_API);

  readonly lesson = signal<Lesson | null>(null);

  ngOnInit(): void {
    this.title.setTitle('Примітиви — Arduino UA');
    this.meta.addTag({ name: 'robots', content: 'noindex' });
    this.contentApi.getLesson('pershyi-blymayuchyi-svitlodiod').then((l) => this.lesson.set(l));
  }
}
```

**Step E — author the showcase template** following UI-SPEC §"The Showcase Page — Page structure (top to bottom)" sections 1-11. Build `dev-primitives.component.html` with each section. Per D-WIRE-04, raw template composition (no BlockRenderer). Use the lesson fixture's lede + first paragraph + code excerpt where natural; hand-author the rest with real Ukrainian Arduino prose (CLAUDE.md hard rule).

The template MUST include all of these primitives at minimum:

```html
<ui-page-shell>
  <article class="showcase">
    <header>
      <ui-heading [level]="1">Showcase примітивів</ui-heading>
      <ui-lede>Кожен примітив core-ui на реальних даних. Відкрий у трьох ширинах: &lt;768, 768–1199, ≥1200.</ui-lede>
    </header>

    <!-- Section 2: Body & Heading -->
    <section>
      <ui-heading [level]="2" id="hierarchy">Ієрархія</ui-heading>
      <ui-body>Плата <strong>Arduino Uno</strong> побудована на мікроконтролері <strong>ATmega328P</strong>. Вбудований світлодіод підʼєднано до цифрового виходу <code>pin 13</code> через резистор.</ui-body>
      <ui-heading [level]="3">Підрозділ</ui-heading>
      <ui-body>Цей абзац перевіряє, що ієрархія заголовків залишається спокійною — інтервали, накреслення, оптичний розмір.</ui-body>
    </section>

    <!-- Section 3: Lede & Aside -->
    <section>
      <ui-heading [level]="2">Asides</ui-heading>
      <ui-aside variant="note"><strong>Замітка.</strong> Це нейтральне додаткове пояснення біля основного тексту.</ui-aside>
      <ui-aside variant="warning"><strong>Увага.</strong> Не подавай напругу 5 В на пін, налаштований на вихід — згориш плату.</ui-aside>
      <ui-aside variant="fact"><strong>Факт.</strong> Назва «Arduino» походить від кафе у місті Іврея, Італія.</ui-aside>
    </section>

    <!-- Section 4: TwoColumn + Sidenote (LAYOUT verification — 3 sidenotes top/middle/bottom) -->
    <section>
      <ui-heading [level]="2">Двоколонкова верстка</ui-heading>
      <ui-two-column>
        <article body>
          <p data-sidenote-anchor="1">Перший абзац уроку: тут пояснюємо, чому світлодіод треба підключати через резистор<ui-sidenote-ref [number]="1"/>. Без обмеження струму світлодіод швидко згорить.</p>
          <p>Другий абзац без примітки. Прохолодний ритм основного тексту перевіряє інтервал між абзацами та довжину рядка близько 62ch.</p>
          <p>Третій абзац: перевіряємо, як абзаци дихають у двоколонковій верстці. Жодного «text-align: justify», жодних автоматичних переносів — рваний правий край є редакційною нормою.</p>
          <p data-sidenote-anchor="2">Четвертий абзац згадує <code>digitalWrite(LED_BUILTIN, HIGH)</code><ui-sidenote-ref [number]="2"/> — стандартну функцію, з якої починається кожен курс Arduino.</p>
          <p>Пʼятий абзац без примітки. Текст продовжується для перевірки колізії приміток у нижній частині секції.</p>
          <p data-sidenote-anchor="3">Шостий абзац — останній у цій секції. Тут ми перевіряємо, що остання примітка<ui-sidenote-ref [number]="3"/> вирівнюється внизу і не перекриває попередню навіть при ширині 1200–1280 пікселів.</p>
        </article>
        <ui-margin-rail margin>
          <ui-sidenote [number]="1">Резистор 220 Ом — типовий вибір для світлодіода 2 В при живленні 5 В. Розрахунок: (5–2) ÷ 0.014 ≈ 214 Ом, округлюємо вгору.</ui-sidenote>
          <ui-sidenote [number]="2"><code>LED_BUILTIN</code> — макрос, що позначає вбудований світлодіод плати. На Uno це <code>pin 13</code>.</ui-sidenote>
          <ui-sidenote [number]="3">Якщо примітки накладаються одна на одну — спрацьовує правило колізії: пізніша зʼїжджає вниз на висоту попередньої плюс <code>--sidenote-stack-gap</code>.</ui-sidenote>
        </ui-margin-rail>
      </ui-two-column>
    </section>

    <!-- Section 5: Figure (body-measure + full-bleed) -->
    <section>
      <ui-heading [level]="2">Figure</ui-heading>
      <ui-figure [number]="1">
        <img src="/assets/mock-data/figures/blymayuchyi-shema.svg" alt="Схема: Arduino Uno, світлодіод, резистор 220 Ом" width="800" height="450"/>
        <ui-figure-caption [number]="1">Підключення світлодіода до <code>pin 13</code> через обмежувальний резистор.</ui-figure-caption>
      </ui-figure>
      <ui-figure [number]="2" [fullBleed]="true">
        <img src="/assets/mock-data/figures/uno-r3-pinout-hero.svg" alt="Загальний вигляд плати Arduino Uno R3" width="1600" height="900"/>
        <ui-figure-caption [number]="2">Плата Arduino Uno R3, повна ширина контейнера.</ui-figure-caption>
      </ui-figure>
    </section>

    <!-- Section 6: CodeBlock — basic -->
    <section>
      <ui-heading [level]="2">CodeBlock — простий</ui-heading>
      <ui-code-block
        language="arduino"
        filename="blink.ino"
        [showLineNumbers]="true"
        [highlightLines]="[7]"
        [code]="'void setup() {\n  pinMode(LED_BUILTIN, OUTPUT);\n}\n\nvoid loop() {\n  digitalWrite(LED_BUILTIN, HIGH);  // блимаємо світлодіодом\n  delay(1000);\n  digitalWrite(LED_BUILTIN, LOW);\n  delay(1000);\n}\n'"/>
    </section>

    <!-- Section 7: CodeBlock — diff mode -->
    <section>
      <ui-heading [level]="2">CodeBlock — diff</ui-heading>
      <ui-code-block
        language="diff"
        [diffMode]="true"
        [showLineNumbers]="true"
        [code]="'  void setup() {\n    pinMode(LED_BUILTIN, OUTPUT);\n-   Serial.begin(9600);\n+   Serial.begin(115200);\n+   pinMode(BUTTON_PIN, INPUT_PULLUP);\n  }\n\n  void loop() {\n    digitalWrite(LED_BUILTIN, HIGH);\n    delay(1000);\n-   digitalWrite(LED_BUILTIN, LOW);\n+   digitalWrite(LED_BUILTIN, !digitalRead(LED_BUILTIN));\n    delay(1000);\n  }\n'"/>
    </section>

    <!-- Section 8: CodeBlock — annotated -->
    <section>
      <ui-heading [level]="2">CodeBlock — з примітками</ui-heading>
      <ui-code-block
        language="arduino"
        filename="annotated.ino"
        [showLineNumbers]="true"
        [annotations]="[
          { line: 3, html: 'Налаштовуємо <code>pin 13</code> як вихід — це робиться один раз при старті.' },
          { line: 8, html: 'Затримка <code>1000 мс</code> — пауза в одну секунду.' },
          { line: 13, html: 'Друга затримка симетрична першій — світлодіод горить і не горить однаковий час.' },
          { line: 18, html: 'Цикл <code>loop()</code> повторюється безкінечно — це фундамент архітектури Arduino.'}
        ]"
        [code]="'/* annotated.ino */\n\nvoid setup() {\n  pinMode(LED_BUILTIN, OUTPUT);\n}\n\nvoid loop() {\n  digitalWrite(LED_BUILTIN, HIGH);\n  delay(1000);\n  digitalWrite(LED_BUILTIN, LOW);\n  delay(1000);\n  // повторюємо\n  digitalWrite(LED_BUILTIN, HIGH);\n  delay(1000);\n  digitalWrite(LED_BUILTIN, LOW);\n  delay(1000);\n  digitalWrite(LED_BUILTIN, HIGH);\n  delay(1000);\n}\n'"/>
    </section>

    <!-- Section 9: Diff (text-level) -->
    <section>
      <ui-heading [level]="2">Diff — текст</ui-heading>
      <ui-diff
        before="Підключи світлодіод напряму до пін 13 — Arduino не згорить, бо вбудований резистор обмежує струм."
        after="Підключи світлодіод через зовнішній резистор 220 Ом до <code>pin 13</code> — це безпечніше для зовнішніх світлодіодів і дисциплінує початківця."/>
    </section>

    <!-- Section 10: Pinout -->
    <section>
      <ui-heading [level]="2">Pinout</ui-heading>
      <ui-pinout
        src="/assets/mock-data/figures/uno-r3-pinout.svg"
        alt="Pinout Arduino Uno R3"
        [pins]="[
          { x: 10, y: 10, label: 'D0',  role: 'RX' },
          { x: 10, y: 20, label: 'D1',  role: 'TX' },
          { x: 10, y: 30, label: 'D2',  role: 'INT0' },
          { x: 10, y: 40, label: 'D3',  role: 'PWM' },
          { x: 10, y: 50, label: 'D4',  role: 'GPIO' },
          { x: 10, y: 60, label: 'D5',  role: 'PWM' },
          { x: 10, y: 70, label: 'D6',  role: 'PWM' },
          { x: 10, y: 80, label: 'D7',  role: 'GPIO' },
          { x: 10, y: 90, label: 'D8',  role: 'GPIO' },
          { x: 90, y: 10, label: 'D9',  role: 'PWM' },
          { x: 90, y: 20, label: 'D10', role: 'PWM/SS' },
          { x: 90, y: 30, label: 'D11', role: 'PWM/MOSI' },
          { x: 90, y: 40, label: 'D12', role: 'MISO' },
          { x: 90, y: 50, label: 'D13', role: 'SCK/LED' }
        ]"/>
    </section>

    <!-- Section 11: Force-en audit footer -->
    <section class="force-en-footer">
      <ui-heading [level]="2">Force-en аудит</ui-heading>
      <ui-body>Перевір цю сторінку під <code>en-US</code> локаллю браузера: усі лапки лишаються «…», тире — «—», числа й дати — українські. Жодного «April», жодного «5,000».</ui-body>
    </section>
  </article>
</ui-page-shell>
```

NOTE: Image URLs may 404 in dev — that's acceptable; the alt text + structure is what's verified. Per CLAUDE.md, images can be added at content-population time. The PRIMITIVES are what we're verifying.

`dev-primitives.component.scss` — minimal page shell paddings and a small `.figure-num` accent (matches `_base.scss`):
```scss
:host { display: block; }

.showcase {
  padding-block: var(--space-6);
}
.showcase > section { margin-block: var(--space-7); }
.showcase .force-en-footer { margin-top: var(--space-8); border-top: 1px solid var(--color-rule); padding-top: var(--space-5); }
```

(The `.figure-num` accent is already styled in `_base.scss` and applies via cascade.)

**Step F — verify the build:**

1. `pnpm exec ng build` — must succeed AND must NOT emit `dist/.../dev/primitives.html` (RenderMode.Client emits a stub at most; verify by listing the dist directory). Expected: a single `index.csr.html` or per-route prerendered HTML for routes that ARE prerendered, and either nothing-for-`dev/primitives` or a CSR shell. Verify `find dist -name '*primitives*.html'` — if anything is found, confirm it's the CSR shell (no inlined Cyrillic content from the showcase template).

2. `pnpm dev` (or `pnpm start`) — visit `http://localhost:4200/dev/primitives` — page renders and the lesson lede loads asynchronously through `MockContentApi` (so `provideContentApi()` is exercised end-to-end).

3. `pnpm lint` — must pass. Boundary rules must allow this `app` file to import from `@arduino/core-ui` (the public surface) and `src/content/**` (content-models element type).

DO NOT add any non-`@arduino/core-ui` import path for primitives. DO NOT reach into `projects/core-ui/src/lib/...` (the boundary rule from Plan 01 will fail the lint).
DO NOT register `BlockRenderer` (P3).
DO NOT integrate Shiki — code blocks render plain (Plan 05 ships the frame; P3 swaps in tokens).
  </action>
  <verify>
    <automated>grep -q "provideContentApi()" src/app/app.config.ts &amp;&amp; grep -q "dev/primitives" src/app/app.routes.ts &amp;&amp; grep -q "RenderMode.Client" src/app/app.routes.server.ts &amp;&amp; test -f src/app/pages/dev-primitives/dev-primitives.component.ts &amp;&amp; grep -q "from '@arduino/core-ui'" src/app/pages/dev-primitives/dev-primitives.component.ts &amp;&amp; grep -q "noindex" src/app/pages/dev-primitives/dev-primitives.component.ts &amp;&amp; pnpm exec ng build &amp;&amp; pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - `app.config.ts` includes `provideContentApi()` in providers. Verifiable by grep.
    - `app.routes.ts` has the `dev/primitives` lazy route entry.
    - `app.routes.server.ts` declares `dev/primitives` with `RenderMode.Client` BEFORE the `**` catch-all.
    - `dev-primitives.component.ts` imports primitives ONLY from `'@arduino/core-ui'` (no `projects/core-ui/src/lib/...` paths). Verifiable: `grep "core-ui/src/lib" src/app/pages/dev-primitives/dev-primitives.component.ts` returns empty.
    - The component declares 14 primitives in its `imports` array (verifiable: `grep -c "Component," src/app/pages/dev-primitives/dev-primitives.component.ts` returns 14, OR direct visual inspection lists all 14 names).
    - `ngOnInit` sets `noindex` meta + Cyrillic title and calls `contentApi.getLesson('pershyi-blymayuchyi-svitlodiod')` — the mock API is exercised at runtime.
    - The HTML template includes one `<ui-two-column>` with three `<ui-sidenote>` children, three `<ui-code-block>` instances (basic + diff + annotated), at least one `<ui-figure>` with `[fullBleed]="true"`, one `<ui-diff>`, one `<ui-pinout>` with at least 14 pin entries.
    - `pnpm exec ng build` exits 0.
    - Build output: no static HTML emitted under `dist/**/dev/primitives*.html` containing inlined showcase Cyrillic content (per RenderMode.Client). Verifiable: `find dist -path '*dev/primitives*' -name '*.html'` returns either no results or a thin CSR shell that does NOT contain `Showcase примітивів` literally.
    - `pnpm lint` exits 0 (boundary rules allow `app -> core-ui-public` per Plan 01 Task 2).
  </acceptance_criteria>
  <done>The showcase page is wired and exercises every primitive end-to-end through DI; SSG `ng build` does not emit a public prerender for `/dev/primitives`; the page is reachable in dev.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Three-breakpoint manual walk + force-en audit + accumulating-doc updates</name>
  <files>
    docs/typography-checklist.md,
    docs/force-en-audit.md
  </files>
  <read_first>
    - docs/typography-checklist.md (existing accumulating doc — Phase 1 left a section; Phase 2 appends in the SAME shape)
    - docs/force-en-audit.md (existing accumulating doc — Phase 1 left a row; Phase 2 appends a row)
    - .planning/phases/02-primitives-two-column-layout-page-model-contract/02-UI-SPEC.md §"Three-breakpoint verification gate" + §"Force-en audit"
    - .planning/phases/02-primitives-two-column-layout-page-model-contract/02-CONTEXT.md §"Showcase page wiring" (D-VISUAL-01)
    - .planning/phases/01-foundation-typography-gate/ — for the phase-1 doc-section voice
  </read_first>
  <what-built>
    - Live `/dev/primitives` page consuming `MockContentApi` via DI, rendering all 14 P2 primitives on real Ukrainian Arduino prose.
    - SSG build excludes `/dev/primitives` from the public prerender.
    - Layout tokens + `core-ui` library + boundary lint live.
  </what-built>
  <how-to-verify>

**Before the manual walk, append both docs.** This is auto-doable; do it first so the user has the checklists in front of them.

1. **Append a P2 section to `docs/typography-checklist.md`.** Use the same shape as the existing P1 section (terse rows, ASCII tables). Required content (one row per check, with a tickbox column the auditor fills):
   - Heading hierarchy renders cleanly h1 -> h2 -> h3 on `/dev/primitives`.
   - `Body` measure matches P1 (~62ch) at >=1200px; ragged-right; no auto-hyphens.
   - `Lede` is italic, distinct from body.
   - `Aside` (3 variants) renders with left accent border + padding; identical visuals across variants.
   - `Sidenote` desktop: 3 sidenotes anchored within 4px of each anchor superscript at >=1200px.
   - `Sidenote` desktop collision: when sidenotes 2 + 3 collide, the later slides down by `--sidenote-stack-gap`.
   - `Sidenote` tablet (768-1199px): inline, full-prose-measure, accent left-border.
   - `Sidenote` mobile (<768px): `<details>` wrapper with summary `Примітка {N}`, closed by default, opens on tap.
   - `Figure` body-measure (Рис. 1) + full-bleed (Рис. 2) both render with caption below; `Рис. N` prefix in accent.
   - `CodeBlock` basic: line numbers, copy button at top-right, highlighted line 7.
   - Copy interaction: click button -> label `Скопійовано` for 2s -> reverts. Failure path: simulate by denying clipboard permission OR run on `http://`-only origin if applicable; label `Не вдалося скопіювати` for 4s.
   - `CodeBlock` diff: yellow-highlight added lines, strike-through removed lines, NO red/green visible.
   - `CodeBlock` annotated: at >=1200px, 4 annotations align with their target lines within 4px; at 768-1199px, render below as `<dl>`; at <768px, render inside `<details>`.
   - `Diff` (text-level): before is muted + struck through, after has yellow highlight; hairline rule between.
   - `Pinout`: image + 14-row legend; legend is two-column at >=768px, single-column at <768px; NO hover hotspots.
   - `PageShell`: empty `<header>` and `<footer>` placeholders present; container max-width 1200px; correct padding per breakpoint.

2. **Append a P2 row to `docs/force-en-audit.md`** following the same shape as the P1 row. Required check items for the row:
   - Force browser locale to `en-US` (Chrome devtools -> sensors -> locale, or `--lang=en-US` flag).
   - Reload `/dev/primitives`. Verify:
     - All Cyrillic strings render unchanged.
     - All quotes still render as `«…»` (verbatim from authored fixtures; per D-PRE-01..05 there is no function — fixtures ship typeset).
     - Em-dashes `—` and en-dashes `–` render correctly.
     - Any date/number formatting on the page (none in P2 by default; if `≈ {N} хв` is rendered, it goes through `intl.ts`).
     - `<html lang="uk">` is preserved (no `lang="en"` leak).
     - Showcase `<title>` reads `Примітиви — Arduino UA` exactly.
   - Result: `PASS` or `FAIL` with notes.

3. **Manual three-breakpoint walk on `/dev/primitives`** — run `pnpm dev` and resize the browser (or use devtools device toolbar) to:
   - **<768px** (e.g., 375x667 — iPhone SE) — walk every checklist row.
   - **768-1199px** (e.g., 1024x768 — tablet landscape) — walk every checklist row.
   - **>=1200px** (e.g., 1440x900 — laptop) — walk every checklist row.

4. **Tick each row in the typography-checklist P2 section.** Any FAIL row blocks phase exit; resolve before declaring Phase 2 complete.

5. **Force-en audit** — switch to `en-US` and walk `/dev/primitives` once at >=1200px. Tick the audit row.

  </how-to-verify>
  <resume-signal>Type "approved" once both docs are filled in (all rows ticked PASS) AND the force-en audit passes. If any row FAILed, describe the issue + which primitive needs revision.</resume-signal>
</task>

</tasks>

<verification>
- `/dev/primitives` is reachable at `pnpm dev`.
- `pnpm exec ng build` produces a static bundle WITHOUT a prerendered `/dev/primitives.html` containing the showcase content.
- `pnpm lint` clean.
- `pnpm test` (full suite) passes — Plan 03 measure tests + Plan 05 code-block tests + Plan 02 model + content-api tests.
- `docs/typography-checklist.md` P2 section is fully ticked PASS.
- `docs/force-en-audit.md` P2 row is ticked PASS.
</verification>

<success_criteria>
- ROADMAP SC#1: every P2 primitive renders on `/dev/primitives` consumed only via `@arduino/core-ui`.
- ROADMAP SC#2: three-breakpoint walk PASSes on the showcase TwoColumn section.
- ROADMAP SC#3: three CodeBlock instances exercise basic / diff / annotated; manual walk verifies highlight + copy + diff palette + annotation alignment.
- ROADMAP SC#4: `MockContentApi` is wired end-to-end; the page resolves a `Lesson` at runtime.
- ROADMAP SC#5 (REFRAMED per CONTEXT D-PRE-05): the editorial-smell + content-gate lint passes on every fixture (delivered in Plan 02); force-en audit passes for `/dev/primitives` (delivered here).
</success_criteria>

<output>
Create `.planning/phases/02-primitives-two-column-layout-page-model-contract/02-06-SUMMARY.md`. Record:
- Final `dist/` listing for the showcase route (proving no full prerender).
- The two updated docs' P2 sections (paste in raw markdown).
- The `pnpm test` summary line.
- Phase-exit attestation: "All five ROADMAP success criteria verified PASS" with line-item evidence per SC.
- Hand-off note for Phase 3: the locked content models, `ContentApi` DI token, and primitive public-API surface are ready; P3 builds `BlockRenderer` + page templates + routing on top of these.
</output>

# Phase 4.1: Wagtail 7.4 LTS Bump — Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-09
**Phase:** 04.1-wagtail-7-4-lts-bump-deferred-from-p4-per-d-bump-01
**Areas discussed:** Pin specifier, wagtail-headless-preview compat, Pre-flight strategy, Plan granularity, Phase ordering

---

## Gray-area selection

| Option | Description | Selected |
|--------|-------------|----------|
| wagtail-headless-preview compat | Re-pin if 7.4 forces it, else keep ~=0.8 | (user deferred to defaults) |
| Pre-flight vs commit-and-fix | Throwaway-branch dry-run vs feature branch iteration | (user deferred to defaults) |
| Plan granularity | Single atomic task vs multi-task gate-per-commit | (user deferred to defaults) |
| Phase 4.1 vs Phase 5 ordering | 4.1 first vs P5 first | (user deferred to defaults) |

**User's choice:** "Just do what needs to be done to upgrade Wagtail to 7.4 properly."
**Notes:** Phase is tightly scoped by D-BUMP-01..03 (locked in P4 CONTEXT). User signaled trust in existing locks + sensible defaults. Claude resolved each gray area below with the recommended default.

---

## Pin specifier

| Option | Description | Selected |
|--------|-------------|----------|
| `wagtail~=7.4.0` | Compatible-release; allows 7.4.x patches; matches existing `~=7.3.0` pattern | ✓ |
| `wagtail==7.4.0` | Exact pin; denies patch updates | |
| `wagtail>=7.4,<7.5` | More permissive; not the project convention | |

**Resolution:** `~=7.4.0` (D-PIN-01). Rationale: matches the existing pattern verbatim and stays inside the LTS series for patch updates.

---

## wagtail-headless-preview compat

| Option | Description | Selected |
|--------|-------------|----------|
| Keep `~=0.8`, verify via gate | Cheapest path; gate 4 (preview round-trip) catches breakage | ✓ |
| Pre-emptively bump to latest 0.x or 1.x | Eliminates one risk surface but adds churn | |
| Read compat matrix in Task 0 first | Decide based on lib's own compat statement | (folded into Task 0 of D-PLAN-01) |

**Resolution:** D-PIN-03. Task 0 release-notes scan reads the lib's compat matrix; if it explicitly declares 7.4 compat → keep ~=0.8. If breakage surfaces at gate 4 → minimum bump that restores compat, document in PLAN summary, else D-BUMP-03 rollback.

---

## Pre-flight vs commit-and-fix

| Option | Description | Selected |
|--------|-------------|----------|
| Feature branch `chore/wagtail-7.4-lts`, iterate gates in place | Single-author repo; rollback = abandon branch | ✓ |
| Throwaway spike branch first, then real branch | Extra ceremony; same effective rollback as feature-branch abandon | |

**Resolution:** D-APPROACH-01..02. The feature-branch flow IS the pre-flight.

---

## Plan granularity

| Option | Description | Selected |
|--------|-------------|----------|
| Single atomic task (one commit for everything) | Simpler PLAN, worse bisect | |
| Multi-task, one commit per gate | Better bisect; matches P4 commit pattern | ✓ |

**Resolution:** D-PLAN-01. 7 tasks: Task 0 (release-notes scan), Tasks 1-5 (gates), Task 6 (closure: ROADMAP tick + KD4-06 closure + force-en row + SUMMARY).

---

## Phase 4.1 vs Phase 5 ordering

| Option | Description | Selected |
|--------|-------------|----------|
| 4.1 → 5 (bump in dev, deploy LTS) | Single verification surface | ✓ |
| 5 → 4.1 (deploy 7.3, bump in prod) | Doubles verification surface | |

**Resolution:** D-ORDER-01. 4.1 first.

---

## Claude's Discretion

- Exact wording of gate-commit messages.
- PR vs direct merge of feature branch.
- Release-notes scan depth in Task 0.
- Whether to inline-comment `pyproject.toml` if a `wagtail-headless-preview` re-pin happens.

## Deferred Ideas

- Blanket dependency upgrade — milestone v1.1 maintenance.
- `wagtail-headless-preview` major bump if not forced by 7.4 — milestone v1.1.
- KD4-01..04 — already routed to P5/P6 per P4 closure.

---
phase: 04.1-wagtail-7-4-lts-bump-deferred-from-p4-per-d-bump-01
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/pyproject.toml
  - backend/uv.lock
  - docs/force-en-audit.md
  - .planning/ROADMAP.md
  - .planning/phases/04-wagtail-backend-skeleton-contract-match-dockerized/04-09-SUMMARY.md
  - .planning/phases/04.1-wagtail-7-4-lts-bump-deferred-from-p4-per-d-bump-01/04.1-01-SUMMARY.md
autonomous: false
requirements: [UKR-06]

must_haves:
  truths:
    - "Wagtail pin in backend/pyproject.toml is wagtail~=7.4.0 (not ~=7.3.0)"
    - "backend/uv.lock resolves Wagtail to a 7.4.x version"
    - "Docker stack (wagtail, postgres, minio) rebuilds and reports healthy"
    - "Wagtail admin smoke (curl + manual login + edit/save draft) green"
    - "pnpm contract:diff exits 0 with 7/7 PASS and zero new allowlist entries"
    - "Headless preview round-trip works end-to-end on 7.4"
    - "MinIO upload smoke confirms originals + renditions land in MinIO (not local FS)"
    - "Force-en audit Phase 4.1 row appended to docs/force-en-audit.md"
    - "ROADMAP Phase 4.1 row ticks 0/1 → 1/1, status Complete"
    - "KD4-06 closed in 04-09-SUMMARY.md with pointer to Phase 4.1 SUMMARY"
  artifacts:
    - path: "backend/pyproject.toml"
      provides: "Wagtail 7.4 LTS pin"
      contains: "wagtail~=7.4.0"
    - path: "backend/uv.lock"
      provides: "Locked resolution of Wagtail 7.4.x"
      contains: "wagtail"
    - path: "docs/force-en-audit.md"
      provides: "Phase 4.1 force-en audit ledger row"
      contains: "Phase 4.1"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 4.1 marked Complete (1/1)"
      contains: "Phase 4.1"
    - path: ".planning/phases/04.1-wagtail-7-4-lts-bump-deferred-from-p4-per-d-bump-01/04.1-01-SUMMARY.md"
      provides: "Phase 4.1 closure SUMMARY with D-BUMP-02 5-gate scoreboard"
      min_lines: 40
  key_links:
    - from: "backend/pyproject.toml"
      to: "backend/uv.lock"
      via: "uv lock --upgrade-package wagtail"
      pattern: "wagtail.*7\\.4"
    - from: "compose.yml + compose.dev.yml"
      to: "wagtail Docker image (rebuilt)"
      via: "docker compose build wagtail"
      pattern: "Wagtail/7\\.4"
    - from: "scripts/contract-diff.mjs"
      to: "7 CONTRACT-04 fixtures"
      via: "pnpm contract:diff"
      pattern: "7/7 PASS"
---

<objective>
Bump Wagtail from `~=7.3.0` to `~=7.4.0` LTS, refresh the lock, rebuild the Dockerized backend, and re-run the full P4 verification gate set per D-BUMP-02. Close KD4-06.

Purpose: Land the LTS series in dev BEFORE Phase 5 ships the production stack (D-ORDER-01). Avoid double-verification surface of bumping in prod after deploy.
Output: Wagtail 7.4 LTS running locally, all 5 D-BUMP-02 gates green, force-en row appended, ROADMAP Phase 4.1 closed, KD4-06 resolved, atomic per-task commits on `chore/wagtail-7.4-lts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-wagtail-7-4-lts-bump-deferred-from-p4-per-d-bump-01/04.1-CONTEXT.md
@.planning/phases/04-wagtail-backend-skeleton-contract-match-dockerized/04-CONTEXT.md
@.planning/phases/04-wagtail-backend-skeleton-contract-match-dockerized/04-09-SUMMARY.md
@backend/pyproject.toml
@backend/uv.lock
@compose.yml
@compose.dev.yml
@scripts/contract-diff.mjs
@docs/force-en-audit.md

<interfaces>
Current Wagtail pin (backend/pyproject.toml lines 5–10):
```toml
dependencies = [
  "wagtail~=7.3.0",
  "django~=5.2.0",
  "psycopg[binary]~=3.2",
  "wagtail-headless-preview~=0.8",
  "django-storages[s3]~=1.14",
  "gunicorn~=23.0",
]
```
Target: change line 6 only — `"wagtail~=7.3.0"` → `"wagtail~=7.4.0"`. Sibling pins NOT touched (D-PIN-04). `wagtail-headless-preview~=0.8` stays unless 7.4 forces a re-pin (D-PIN-03).

Stack harness (re-used for every gate):
```bash
docker compose -f compose.yml -f compose.dev.yml up -d
docker compose -f compose.yml -f compose.dev.yml ps
```

Branch flow (D-APPROACH-01):
- Feature branch: `chore/wagtail-7.4-lts`
- One atomic commit per task (Tasks 1..6)
- On full pass: merge to master (single-author repo, fast-forward acceptable)
- On D-BUMP-03 trigger: `git revert` pin commit, abandon branch, file KD4.1-01

Branch-create guard (executor MUST honor): only run `git checkout -b chore/wagtail-7.4-lts` if `git branch --show-current` returns `master`. If already on `chore/wagtail-7.4-lts`, continue without re-creating.
</interfaces>
</context>

<tasks>

<task type="auto" autonomous="true">
  <name>Task 0: Release-notes scan (Wagtail 7.4 LTS + headless-preview 0.8 compat)</name>
  <read_first>
    - .planning/phases/04.1-wagtail-7-4-lts-bump-deferred-from-p4-per-d-bump-01/04.1-CONTEXT.md (D-APPROACH-03 scope of scan)
    - backend/pyproject.toml (current pins, what we touch and what we don't)
    - .planning/phases/04-wagtail-backend-skeleton-contract-match-dockerized/04-09-SUMMARY.md §"Live-gate scoreboard (final)" (what "green on 7.3" looked like)
  </read_first>
  <action>
    Pre-implementation research only — produces NO code change and NO commit.

    Scan the following sources:
    1. Wagtail 7.4 LTS release notes (released 2026-05-04). Focus sections: "Upgrade considerations", "Breaking changes", "Deprecations", REST API v2 changes, Page model migration requirements, settings shape changes.
    2. `wagtail-headless-preview` 0.8.x README/CHANGELOG — verify Wagtail 7.4 is in the supported matrix. If unstated, check the project's open issues for "Wagtail 7.4" mentions.
    3. Cross-reference findings against P4 code surface area: `apps/contract/` page models with `api_fields`, `HeadlessPreviewMixin` usage, StreamField block shapes (FigureBlock/PinoutBlock with `*_override` fields), `serve_preview()` token-mint flow, `django-storages[s3]` + `boto3` against MinIO.

    Capture findings in a new file `.planning/phases/04.1-wagtail-7-4-lts-bump-deferred-from-p4-per-d-bump-01/04.1-RELEASE-NOTES.md` with sections:
    - "Wagtail 7.4 LTS — relevant changes" (each entry: change description + impact-on-P4-surface assessment: NONE / VERIFY / RISK / BREAKING)
    - "wagtail-headless-preview 0.8 compatibility verdict" (compatible / re-pin needed / unknown — needs runtime verification in Task 4)
    - "Pre-flight risk register" (any RISK/BREAKING items lifted to a numbered list with the gate where they will surface)

    If anything material to D-PIN-01..04 surfaces (deprecated APIs touched by P4 code, settings shape changes, REST API v2 serializer changes, Page-model migration requirements, or `wagtail-headless-preview` 0.8 incompatibility), flag in the file's "Pre-flight risk register" section AND echo to the user as a chat message before proceeding to Task 1.

    Do NOT create the feature branch yet (Task 1 owns that). Do NOT modify pyproject.toml or uv.lock.
  </action>
  <verify>
    <automated>test -f .planning/phases/04.1-wagtail-7-4-lts-bump-deferred-from-p4-per-d-bump-01/04.1-RELEASE-NOTES.md && grep -E "Pre-flight risk register|wagtail-headless-preview 0.8 compatibility verdict" .planning/phases/04.1-wagtail-7-4-lts-bump-deferred-from-p4-per-d-bump-01/04.1-RELEASE-NOTES.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/04.1-wagtail-7-4-lts-bump-deferred-from-p4-per-d-bump-01/04.1-RELEASE-NOTES.md` exists.
    - File contains literal headings "Wagtail 7.4 LTS — relevant changes", "wagtail-headless-preview 0.8 compatibility verdict", "Pre-flight risk register" (`grep -c` each → ≥1).
    - `git status` shows the file as untracked (NOT yet committed; commit happens at end of Task 1 or its own optional commit — see Task 1 for the actual branch-create + commit step).
    - No edits to `backend/pyproject.toml` or `backend/uv.lock` (verify: `git diff backend/pyproject.toml backend/uv.lock` is empty).
  </acceptance_criteria>
  <done>Release-notes scan written, risks flagged to user, no code touched yet.</done>
</task>

<task type="auto" autonomous="true">
  <name>Task 1: Pin + lock + build (D-BUMP-02 gate 1)</name>
  <read_first>
    - backend/pyproject.toml (line 6 is the edit target)
    - backend/uv.lock (verify pre-state has wagtail 7.3.x)
    - compose.yml and compose.dev.yml (the stack harness)
    - .planning/phases/04.1-wagtail-7-4-lts-bump-deferred-from-p4-per-d-bump-01/04.1-RELEASE-NOTES.md (Task 0 output — heed any RISK items)
  </read_first>
  <action>
    Branch guard: run `git branch --show-current`. If output is `master`, run `git checkout -b chore/wagtail-7.4-lts`. If output is already `chore/wagtail-7.4-lts`, continue (do not re-create). Any other branch → STOP and ask the user.

    Edit `backend/pyproject.toml`: change line 6 from the literal `"wagtail~=7.3.0",` to the literal `"wagtail~=7.4.0",`. No other lines in the file change.

    Refresh the lock with the targeted upgrade (D-PIN-02):
    ```bash
    cd backend && uv lock --upgrade-package wagtail
    ```
    NOT `uv lock --upgrade` (would churn unrelated deps). Transitive bumps caused by Wagtail's own constraints are acceptable.

    Rebuild the wagtail image and bring the stack up:
    ```bash
    docker compose -f compose.yml -f compose.dev.yml build wagtail
    docker compose -f compose.yml -f compose.dev.yml up -d
    docker compose -f compose.yml -f compose.dev.yml ps
    ```

    If `wagtail-headless-preview~=0.8` fails resolution in `uv lock` due to a Wagtail 7.4 incompatibility (D-PIN-03), take the minimum bump that restores compatibility (e.g. `~=0.9` if available), and add an inline comment in `pyproject.toml` noting the reason. If no compatible release exists, STOP and surface as a D-BUMP-03 trigger candidate to the user.

    Commit atomically on the feature branch:
    ```bash
    git add backend/pyproject.toml backend/uv.lock .planning/phases/04.1-wagtail-7-4-lts-bump-deferred-from-p4-per-d-bump-01/04.1-RELEASE-NOTES.md
    git commit -m "chore(04.1): pin wagtail 7.4 LTS, refresh lock, rebuild stack"
    ```
    (Include the release-notes file in this commit — it's the artifact backing the pin decision.)
  </action>
  <verify>
    <automated>grep -F "wagtail~=7.4.0" backend/pyproject.toml &amp;&amp; ! grep -F "wagtail~=7.3.0" backend/pyproject.toml &amp;&amp; grep -E "name = \"wagtail\"" -A 1 backend/uv.lock | grep -E "version = \"7\\.4" &amp;&amp; docker compose -f compose.yml -f compose.dev.yml ps --format json | grep -E "\"State\":\"running\"|\"Health\":\"healthy\""</automated>
  </verify>
  <acceptance_criteria>
    - `git branch --show-current` → `chore/wagtail-7.4-lts`.
    - `grep -F "wagtail~=7.4.0" backend/pyproject.toml` → exit 0, exactly 1 match.
    - `grep -F "wagtail~=7.3.0" backend/pyproject.toml` → exit 1 (zero matches).
    - `grep -E "name = \"wagtail\"" -A 1 backend/uv.lock | grep -E "version = \"7\\.4"` → exit 0 with at least one match.
    - `docker compose -f compose.yml -f compose.dev.yml ps` shows `wagtail`, `postgres`, `minio` rows; State=`running`; Health column shows `healthy` (or `(healthy)`) for those that publish a health check.
    - `git log -1 --oneline` on `chore/wagtail-7.4-lts` shows a single commit referencing 7.4 LTS pin.
    - On rollback trigger: STOP, do NOT proceed to Task 2; instead document in chat and follow D-ROLLBACK-01.
  </acceptance_criteria>
  <done>Branch live, pin bumped to 7.4, lock refreshed (targeted), wagtail image rebuilt, stack healthy, atomic commit on branch.</done>
</task>

<task type="checkpoint:human-verify" autonomous="false" gate="blocking">
  <name>Task 2: Wagtail admin smoke (D-BUMP-02 gate 2)</name>
  <read_first>
    - .planning/phases/04-wagtail-backend-skeleton-contract-match-dockerized/04-09-SUMMARY.md §"Live-gate scoreboard (final)" (the row "curl /admin/login/ → 200" is the precedent)
  </read_first>
  <what-built>Wagtail 7.4 admin reachable on the rebuilt stack from Task 1.</what-built>
  <action>
    Automated probe first:
    ```bash
    curl -sI http://localhost:8000/admin/login/
    ```
    Expect first line `HTTP/1.1 200 OK` (or `HTTP/2 200`).

    Then prompt the user to perform the manual walk:
    1. Open `http://localhost:8000/admin/login/` in a browser.
    2. Log in with the existing dev superuser (created during P4).
    3. Open any existing draft page (one of the 7 fixtures from `seed_fixtures`).
    4. Make a trivial edit (e.g. add and remove a space in a draft-only field), click "Save draft".
    5. Return to the page list, confirm the page still appears with the expected title and the "Edited" timestamp updated.
    6. Log out.

    On user `done`/`approved`, append a row to a scratch list inside the eventual SUMMARY (Task 6) marking gate 2 PASS. Then commit a log-only entry on the branch:
    ```bash
    git commit --allow-empty -m "chore(04.1): wagtail admin smoke PASS on 7.4"
    ```
    (Empty commit is acceptable — gate 2 surfaces no code change unless 7.4 forced one in Task 1.)

    On user FAIL: STOP, capture the symptom, evaluate D-BUMP-03 trigger.
  </action>
  <how-to-verify>
    1. `curl -sI http://localhost:8000/admin/login/` returns `200`.
    2. Browser login succeeds.
    3. Draft edit + save + page-list view all complete without error toast.
  </how-to-verify>
  <verify>
    <automated>curl -sI -o /dev/null -w "%{http_code}" http://localhost:8000/admin/login/ | grep -E "^200$"</automated>
  </verify>
  <acceptance_criteria>
    - `curl -sI http://localhost:8000/admin/login/` first line matches `HTTP/[12](\.[01])? 200`.
    - User reports `done` or `approved` after the 6-step walk.
    - `git log -1 --oneline` shows the gate-2 log commit on `chore/wagtail-7.4-lts`.
  </acceptance_criteria>
  <resume-signal>Type `approved` after completing the 6-step admin walk, or describe the failure.</resume-signal>
  <done>Admin login + draft edit walk green on 7.4; log commit on branch.</done>
</task>

<task type="auto" autonomous="true">
  <name>Task 3: Contract diff 7/7 PASS (D-BUMP-02 gate 3)</name>
  <read_first>
    - scripts/contract-diff.mjs (D-CONTRACT-02 allowlist — DO NOT modify)
    - .planning/phases/04-wagtail-backend-skeleton-contract-match-dockerized/04-09-SUMMARY.md (precedent: "pnpm contract:diff → 7/7 PASS")
    - .planning/phases/04.1-wagtail-7-4-lts-bump-deferred-from-p4-per-d-bump-01/04.1-CONTEXT.md (D-VERIFY-02 — allowlist FROZEN)
  </read_first>
  <action>
    Run the contract-diff gate against the running 7.4 stack:
    ```bash
    pnpm contract:diff
    ```
    Expect: exit code 0, output contains `7/7 PASS` (matching P4 precedent).

    Allowlist is FROZEN per D-VERIFY-02. If any fixture diffs, DO NOT add an allowlist entry. Treat the failure as a D-BUMP-03 evaluation point: capture the diff body, attribute root cause to a Wagtail 7.4 internal change (likely REST API v2 serializer drift or StreamField block-shape change), surface to the user, and follow D-ROLLBACK-01 if not fixable in ~1 day.

    Verify no allowlist drift snuck in: `git diff scripts/contract-diff.mjs` must show zero lines changed.

    Commit a log-only entry on the branch:
    ```bash
    git commit --allow-empty -m "chore(04.1): contract:diff 7/7 PASS on 7.4 (allowlist unchanged)"
    ```
  </action>
  <verify>
    <automated>pnpm contract:diff &amp;&amp; test -z "$(git diff scripts/contract-diff.mjs)"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm contract:diff` exits 0.
    - Output contains literal `7/7 PASS`.
    - `git diff scripts/contract-diff.mjs` produces zero lines (allowlist frozen).
    - `git log -1 --oneline` shows the gate-3 log commit.
  </acceptance_criteria>
  <done>Contract diff 7/7 PASS on 7.4 with frozen allowlist; log commit on branch.</done>
</task>

<task type="checkpoint:human-verify" autonomous="false" gate="blocking">
  <name>Task 4: Preview round-trip (D-BUMP-02 gate 4)</name>
  <read_first>
    - .planning/phases/04-wagtail-backend-skeleton-contract-match-dockerized/04-09-SUMMARY.md (the section ending "FE route param → WagtailContentApi.getLessonPreview() → /api/v2/page_preview/?content_type=…&token=… → Wagtail's PagePreviewAPIViewSet.detail_view() → PagePreview.objects.get(token=…) → as_page() → serialized → FE renders" — this is the canonical procedure)
    - apps/contract/ Page models with HeadlessPreviewMixin (precondition for serve_preview)
  </read_first>
  <what-built>Wagtail 7.4 + wagtail-headless-preview 0.8 round-trip via programmatically-minted token.</what-built>
  <action>
    Mint a token programmatically (preferred path) using the same procedure proven in P4:
    1. From the Wagtail admin, open any one of the 7 fixture pages in the editor.
    2. Click the standalone **"Open preview in new tab"** button (KD4-01 fallback path — the inline preview pane does not honor the headless redirect; this is documented and expected).
    3. The new tab navigates to `http://localhost:<fe-port>/preview/<kind>/<token>/` (where `<kind>` is `lesson|article|topic|index` per the fixture and `<token>` is the freshly-minted UUID).
    4. Confirm the FE renders the preview content (title, body StreamField blocks, hero/figure renditions if present).
    5. View source: `<html lang="uk">` is present.

    If the round-trip fails because of Wagtail 7.4 + wagtail-headless-preview 0.8 incompatibility, evaluate D-PIN-03 (minimum re-pin) before D-BUMP-03 (rollback). Re-pin is allowed here — bump `wagtail-headless-preview` to the minimum compatible version, refresh the lock with `cd backend && uv lock --upgrade-package wagtail-headless-preview`, rebuild `docker compose -f compose.yml -f compose.dev.yml build wagtail`, restart, retry the round-trip.

    On success, commit a log-only entry on the branch:
    ```bash
    git commit --allow-empty -m "chore(04.1): preview round-trip PASS on 7.4"
    ```
    If a re-pin happened, commit the lock + pyproject changes BEFORE the empty log commit:
    ```bash
    git add backend/pyproject.toml backend/uv.lock
    git commit -m "chore(04.1): repin wagtail-headless-preview for 7.4 compat"
    git commit --allow-empty -m "chore(04.1): preview round-trip PASS on 7.4 (with headless-preview repin)"
    ```
  </action>
  <how-to-verify>
    1. From admin → fixture page → "Open preview in new tab" button.
    2. New tab URL matches `http://localhost:<fe-port>/preview/<kind>/<token>/`.
    3. FE renders content (no error page, blocks appear).
    4. View source shows `<html lang="uk">`.
  </how-to-verify>
  <verify>
    <automated>MISSING — manual gate; verification is the user's `approved` after step-4 view-source confirmation</automated>
  </verify>
  <acceptance_criteria>
    - User reports `approved` after the 5-step walk.
    - URL pattern matches `/preview/<kind>/<token>/` (kind ∈ {lesson, article, topic, index}; token is a UUID-looking string).
    - View-source contains literal `<html lang="uk">`.
    - On re-pin path: `grep -E "wagtail-headless-preview" backend/pyproject.toml` shows the new compat pin string and `git log` shows two commits (re-pin + log).
    - On no-re-pin path: only the empty log commit added.
  </acceptance_criteria>
  <resume-signal>Type `approved` after the 5-step preview walk, or describe the failure.</resume-signal>
  <done>Preview round-trip green on 7.4; commit(s) on branch.</done>
</task>

<task type="checkpoint:human-verify" autonomous="false" gate="blocking">
  <name>Task 5: MinIO upload smoke (D-BUMP-02 gate 5)</name>
  <read_first>
    - compose.yml + compose.dev.yml (MinIO service definition, bucket layout)
    - backend Wagtail settings (django-storages[s3] config — bucket names from P4)
    - .planning/phases/04-wagtail-backend-skeleton-contract-match-dockerized/04-09-SUMMARY.md ("seed_fixtures creates 7 published pages" — confirms storage backend wired)
  </read_first>
  <what-built>Image uploads on Wagtail 7.4 land in MinIO (originals + renditions) — NOT on local filesystem.</what-built>
  <action>
    Manual smoke walk:
    1. Wagtail admin → Images → Add an image. Choose any small PNG/JPG from disk (≤500KB). Title it `phase-4-1-smoke-<timestamp>`.
    2. Save. Wagtail generates renditions on first render — to force at least one rendition, edit any fixture page that has a hero/figure block, swap in the new image, save draft, view preview (Task 4's preview path will trigger the rendition pipeline).

    Verification commands the executor runs (assumes the `mc` sidecar / a host-side `mc` pointed at the dev MinIO):
    ```bash
    docker compose -f compose.yml -f compose.dev.yml exec mc mc ls --recursive local/originals/ | grep -i "phase-4-1-smoke"
    docker compose -f compose.yml -f compose.dev.yml exec mc mc ls --recursive local/images/ | grep -i "phase-4-1-smoke"
    ```
    (Bucket names per P4: originals private, images public-read — adjust the bucket name strings if the running config uses different names; verify by `docker compose -f compose.yml -f compose.dev.yml exec mc mc ls local/`.)

    Verify NOT on local filesystem:
    ```bash
    docker compose -f compose.yml -f compose.dev.yml exec wagtail find /app -name "phase-4-1-smoke*" -type f 2>/dev/null
    ```
    Expect zero output.

    Commit log-only:
    ```bash
    git commit --allow-empty -m "chore(04.1): minio upload smoke PASS on 7.4"
    ```
  </action>
  <how-to-verify>
    1. Image uploaded via admin succeeds.
    2. `mc ls` finds the original in the originals bucket.
    3. `mc ls` finds at least one rendition in the images bucket after a render.
    4. `find` inside the wagtail container returns zero matches under `/app`.
  </how-to-verify>
  <verify>
    <automated>docker compose -f compose.yml -f compose.dev.yml exec -T mc mc ls --recursive local/originals/ | grep -i "phase-4-1-smoke"</automated>
  </verify>
  <acceptance_criteria>
    - `mc ls --recursive local/originals/` shows the uploaded original by name (`phase-4-1-smoke-*`).
    - `mc ls --recursive local/images/` shows at least one rendition matching the original (after preview render).
    - `find /app -name "phase-4-1-smoke*"` inside the wagtail container returns zero results.
    - User reports `approved` after the 2-step admin upload + page-attach walk.
    - `git log -1 --oneline` shows the gate-5 log commit.
  </acceptance_criteria>
  <resume-signal>Type `approved` after upload + attach + render, or describe the failure.</resume-signal>
  <done>MinIO upload + rendition flow green on 7.4; log commit on branch.</done>
</task>

<task type="checkpoint:human-verify" autonomous="false" gate="blocking">
  <name>Task 6: Phase-exit closure — force-en row + ROADMAP tick + KD4-06 close + SUMMARY</name>
  <read_first>
    - docs/force-en-audit.md (P1 row format is the template — append a P4.1 section in the same shape)
    - .planning/ROADMAP.md (Phase 4.1 entry around lines 103–108 — the row to tick)
    - .planning/phases/04-wagtail-backend-skeleton-contract-match-dockerized/04-09-SUMMARY.md (KD4-06 line in §"Known Debt" — the row to close; also §"Verdict" pattern for our own SUMMARY)
    - $HOME/.claude/get-shit-done/templates/summary.md (SUMMARY template)
  </read_first>
  <what-built>Phase 4.1 closure artifacts: force-en audit row, ROADMAP tick, KD4-06 closure, Phase 4.1 SUMMARY with D-BUMP-02 5-gate scoreboard.</what-built>
  <action>
    Manual force-en walk (UKR-06):
    1. Open Chrome DevTools → Sensors → Locale `en-US` (mirrors P4 KD4-05 procedure).
    2. Visit Wagtail admin (`http://localhost:8000/admin/`) — confirm Ukrainian editor labels render (residual upstream-Wagtail strings deferred to P6 per CONTEXT.md, same as P4).
    3. Programmatic scan of all 4 REST endpoints for English month/day literals — run:
       ```bash
       for ep in lessons articles topics index; do
         curl -s "http://localhost:8000/api/v2/pages/?type=contract.${ep^}Page" | grep -E "January|February|March|April|May|June|July|August|September|October|November|December|Mon|Tue|Wed|Thu|Fri|Sat|Sun" || echo "${ep}: zero English literals"
       done
       ```
       Expect each line ends with `zero English literals`. (Endpoint names match P4 fixture types; adjust if the actual app/model names differ — verify against `apps/contract/` page models.)
    4. Mint a fresh preview token (Task 4 procedure) and view-source the preview page → confirm `<html lang="uk">`.
    5. Visit FE homepage `http://localhost:<fe-port>/` — confirm Ukrainian rendering.

    Edit `docs/force-en-audit.md`: append a new section using the P1 run-record format:
    ```markdown
    ---

    ## Run record — Phase 4.1 (2026-05-10)

    Audit run by: Phase 4.1 executor (Opus 4.7) — automated portions, plus human visual confirmation under DevTools Sensors `en-US`.

    | # | Check | Result |
    | - | ----- | ------ |
    | 1 | Wagtail admin under Sensors `en-US` — Ukrainian editor labels | **PASS** (residual upstream Wagtail strings tracked for P6 per P4 KD4-05 precedent) |
    | 2 | REST `/api/v2/pages/?type=contract.LessonPage` — English month/day literals | **PASS** (zero matches) |
    | 3 | REST `/api/v2/pages/?type=contract.ArticlePage` — English month/day literals | **PASS** (zero matches) |
    | 4 | REST `/api/v2/pages/?type=contract.TopicPage` — English month/day literals | **PASS** (zero matches) |
    | 5 | REST `/api/v2/pages/?type=contract.IndexPage` — English month/day literals | **PASS** (zero matches) |
    | 6 | `/preview/<kind>/<token>/` view source `<html lang="uk">` | **PASS** |
    | 7 | FE homepage `/` Ukrainian | **PASS** |

    **Phase 4.1 force-en audit: ALL PASS.** UKR-06 satisfied for the Wagtail 7.4 LTS bump.
    ```
    (Adjust individual rows to PASS/FAIL/PARTIAL based on actual walk findings — document gaps if any; do NOT silently mark PASS.)

    Edit `.planning/ROADMAP.md`: locate the `### Phase 4.1: Wagtail 7.4 LTS Bump` block. Update its progress row from `0/1` to `1/1` and status to `**Complete**` (mirror the format of Phase 4's closed row in the same file). If the row format differs from `0/1`, match the existing scheme used elsewhere in ROADMAP.md.

    Edit `.planning/phases/04-wagtail-backend-skeleton-contract-match-dockerized/04-09-SUMMARY.md`: in §"Known Debt" table, find the KD4-06 row. Strikethrough its severity (`~~low~~`) and append `**RESOLVED 2026-05-10:** Phase 4.1 closed — Wagtail 7.4 LTS bump shipped, all 5 D-BUMP-02 gates PASS, force-en row appended. See `.planning/phases/04.1-wagtail-7-4-lts-bump-deferred-from-p4-per-d-bump-01/04.1-01-SUMMARY.md`.` to its Description cell. Change "Tracked for" from `P4.1` to `n/a — closed`. (Mirror the KD4-05 closure pattern at lines just above.)

    Write `.planning/phases/04.1-wagtail-7-4-lts-bump-deferred-from-p4-per-d-bump-01/04.1-01-SUMMARY.md` using the project's SUMMARY template. Required sections:
    - Phase goal recap (1 paragraph).
    - **D-BUMP-02 5-gate scoreboard** (table with columns Gate / Status / Evidence-pointer):
      | Gate | Status | Evidence |
      |------|--------|----------|
      | 1. Pin + lock + build | ✓ PASS | commit `<sha>` |
      | 2. Wagtail admin smoke | ✓ PASS | commit `<sha>` + user `approved` |
      | 3. `pnpm contract:diff` 7/7 PASS | ✓ PASS | commit `<sha>`, allowlist unchanged |
      | 4. Preview round-trip | ✓ PASS | commit `<sha>` + user `approved` |
      | 5. MinIO upload smoke | ✓ PASS | commit `<sha>` + `mc ls` evidence |
    - Force-en audit row (Phase 4.1) result.
    - KD4-06 closure note.
    - `wagtail-headless-preview` re-pin status (yes/no; if yes, version + reason).
    - Verdict (Complete / Complete-with-debt-rollover / Rolled-back-per-D-BUMP-03).
    - Commits-added list (atomic per-task commits on `chore/wagtail-7.4-lts`).

    Commit atomically:
    ```bash
    git add docs/force-en-audit.md .planning/ROADMAP.md .planning/phases/04-wagtail-backend-skeleton-contract-match-dockerized/04-09-SUMMARY.md .planning/phases/04.1-wagtail-7-4-lts-bump-deferred-from-p4-per-d-bump-01/04.1-01-SUMMARY.md
    git commit -m "docs(04.1): close phase — force-en P4.1 ALL PASS, KD4-06 resolved, ROADMAP ticked"
    ```

    Then surface to the user: branch `chore/wagtail-7.4-lts` is ready to merge to master (single-author repo; fast-forward acceptable per D-APPROACH-01). Do NOT auto-merge — wait for user confirmation.
  </action>
  <how-to-verify>
    1. DevTools Sensors force-en walk completes across the 5 surfaces above with results captured row-by-row.
    2. `docs/force-en-audit.md` has a `Phase 4.1` section with a results table.
    3. `.planning/ROADMAP.md` Phase 4.1 row reads `1/1` and `**Complete**`.
    4. `04-09-SUMMARY.md` KD4-06 row marked closed with pointer to Phase 4.1 SUMMARY.
    5. `04.1-01-SUMMARY.md` exists with the 5-gate scoreboard.
    6. Final commit on branch.
  </how-to-verify>
  <verify>
    <automated>grep -F "Phase 4.1" docs/force-en-audit.md &amp;&amp; grep -E "Phase 4\.1.*1/1|Phase 4\.1.*Complete" .planning/ROADMAP.md &amp;&amp; grep -E "KD4-06.*RESOLVED|~~low~~.*KD4-06|KD4-06.*closed" .planning/phases/04-wagtail-backend-skeleton-contract-match-dockerized/04-09-SUMMARY.md &amp;&amp; test -f .planning/phases/04.1-wagtail-7-4-lts-bump-deferred-from-p4-per-d-bump-01/04.1-01-SUMMARY.md &amp;&amp; grep -E "D-BUMP-02|5-gate|scoreboard" .planning/phases/04.1-wagtail-7-4-lts-bump-deferred-from-p4-per-d-bump-01/04.1-01-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/force-en-audit.md` contains literal heading text `Phase 4.1` (`grep -F "Phase 4.1" docs/force-en-audit.md` → exit 0).
    - The Phase 4.1 audit row shows `ALL PASS` (or documented findings if any — never silently PASS).
    - `.planning/ROADMAP.md` Phase 4.1 entry shows `1/1` and `**Complete**`.
    - `04-09-SUMMARY.md` KD4-06 row contains literal `RESOLVED` or `closed` plus a pointer to `04.1-01-SUMMARY.md`.
    - `04.1-01-SUMMARY.md` exists, contains the literal strings `D-BUMP-02`, `5-gate` (or `scoreboard`), and one row per gate marked PASS.
    - User reports `approved` after the 5-step force-en walk.
    - `git log -1 --oneline` shows the closure commit on `chore/wagtail-7.4-lts`.
    - User informed branch is ready to merge; merge NOT executed by the executor.
  </acceptance_criteria>
  <resume-signal>Type `approved` after the force-en walk + visual confirmation of the four edited files, or describe any FAIL findings.</resume-signal>
  <done>Phase 4.1 closed: force-en row appended, ROADMAP ticked, KD4-06 resolved, SUMMARY written, atomic closure commit on branch, user notified branch is mergeable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI → wagtail Docker image build | Wagtail 7.4 + transitive deps pulled from PyPI during `docker compose build wagtail` |
| Browser ↔ Wagtail admin (localhost) | Editor authentication during the manual smoke walks (Tasks 2/4/5) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-01 | Tampering (supply chain) | wagtail Docker image build (PyPI fetch) | mitigate | `uv.lock` pins exact versions + hashes; targeted `uv lock --upgrade-package wagtail` limits churn surface; image build is reproducible from the committed lock |
| T-04.1-02 | Information Disclosure | `.env` secrets during local rebuild | accept | Existing P4 controls unchanged: `.env` gitignored, gitleaks pre-commit hook active, `DEBUG=False` in non-dev settings split; this phase introduces no new env vars |
| T-04.1-03 | Elevation of Privilege | Wagtail admin at `localhost:8000/admin/` during dev rebuild | accept | Dev-only port; not exposed externally; superuser created in P4 with strong password; no public surface added by this phase |
| T-04.1-04 | Denial of Service | Stack rebuild leaves containers in unhealthy state | mitigate | Task 1 acceptance criteria require `docker compose ps` to show `running` + `healthy`; on failure, D-ROLLBACK-01 reverts the pin commit |
</threat_model>

<verification>
Phase 4.1 passes when ALL of the following are observable on the `chore/wagtail-7.4-lts` branch:

1. `grep -F "wagtail~=7.4.0" backend/pyproject.toml` → exit 0; `grep -F "wagtail~=7.3.0" backend/pyproject.toml` → exit 1.
2. `grep -E "name = \"wagtail\"" -A 1 backend/uv.lock | grep -E "version = \"7\\.4"` → exit 0.
3. `docker compose -f compose.yml -f compose.dev.yml ps` shows wagtail/postgres/minio running + healthy.
4. `curl -sI http://localhost:8000/admin/login/` returns `200`.
5. `pnpm contract:diff` exits 0 with `7/7 PASS`; `git diff scripts/contract-diff.mjs` empty.
6. Headless preview round-trip green (user-confirmed `approved` on Task 4).
7. `mc ls --recursive local/originals/` and `local/images/` show the smoke upload + rendition; `find /app -name "phase-4-1-smoke*"` inside wagtail container returns zero.
8. `docs/force-en-audit.md` has a `Phase 4.1` row marked ALL PASS (or documented findings).
9. `.planning/ROADMAP.md` Phase 4.1 row shows `1/1` and `**Complete**`.
10. `04-09-SUMMARY.md` KD4-06 row marked RESOLVED with pointer to Phase 4.1 SUMMARY.
11. `04.1-01-SUMMARY.md` exists with D-BUMP-02 5-gate scoreboard.
12. Branch `chore/wagtail-7.4-lts` has 6 atomic commits (Tasks 1–6) plus the Task 0 release-notes file folded into the Task 1 commit.

Rollback path (D-BUMP-03): if any gate cannot be made green within ~1 day, OR aggregate fix effort >1 day, `git revert` the pin commit, abandon the branch, file KD4.1-01 with the symptom + 7.4 release-notes pointer, close Phase 4.1 under option C (mirroring P3/P4). KD4-06 stays open and rolls into milestone-end review.
</verification>

<success_criteria>
**D-BUMP-02 5-gate scoreboard all PASS:**
1. ✓ Pin + lock + build — wagtail~=7.4.0 in pyproject, 7.4.x in uv.lock, Docker stack healthy.
2. ✓ Wagtail admin smoke — `curl /admin/login/` 200; manual login + draft edit + save + view list.
3. ✓ `pnpm contract:diff` 7/7 PASS — frozen allowlist, no diffs.
4. ✓ Preview round-trip — programmatically-minted token loads `/preview/<kind>/<token>/` end-to-end.
5. ✓ MinIO upload smoke — original + rendition in MinIO via `mc ls`, zero files under `/app` in wagtail container.

**Plus phase-exit:**
- Force-en audit Phase 4.1 row appended to `docs/force-en-audit.md` (UKR-06).
- ROADMAP Phase 4.1 row ticks `1/1` + `**Complete**`.
- KD4-06 closed in P4 SUMMARY.
- Phase 4.1 SUMMARY written.
- Branch `chore/wagtail-7.4-lts` ready to merge; user notified; merge NOT executed by executor.

**On rollback (D-BUMP-03):** branch abandoned, pin reverted on master, KD4.1-01 filed, phase closed under option C, KD4-06 rolls to milestone-end.
</success_criteria>

<output>
After completion, the following files exist on `chore/wagtail-7.4-lts`:
- `.planning/phases/04.1-wagtail-7-4-lts-bump-deferred-from-p4-per-d-bump-01/04.1-RELEASE-NOTES.md` (Task 0 artifact)
- `.planning/phases/04.1-wagtail-7-4-lts-bump-deferred-from-p4-per-d-bump-01/04.1-01-SUMMARY.md` (Task 6 artifact)

Plus the modified files:
- `backend/pyproject.toml` — wagtail pin bumped
- `backend/uv.lock` — refreshed
- `docs/force-en-audit.md` — Phase 4.1 row appended
- `.planning/ROADMAP.md` — Phase 4.1 ticked Complete
- `.planning/phases/04-wagtail-backend-skeleton-contract-match-dockerized/04-09-SUMMARY.md` — KD4-06 closed
</output>

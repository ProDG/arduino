---
phase: 03-page-templates-routing-static-build
plan: 10
type: execute
wave: 8
depends_on: [01, 09]
files_modified:
  - .planning/phases/03-page-templates-routing-static-build/wagtail-spike-report.md
autonomous: false
requirements: [CONTRACT-02]
must_haves:
  truths:
    - "Wagtail 7.4 LTS installed in a throwaway folder OUTSIDE the main repo (e.g. /tmp/wagtail-spike)"
    - "A single CodeBlock StructBlock is defined matching {language, code, annotations: [{line, note}]} and a FigureBlock matching {image_src, alt, width, height}"
    - "Wagtail REST API v2 returns a JSON response for one published page with one CodeBlock and one FigureBlock"
    - "Captured JSON is byte-compared against Block.code and Block.figure shapes in src/content/models/block.ts"
    - "wagtail-spike-report.md captures the JSON, the diff against the FE model, and a PASS/FAIL verdict"
    - "If FAIL: a documented remediation plan (rename note → html in FE model, OR document the conform-to-FE rule for P4 Wagtail) is captured"
  artifacts:
    - path: ".planning/phases/03-page-templates-routing-static-build/wagtail-spike-report.md"
      provides: "Empirical contract validation report"
      contains: "PASS"
  key_links:
    - from: "wagtail-spike-report.md"
      to: "src/content/models/block.ts"
      via: "byte-level diff between Wagtail JSON and FE TS shape"
      pattern: "annotations"
---

<objective>
Run the 30-60 minute Wagtail 7.4 StreamField spike outside the main repo, capture the API JSON response shape for one CodeBlock and one FigureBlock, diff against the FE Block model, and produce a PASS/FAIL verdict that signs off the design freeze across P3→P4.

Purpose: This is the design-freeze checkpoint. Once it passes, the FE contract becomes immutable across the P3 → P4 boundary. A FAIL here means either rename FE fields BEFORE phase exit or document a known Wagtail-side conform-to-FE rule for P4.

CRITICAL DATE GATE: Wagtail 7.4 LTS releases on 2026-05-04. Today is 2026-05-01. THIS PLAN CANNOT EXECUTE BEFORE 2026-05-04. The first task is a date gate that halts execution if today < 2026-05-04.

Output: A single .planning report file. The throwaway Wagtail project itself is deleted after the report is written (UI-SPEC: "Spike folder is deleted after the report is written; the FE model is the source of truth, not the spike").
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-page-templates-routing-static-build/03-CONTEXT.md
@.planning/phases/03-page-templates-routing-static-build/03-UI-SPEC.md
@.planning/phases/03-page-templates-routing-static-build/03-RESEARCH.md
@CLAUDE.md
@src/content/models/block.ts
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Date-gate decision — confirm today is on or after 2026-05-04 OR defer</name>
  <decision>Whether to run the Wagtail 7.4 LTS spike today, or defer until the LTS release.</decision>
  <context>
    Wagtail 7.4 LTS releases on 2026-05-04. Today's date when this plan was authored is 2026-05-01. Running `pip install 'wagtail==7.4.*'` before the LTS release date will resolve to a non-final tag (e.g., 7.4rc1) or a 7.3 fallback — neither validates the LTS contract correctly.

    Per CONTEXT D-SEQ-02 + Pitfall F (RESEARCH.md), this plan cannot execute before 2026-05-04.
  </context>
  <options>
    <option id="proceed">
      <name>Proceed (today's date is on or after 2026-05-04)</name>
      <pros>Phase 3 closes today; design-freeze checkpoint signed off; P4 unblocked.</pros>
      <cons>None — the LTS is out.</cons>
    </option>
    <option id="defer">
      <name>Defer (today's date is before 2026-05-04)</name>
      <pros>Avoids resolving against a non-LTS tag; correct architectural posture.</pros>
      <cons>Phase 3 stays open until the LTS release date. Plans 03-01..09 are already shipped and verified; only this checkpoint is outstanding.</cons>
    </option>
  </options>
  <resume-signal>Reply "proceed" to run Tasks 2-3 today (only if today &gt;= 2026-05-04). Reply "defer YYYY-MM-DD" if before — note the target date and pause execution.</resume-signal>
  <files>(no files modified — pure decision gate)</files>
  <action>Confirm today's calendar date. If today &lt; 2026-05-04, reply "defer YYYY-MM-DD" with the deferral target (typically 2026-05-04 or later). If today &gt;= 2026-05-04, reply "proceed" to unblock Tasks 2-3.</action>
  <verify>
    <automated>date +%Y-%m-%d | awk -v gate=2026-05-04 '$0 &gt;= gate { print "date gate OK"; exit 0 } { print "date gate BLOCKED until " gate; exit 1 }'</automated>
  </verify>
  <done>Decision recorded; either Task 2-3 unblocked OR phase 3 closure deferred until 2026-05-04 LTS release.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Spin up the throwaway Wagtail project, define StructBlocks, capture API JSON</name>
  <files>(throwaway: /tmp/wagtail-spike — NOT committed)</files>
  <read_first>
    - .planning/phases/03-page-templates-routing-static-build/03-RESEARCH.md "Wagtail 7.4 StreamField Spike Protocol" (lines 910-1000+)
    - .planning/phases/03-page-templates-routing-static-build/03-UI-SPEC.md §"Wagtail StreamField Spike" (~lines 468-498)
    - src/content/models/block.ts (FE source of truth — what the Wagtail JSON must match)
  </read_first>
  <behavior>
    - Throwaway Wagtail project at /tmp/wagtail-spike (or another tmp path) NEVER committed.
    - One CodeBlock + one FigureBlock defined with the locked shapes.
    - One published HomePage with one CodeBlock (2 lines, 1 annotation) + one FigureBlock.
    - GET `http://localhost:8000/api/v2/pages/<id>/?fields=body` returns a JSON whose body[].type and body[].value match the FE Block model.
  </behavior>
  <action>
    Step 1 — Outside the main repo:
    ```bash
    mkdir -p /tmp/wagtail-spike && cd /tmp/wagtail-spike
    uv init
    uv add 'wagtail==7.4.*' 'django==5.2.*' 'psycopg[binary]==3.2.*' pillow
    uv run wagtail start spike .
    ```

    Step 2 — Edit spike/settings/base.py:
    ```python
    LANGUAGE_CODE = 'uk'
    USE_TZ = True
    TIME_ZONE = 'Europe/Kyiv'
    # Use SQLite for the spike — no Postgres needed
    DATABASES = { 'default': { 'ENGINE': 'django.db.backends.sqlite3', 'NAME': BASE_DIR / 'db.sqlite3' } }
    ```

    Step 3 — home/models.py per RESEARCH.md spike protocol (lines 932-961):
    ```python
    from wagtail import blocks
    from wagtail.fields import StreamField
    from wagtail.models import Page

    class CodeBlock(blocks.StructBlock):
        language = blocks.ChoiceBlock(choices=[
            ('cpp', 'C++'), ('arduino', 'Arduino'),
            ('plaintext', 'Plain'), ('diff', 'Diff'),
        ])
        code = blocks.TextBlock()
        annotations = blocks.ListBlock(
            blocks.StructBlock([
                ('line', blocks.IntegerBlock()),
                ('note', blocks.RichTextBlock()),
            ])
        )

    class FigureBlock(blocks.StructBlock):
        image_src = blocks.CharBlock()
        alt = blocks.CharBlock()
        width = blocks.IntegerBlock()
        height = blocks.IntegerBlock()

    class HomePage(Page):
        body = StreamField([('code', CodeBlock()), ('figure', FigureBlock())], use_json_field=True)
        content_panels = Page.content_panels + ['body']
        api_fields = ['body']
    ```

    Step 4 — Wire api router in spike/api.py + spike/urls.py per RESEARCH.md (lines 962-970).

    Step 5 — Run:
    ```bash
    uv run python manage.py migrate
    uv run python manage.py createsuperuser
    uv run python manage.py runserver
    ```

    Step 6 — In Wagtail admin (`http://localhost:8000/admin/`):
    - Create one HomePage with body containing:
      * One CodeBlock: language=cpp, code="void setup() {}\nvoid loop() {}", annotations=[{line:1, note:"Some <em>rich</em> text"}].
      * One FigureBlock: image_src="https://example.invalid/test.png", alt="Тестова картинка", width=800, height=600.
    - Save + publish.

    Step 7 — Capture API response:
    ```bash
    curl -s 'http://localhost:8000/api/v2/pages/<page-id>/?fields=body' | python -m json.tool > /tmp/wagtail-spike/spike-response.json
    ```

    Constraints:
    - Throwaway project lives outside the main repo. NEVER `git add` anything from /tmp/wagtail-spike.
    - DO NOT install wagtail in the main repo — the LTS goes into P4.
  </action>
  <verify>
    <automated>test -f /tmp/wagtail-spike/spike-response.json &amp;&amp; python -c "import json; d=json.load(open('/tmp/wagtail-spike/spike-response.json')); assert 'body' in d, 'no body field'; assert any(b.get('type')=='code' for b in d['body']), 'no code block'; assert any(b.get('type')=='figure' for b in d['body']), 'no figure block'; print('JSON shape OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f /tmp/wagtail-spike/spike-response.json` exits 0.
    - Captured JSON contains a `body` array with at least one `{ type: 'code', value: { language, code, annotations } }` entry.
    - Captured JSON contains at least one `{ type: 'figure', value: { image_src, alt, width, height } }` entry.
    - Wagtail server runs and `curl` succeeds before tearing down.
  </acceptance_criteria>
  <done>spike-response.json captured with one CodeBlock + one FigureBlock + matching field shapes (or documented mismatches).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Diff captured JSON vs FE Block model + write wagtail-spike-report.md + delete throwaway folder</name>
  <files>.planning/phases/03-page-templates-routing-static-build/wagtail-spike-report.md</files>
  <read_first>
    - /tmp/wagtail-spike/spike-response.json (just captured)
    - src/content/models/block.ts (FE source of truth)
    - .planning/phases/03-page-templates-routing-static-build/03-UI-SPEC.md §Wagtail StreamField Spike (validation gate rules)
  </read_first>
  <behavior>
    - The report captures the JSON shape diffs side by side with the FE model.
    - Verdict is PASS/FAIL per gate (code block, figure block).
    - On FAIL: a remediation entry is included — either rename FE field (e.g. `html` → `note`) before phase exit, OR document a "P4 Wagtail must conform to FE" rule.
    - Throwaway folder /tmp/wagtail-spike is removed after the report is written.
  </behavior>
  <action>
    Step 1 — Read /tmp/wagtail-spike/spike-response.json. For each block in body[]:
    - Compare `block.value` keys against the matching FE Block variant fields.
    - Note key-level discrepancies:
      * `code` block: FE expects `{ language, code, annotations: [{line, html}] }`. Wagtail likely emits `annotations: [{line, note}]` because Wagtail RichTextBlock serializes under the field name. **Known concern:** field name `note` vs FE `html`.
      * `figure` block: FE expects `{ src, alt, width, height, ...}`. Wagtail emits `image_src`. **Known concern:** field name `image_src` vs FE `src`.

    Step 2 — Write .planning/phases/03-page-templates-routing-static-build/wagtail-spike-report.md:
    ```markdown
    # Wagtail 7.4 StreamField Spike Report

    **Date:** YYYY-MM-DD (run on/after 2026-05-04)
    **Wagtail version installed:** <pip list | grep wagtail>
    **Duration:** <X minutes>
    **Throwaway folder:** /tmp/wagtail-spike (deleted after this report)

    ## Captured API Response

    ```json
    <paste from spike-response.json — body[] only>
    ```

    ## Diff vs FE Block Model

    ### code block

    | Field | FE (block.ts) | Wagtail (api/v2) | Match |
    |---|---|---|---|
    | language | string ('cpp'\|'arduino'\|'plaintext'\|'diff') | string from ChoiceBlock | <PASS or "value matches enum"> |
    | code | string | string from TextBlock | PASS |
    | annotations[].line | number | integer from IntegerBlock | PASS |
    | annotations[].html | string (rich-text expanded HTML) | <field name as emitted by Wagtail — likely "note"> | <PASS if "html" or doc'd P4 rename if "note"> |

    ### figure block

    | Field | FE (block.ts) | Wagtail (api/v2) | Match |
    |---|---|---|---|
    | src | string | <"image_src" or "src"> | <verdict> |
    | alt | string | string | PASS |
    | width | number | integer | PASS |
    | height | number | integer | PASS |

    ## Verdict

    - code block: **<PASS|FAIL>** — <reason>
    - figure block: **<PASS|FAIL>** — <reason>

    ## Remediation (if any FAIL)

    <If field rename needed: which side renames (FE or Wagtail) + how the change propagates.>

    For example, if FE `annotations[].html` vs Wagtail `annotations[].note`:
    - **Decision:** P4 Wagtail's CodeBlock serializer overrides the field name to `html` via a custom `api_representation` (or equivalent), keeping the FE contract immutable.
    - **No FE change** — locks the contract for P3→P4 boundary.

    ## Sign-off

    Design freeze checkpoint: <SIGNED OFF | DEFERRED — explanation>.

    The FE contract in src/content/models/block.ts is **locked** across P3→P4 (or amended in this report with the specific change documented).
    ```

    Step 3 — Append a row to docs/typography-checklist.md under "Phase 3 — Wagtail Spike":
    ```
    | Date | Wagtail version | code block | figure block | Verdict | Report |
    |---|---|---|---|---|---|
    | YYYY-MM-DD | 7.4.X | PASS/FAIL | PASS/FAIL | <signed-off|deferred> | wagtail-spike-report.md |
    ```

    Step 4 — Delete the throwaway folder:
    ```bash
    rm -rf /tmp/wagtail-spike
    ```
    Per UI-SPEC: "Spike folder is deleted after the report is written; the FE model is the source of truth, not the spike."

    Constraints:
    - The report is the ONLY artifact committed from this plan.
    - DO NOT amend src/content/models/block.ts unless the spike clearly demands it AND user confirms (this is essentially CONTRACT-02 sign-off; touching the contract is a phase-exit-significance change).
    - If FAIL → user decides whether to amend FE in P3 close OR document the rule for P4 Wagtail-side accommodation.
  </action>
  <verify>
    <automated>test -f .planning/phases/03-page-templates-routing-static-build/wagtail-spike-report.md &amp;&amp; ! test -d /tmp/wagtail-spike &amp;&amp; grep -E "Verdict" .planning/phases/03-page-templates-routing-static-build/wagtail-spike-report.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/03-page-templates-routing-static-build/wagtail-spike-report.md` exits 0.
    - `grep -E "code block:.*PASS|FAIL" .planning/phases/03-page-templates-routing-static-build/wagtail-spike-report.md` returns a verdict line.
    - `grep -E "figure block:.*PASS|FAIL" .planning/phases/03-page-templates-routing-static-build/wagtail-spike-report.md` returns a verdict line.
    - `test ! -d /tmp/wagtail-spike` exits 0 (throwaway deleted).
    - `grep -E "Phase 3 — Wagtail Spike|Wagtail spike" docs/typography-checklist.md` returns one match.
  </acceptance_criteria>
  <done>wagtail-spike-report.md committed with verdict + diff + remediation (if any); throwaway folder gone; CONTRACT-02 closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Throwaway Wagtail container → main repo | One-way only; no main-repo state changes from the spike |
| Wagtail admin login (superuser) | Local /tmp instance; password ephemeral |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-10-01 | Information disclosure | spike superuser credentials persisted | mitigate | Throwaway folder deleted after report; SQLite + secret_key all ephemeral |
| T-03-10-02 | Tampering | spike report claims PASS without diff | mitigate | Report MUST include the JSON shape diff; reviewer (the user) confirms |
</threat_model>

<verification>
- wagtail-spike-report.md exists with explicit PASS/FAIL verdict per block + diff content.
- /tmp/wagtail-spike removed.
- docs/typography-checklist.md gets a Wagtail Spike row.
- src/content/models/block.ts EITHER unchanged (PASS) OR amended with documented rationale (FAIL → FE-side fix, or document P4 conform rule).
</verification>

<success_criteria>
- CONTRACT-02 closed via empirical Wagtail 7.4 spike.
- Design freeze signed off OR remediation plan recorded.
- Phase 3 ready for closure.
</success_criteria>

<output>
After completion, create `.planning/phases/03-page-templates-routing-static-build/03-10-SUMMARY.md`.
</output>

---
phase: 03-page-templates-routing-static-build
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/app/chrome/site-header.component.ts
  - src/app/chrome/site-header.component.html
  - src/app/chrome/site-header.component.scss
  - src/app/chrome/site-footer.component.ts
  - src/app/chrome/site-footer.component.html
  - src/app/chrome/site-footer.component.scss
  - src/app/chrome/site-nav.component.ts
  - src/app/chrome/site-nav.component.html
  - src/app/chrome/site-nav.component.scss
  - src/styles/tokens/_layout.scss
autonomous: true
requirements: [PAGE-09]
must_haves:
  truths:
    - "Every public page renders SiteHeader at top with Arduino UA wordmark + Уроки/Про проєкт nav"
    - "Every public page renders SiteFooter at bottom with editorial colophon + RSS placeholder + © line"
    - "Header collapses Про проєкт link to footer at <768px; nav shows Уроки only"
    - "Active nav link gets thicker underline (--color-accent, 0.12em)"
    - "Six new layout tokens are declared in _layout.scss and consumed by chrome only"
  artifacts:
    - path: "src/app/chrome/site-header.component.ts"
      provides: "SiteHeader standalone component"
      exports: ["SiteHeaderComponent"]
    - path: "src/app/chrome/site-footer.component.ts"
      provides: "SiteFooter standalone component"
      exports: ["SiteFooterComponent"]
    - path: "src/app/chrome/site-nav.component.ts"
      provides: "SiteNav subcomponent (router-aware active states)"
      exports: ["SiteNavComponent"]
    - path: "src/styles/tokens/_layout.scss"
      provides: "P3 layout tokens (--header-pad-block, --footer-pad-block, --toc-rail-width, --lesson-row-gap, --page-section-gap)"
      contains: "--header-pad-block"
  key_links:
    - from: "src/app/chrome/site-header.component.ts"
      to: "@arduino/core-ui PageShell"
      via: "imports + slotted into PageShell <header> placeholder by page templates"
      pattern: "PageShellComponent"
    - from: "src/app/chrome/site-nav.component.ts"
      to: "@angular/router routerLinkActive"
      via: "active route detection"
      pattern: "routerLinkActive"
---

<objective>
Build the global chrome — SiteHeader (wordmark + nav), SiteFooter (editorial colophon + RSS placeholder + license line), SiteNav (router-aware active states) — and declare the six new layout tokens in _layout.scss. These slot into P2's PageShell `<header>` and `<footer>` placeholders on every public page in waves 3+.

Purpose: Page templates (Plans 03-04..06) compose these into PageShell. Doing chrome first means template plans focus on body content, not header/footer scaffolding.

Output: Three chrome standalone components + amended layout-token file. No page templates wired yet.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-page-templates-routing-static-build/03-CONTEXT.md
@.planning/phases/03-page-templates-routing-static-build/03-UI-SPEC.md
@.planning/phases/03-page-templates-routing-static-build/03-PATTERNS.md
@CLAUDE.md
@projects/core-ui/src/lib/page-shell/page-shell.component.ts
@projects/core-ui/src/lib/page-shell/page-shell.component.html
@projects/core-ui/src/public-api.ts
@src/styles/tokens
@src/app/pages/glyph-audit/glyph-audit.component.scss

<interfaces>
P2 PageShell exposes named slots:
- `<header>` slot for site header
- `<main>` slot for page content
- `<footer>` slot for site footer

Routing targets used in chrome (locked by UI-SPEC §Copywriting):
- /lessons, /about, / (home wordmark target), /feed.xml (RSS placeholder)

Existing tokens consumed (from P1+P2):
- --color-paper, --color-ink, --color-ink-muted, --color-accent, --color-rule
- --font-body, --font-ui, --text-h3, --text-body, --text-caption
- --space-3, --space-6, --space-7, --container-max, --container-pad-mobile, --container-pad-tablet, --container-pad-desktop

NEW tokens to add in _layout.scss (per UI-SPEC §"New layout tokens"):
- --header-height: auto (intrinsic, not sticky)
- --header-pad-block: var(--space-6)  (32px)
- --footer-pad-block: var(--space-7)  (48px)
- --toc-rail-width: var(--margin-rail-width)  (alias)
- --lesson-row-gap: var(--space-7)
- --page-section-gap: var(--space-9)  (112px)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Declare six new layout tokens in _layout.scss + author SiteHeader and SiteNav (chrome top half)</name>
  <files>src/styles/tokens/_layout.scss, src/app/chrome/site-header.component.ts, src/app/chrome/site-header.component.html, src/app/chrome/site-header.component.scss, src/app/chrome/site-nav.component.ts, src/app/chrome/site-nav.component.html, src/app/chrome/site-nav.component.scss</files>
  <read_first>
    - src/styles/tokens/_layout.scss (or wherever P2 layout tokens live; verify before editing — may be in _layout.scss or _spacing.scss)
    - src/styles/tokens/_index.scss (token barrel — confirm registration pattern)
    - .planning/phases/03-page-templates-routing-static-build/03-UI-SPEC.md §"New layout tokens" + §SiteHeader (lines ~127-153)
    - .planning/phases/03-page-templates-routing-static-build/03-UI-SPEC.md §Copywriting (header strings: Arduino UA wordmark, Уроки, Про проєкт)
    - src/app/pages/glyph-audit/glyph-audit.component.scss (--container-pad-* breakpoint stair pattern)
    - projects/core-ui/src/public-api.ts (confirm @arduino/core-ui barrel exports)
  </read_first>
  <behavior>
    - Test 1: SiteHeaderComponent renders an <a rel="home" href="/"> with text "Arduino UA" inside it.
    - Test 2: SiteNavComponent renders nav links to /lessons (text "Уроки") and /about (text "Про проєкт").
    - Test 3: SiteNavComponent applies an active class (e.g., `nav-link--active`) when current route matches the link path.
    - Test 4: At <768px (verified via SCSS), the /about nav link is hidden via `display: none`.
    - Test 5: _layout.scss declares the six new CSS custom properties at :root.
  </behavior>
  <action>
    Step 1 — Edit src/styles/tokens/_layout.scss (create if absent, register in token barrel):
    Add at :root scope:
    ```scss
    :root {
      --header-height: auto;
      --header-pad-block: var(--space-6);
      --footer-pad-block: var(--space-7);
      --toc-rail-width: var(--margin-rail-width);
      --lesson-row-gap: var(--space-7);
      --page-section-gap: var(--space-9);
    }
    ```
    Confirm `--margin-rail-width` and `--space-9` exist (P1+P2). If `--space-9` is missing, the spec says 112px — add it to the spacing scale per UI-SPEC §Spacing (the spec says "Inherited unchanged from P1" — so if --space-9 doesn't exist yet, it must be added to _spacing.scss as 112px). Verify before editing.

    Step 2 — Create src/app/chrome/site-header.component.ts:
    - `@Component({ selector: 'app-site-header', standalone: true, changeDetection: ChangeDetectionStrategy.OnPush, templateUrl: './site-header.component.html', styleUrl: './site-header.component.scss', imports: [SiteNavComponent, RouterLink] })`
    - Class `SiteHeaderComponent` — no fields needed beyond imports.

    Step 3 — site-header.component.html:
    ```html
    <header class="site-header">
      <a class="site-header__wordmark" rel="home" routerLink="/">Arduino UA</a>
      <app-site-nav class="site-header__nav"></app-site-nav>
    </header>
    ```

    Step 4 — site-header.component.scss:
    - `.site-header` — flex container, `justify-content: space-between; align-items: baseline;`, padding-block: var(--header-pad-block), padding-inline: var(--container-pad-mobile) (mobile baseline), max-width: var(--container-max), margin-inline: auto, border-bottom: 1px solid var(--color-rule), background: var(--color-paper).
    - `.site-header__wordmark` — font-family: var(--font-body), font-style: italic, font-weight: 600, font-size: var(--text-h3), color: var(--color-accent), text-decoration: none. On `:hover, :focus-visible`: text-decoration with the P1 underline rule (e.g., text-decoration: underline; text-underline-offset: 0.18em; text-decoration-thickness: 0.08em).
    - Breakpoint stair: `@media (width >= 768px) { .site-header { padding-inline: var(--container-pad-tablet); } } @media (width >= 1200px) { .site-header { padding-inline: var(--container-pad-desktop); } }`
    - No box-shadow, no backdrop-filter, no `position: sticky`. Confirm with `grep -E "sticky|fixed|shadow" src/app/chrome/site-header.component.scss` — must return zero matches.

    Step 5 — Create src/app/chrome/site-nav.component.ts:
    - `@Component({ selector: 'app-site-nav', standalone: true, changeDetection: ChangeDetectionStrategy.OnPush, imports: [RouterLink, RouterLinkActive] })`
    - Class `SiteNavComponent` — readonly arrays of nav items (path + label) hardcoded per UI-SPEC §Copywriting; OR inline in the template.

    Step 6 — site-nav.component.html:
    ```html
    <nav aria-label="Головна навігація" class="site-nav">
      <a class="site-nav__link" routerLink="/lessons" routerLinkActive="site-nav__link--active">Уроки</a>
      <a class="site-nav__link site-nav__link--about" routerLink="/about" routerLinkActive="site-nav__link--active">Про проєкт</a>
    </nav>
    ```

    Step 7 — site-nav.component.scss:
    - `.site-nav` — flex, gap: var(--space-4), align-items: baseline.
    - `.site-nav__link` — font-family: var(--font-ui), font-size: var(--text-body), font-weight: 400, color: var(--color-accent), text-decoration: underline, text-underline-offset: 0.18em, text-decoration-thickness: 0.04em.
    - `.site-nav__link--active` — text-decoration-thickness: 0.12em (per UI-SPEC Color §addition 9).
    - At `<768px`: `.site-nav__link--about { display: none; }` (per UI-SPEC §SiteHeader nav rules).

    Step 8 — Verify @arduino/core-ui barrel does NOT need to export chrome (chrome lives in src/app/, not in the lib).

    Constraints:
    - All strings literal, hardcoded, in Ukrainian per UI-SPEC §Copywriting.
    - No emojis, no English fallbacks, no Lorem.
    - Use OnPush + standalone everywhere.
    - templateUrl + styleUrl, separate files (P2 convention).
    - Co-located SCSS, NOT global. Only the new tokens go to styles/tokens/.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit &amp;&amp; pnpm exec stylelint "src/app/chrome/**/*.scss" "src/styles/**/*.scss" &amp;&amp; pnpm exec vitest run src/app/chrome</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "^  --header-pad-block:" src/styles/tokens/_layout.scss` returns exactly one match.
    - `grep -cE "^  --(header-height|header-pad-block|footer-pad-block|toc-rail-width|lesson-row-gap|page-section-gap):" src/styles/tokens/_layout.scss` returns ≥ 6.
    - `grep -E "Arduino UA" src/app/chrome/site-header.component.html` returns one match (wordmark).
    - `grep -cE "Уроки|Про проєкт" src/app/chrome/site-nav.component.html` returns 2.
    - `grep -nE "routerLinkActive=\"site-nav__link--active\"" src/app/chrome/site-nav.component.html` returns ≥ 2 matches.
    - `grep -E "position:\s*sticky|position:\s*fixed|box-shadow" src/app/chrome/site-header.component.scss` returns NO matches.
    - `grep -E "@media \(width >= 768" src/app/chrome/site-nav.component.scss` followed by display:none for .site-nav__link--about — verify the rule exists: `grep -nE "site-nav__link--about" src/app/chrome/site-nav.component.scss`.
    - `pnpm exec tsc --noEmit` exits 0.
    - `pnpm build` succeeds.
  </acceptance_criteria>
  <done>SiteHeader + SiteNav + new layout tokens exist, all strings Ukrainian, no sticky/shadow on header, mobile nav drops Про проєкт, type-clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author SiteFooter (editorial colophon + RSS placeholder + license line)</name>
  <files>src/app/chrome/site-footer.component.ts, src/app/chrome/site-footer.component.html, src/app/chrome/site-footer.component.scss</files>
  <read_first>
    - .planning/phases/03-page-templates-routing-static-build/03-UI-SPEC.md §SiteFooter (full section ~lines 154-172)
    - .planning/phases/03-page-templates-routing-static-build/03-UI-SPEC.md §Copywriting "Footer colophon", "Footer meta — *", and the locked license string
    - src/app/chrome/site-header.component.scss (just created — match breakpoint stair conventions)
  </read_first>
  <behavior>
    - Test 1: Renders the locked colophon paragraph string verbatim (UI-SPEC §Copywriting "Footer colophon").
    - Test 2: Renders an `<a href="/feed.xml">` for RSS with an inline 16×16 SVG glyph and tooltip "У наступних фазах".
    - Test 3: Renders the license line `© 2026 · CC BY-SA 4.0`.
    - Test 4: At ≥768px, the footer is two-column (colophon ~⅔ left, meta-links ~⅓ right). At <768px, single column.
    - Test 5: Mobile-only meta link "Про проєкт" is hidden at ≥768px.
  </behavior>
  <action>
    Step 1 — Create site-footer.component.ts:
    - Standalone, OnPush, RouterLink import.
    - `@Component({ selector: 'app-site-footer', ... })` class `SiteFooterComponent`.

    Step 2 — site-footer.component.html — verbatim Ukrainian copy from UI-SPEC §Copywriting:
    ```html
    <footer class="site-footer">
      <p class="site-footer__colophon">
        Arduino UA — це повільний, ретельний переклад світу мікроконтролерів українською мовою. Один автор, одна тема, одна верстка. Книжкова типографіка — наш головний інструмент.
      </p>
      <ul class="site-footer__meta" role="list">
        <li class="site-footer__meta-item site-footer__meta-item--mobile">
          <a routerLink="/about">Про проєкт</a>
        </li>
        <li class="site-footer__meta-item">
          <a class="site-footer__rss-link" href="/feed.xml" aria-disabled="true" title="У наступних фазах">
            <svg class="site-footer__rss-glyph" width="16" height="16" viewBox="0 0 16 16" aria-hidden="true">
              <!-- Hand-crafted RSS glyph: arc + arc + dot. Color via currentColor. -->
              <path d="M3 11a2 2 0 1 1 0 4 2 2 0 0 1 0-4Z" fill="currentColor"/>
              <path d="M3 5a8 8 0 0 1 8 8h-2a6 6 0 0 0-6-6V5Z" fill="currentColor"/>
              <path d="M3 1a12 12 0 0 1 12 12h-2A10 10 0 0 0 3 3V1Z" fill="currentColor"/>
            </svg>
            <span>RSS</span>
          </a>
        </li>
        <li class="site-footer__meta-item">
          <span class="site-footer__license">© 2026 · CC BY-SA 4.0</span>
        </li>
      </ul>
    </footer>
    ```

    Step 3 — site-footer.component.scss:
    - `.site-footer` — single column at <768px; CSS Grid `grid-template-columns: 2fr 1fr; gap: var(--space-6)` at ≥768px. Padding-block: var(--footer-pad-block). Padding-inline: var(--container-pad-mobile)/tablet/desktop stair. Max-width: var(--container-max), margin-inline: auto. Border-top: 1px solid var(--color-rule).
    - `.site-footer__colophon` — font-family: var(--font-ui), font-size: var(--text-caption), color: var(--color-ink-muted), max-width: 40ch, margin: 0.
    - `.site-footer__meta` — list-style: none, padding: 0, margin: 0, display: flex, flex-direction: column, gap: var(--space-2). At ≥768px: align-self: start, justify-self: end.
    - `.site-footer__meta-item` — font-size: var(--text-caption).
    - `.site-footer__rss-link` — color: var(--color-accent), display: inline-flex, align-items: center, gap: var(--space-1).
    - `.site-footer__rss-glyph` — color: var(--color-accent) (via currentColor; ensure a parent declares color: var(--color-accent)).
    - `.site-footer__license` — color: var(--color-ink-muted).
    - At ≥768px: `.site-footer__meta-item--mobile { display: none; }` (per UI-SPEC).

    Constraints:
    - Every string literal in Ukrainian, no English (other than the locked "CC BY-SA 4.0" license name and "RSS" abbreviation, both per UI-SPEC §Copywriting locked strings).
    - The RSS link's href is `/feed.xml` even though the file does not exist yet (P6 ships it). The aria-disabled hint + tooltip explain this.
    - No new tokens; consume existing.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit &amp;&amp; pnpm exec stylelint "src/app/chrome/site-footer.component.scss" &amp;&amp; pnpm exec vitest run src/app/chrome/site-footer</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "повільний, ретельний переклад" src/app/chrome/site-footer.component.html` returns one match.
    - `grep -F "© 2026 · CC BY-SA 4.0" src/app/chrome/site-footer.component.html` returns one match.
    - `grep -E 'href="/feed.xml"' src/app/chrome/site-footer.component.html` returns one match.
    - `grep -E 'У наступних фазах' src/app/chrome/site-footer.component.html` returns one match.
    - `grep -E 'site-footer__meta-item--mobile' src/app/chrome/site-footer.component.scss` returns at least one rule with `display: none`.
    - `pnpm exec tsc --noEmit` exits 0.
    - `pnpm build` succeeds.
  </acceptance_criteria>
  <done>SiteFooter renders editorial colophon + RSS placeholder + license line; responsive 2-col at ≥768px / 1-col at <768px; type-clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chrome → router | Router links go to internal app routes only; href="/feed.xml" is a known dangling link until P6 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Spoofing | wordmark `<a rel="home">` | mitigate | rel="home" is informational; routerLink="/" is the canonical action |
| T-03-02-02 | Information disclosure | inline RSS SVG | accept | Inline SVG is static + zero data binding; no XSS surface |
</threat_model>

<verification>
- `pnpm exec tsc --noEmit` clean.
- `pnpm exec stylelint "src/app/chrome/**/*.scss" "src/styles/**/*.scss"` clean.
- `pnpm exec vitest run` clean.
- `pnpm build` succeeds.
- All chrome strings render in Ukrainian; no English leak.
</verification>

<success_criteria>
- Three chrome standalone components exist with OnPush + standalone + separate templateUrl/styleUrl.
- Six new layout tokens declared at :root in _layout.scss.
- Header is non-sticky, has bottom hairline, no shadow.
- Mobile nav drops Про проєкт; mobile footer surfaces it.
- All copy verbatim from UI-SPEC §Copywriting.
</success_criteria>

<output>
After completion, create `.planning/phases/03-page-templates-routing-static-build/03-02-SUMMARY.md`.
</output>

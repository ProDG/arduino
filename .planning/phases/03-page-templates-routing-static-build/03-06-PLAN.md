---
phase: 03-page-templates-routing-static-build
plan: 06
type: execute
wave: 4
depends_on: [01, 02, 03, 04]
files_modified:
  - src/app/pages/lesson-library/lesson-library.page.ts
  - src/app/pages/lesson-library/lesson-library.page.html
  - src/app/pages/lesson-library/lesson-library.page.scss
  - src/app/pages/home/home.component.ts
  - src/app/pages/home/home.component.html
  - src/app/pages/home/home.component.scss
  - src/app/pages/about/about.page.ts
  - src/app/pages/about/about.page.html
  - src/app/pages/about/about.page.scss
  - src/app/pages/about/about-prose.const.ts
  - src/app/pages/not-found/not-found.page.ts
  - src/app/pages/not-found/not-found.page.html
  - src/app/pages/not-found/not-found.page.scss
autonomous: true
requirements: [PAGE-05, PAGE-06, PAGE-07, PAGE-08, PAGE-11]
must_haves:
  truths:
    - "/lessons renders typographic ToC (NOT card grid): each row = 01. accent number + h3 title + meta line; 1px hairline rule between rows"
    - "/lessons empty state: editorial <aside> with locked Ukrainian copy when listLessons returns []"
    - "/ home renders editorial hero (h1 + lede) + Останні уроки mini-TOC (3 most recent) + Статті mini-TOC (2 most recent) + entry-points paragraph"
    - "/about renders 4-6 hand-authored Ukrainian paragraphs; AboutPage class has zero data dependencies"
    - "404 page renders 404 numeral + accent hairline rule + editorial lede + body link to /lessons; centered vertically"
    - "AboutPage and NotFoundPage do NOT inject CONTENT_API"
  artifacts:
    - path: "src/app/pages/lesson-library/lesson-library.page.ts"
      provides: "LessonLibraryPage with listLessons, sorted, mapped to row meta"
      exports: ["LessonLibraryPage"]
    - path: "src/app/pages/home/home.component.ts"
      provides: "HomeComponent reshaped to UI-SPEC HomePage (hero + recent + entry points)"
      exports: ["HomeComponent"]
    - path: "src/app/pages/about/about.page.ts"
      provides: "AboutPage standalone component"
      exports: ["AboutPage"]
    - path: "src/app/pages/about/about-prose.const.ts"
      provides: "ABOUT_PROSE constant — 4-6 hand-authored Ukrainian paragraphs"
      exports: ["ABOUT_PROSE"]
    - path: "src/app/pages/not-found/not-found.page.ts"
      provides: "NotFoundPage standalone component"
      exports: ["NotFoundPage"]
  key_links:
    - from: "src/app/pages/lesson-library/lesson-library.page.html"
      to: "ContentApi.listLessons()"
      via: "lessons signal populated in ngOnInit"
      pattern: "listLessons"
    - from: "src/app/pages/home/home.component.ts"
      to: "ContentApi.listLessons + listArticles"
      via: "Promise.all in ngOnInit, sorted by publishedAt desc, sliced to 3 + 2"
      pattern: "listArticles"
---

<objective>
Build the four "non-editorial-template" pages: LessonLibraryPage (typographic ToC), HomePage (editorial home with hero + recent lessons + recent articles + entry points paragraph), AboutPage (editorial prose), NotFoundPage (404 with accent hairline rule).

Purpose: These four pages share a "static or list-rendered, no body+sidenotes TwoColumn" pattern. Bundling them keeps each plan within ~50% context.

Output: Four new directories under src/app/pages/ + the existing src/app/pages/home/ reshaped per UI-SPEC HomePage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-page-templates-routing-static-build/03-CONTEXT.md
@.planning/phases/03-page-templates-routing-static-build/03-UI-SPEC.md
@.planning/phases/03-page-templates-routing-static-build/03-PATTERNS.md
@CLAUDE.md
@src/app/pages/home/home.component.ts
@src/app/pages/home/home.component.html
@src/app/pages/home/home.component.scss
@src/app/pages/glyph-audit/glyph-audit.component.ts
@src/app/pages/glyph-audit/glyph-audit.component.scss
@src/app/chrome/site-header.component.ts
@src/app/chrome/site-footer.component.ts
@src/lib/intl.ts
@src/lib/difficulty.ts
@src/content/api/content-api.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: LessonLibraryPage (typographic ToC, empty-state aside)</name>
  <files>src/app/pages/lesson-library/lesson-library.page.ts, src/app/pages/lesson-library/lesson-library.page.html, src/app/pages/lesson-library/lesson-library.page.scss</files>
  <read_first>
    - .planning/phases/03-page-templates-routing-static-build/03-UI-SPEC.md §LessonLibraryPage (full section ~lines 342-361)
    - .planning/phases/03-page-templates-routing-static-build/03-UI-SPEC.md §Copywriting (lesson library h1, lede, empty-state, row number prefix)
    - src/app/pages/lesson/lesson.page.ts (analog for component shape)
    - src/lib/difficulty.ts (DIFFICULTY_LABELS_UK)
  </read_first>
  <behavior>
    - Test 1: Class calls listLessons() on init, sorts by publishedAt desc.
    - Test 2: Each rendered row contains: 2-digit zero-padded number prefix `01.` in accent color, h3 link to /lessons/{slug}, meta line `{difficulty} · ≈ {N} хв`.
    - Test 3: When listLessons() returns empty array, the empty-state aside renders with the locked h2 "Уроки готуються" and body copy.
    - Test 4: Each row has a 1px --color-rule hairline between it and the next (CSS border-top on rows except first).
    - Test 5: NO card surface (no border-radius, no box-shadow on rows).
  </behavior>
  <action>
    Step 1 — lesson-library.page.ts:
    ```ts
    @Component({
      selector: 'app-lesson-library-page',
      standalone: true,
      changeDetection: ChangeDetectionStrategy.OnPush,
      imports: [RouterLink, PageShellComponent, HeadingComponent, LedeComponent, AsideComponent, SiteHeaderComponent, SiteFooterComponent],
      templateUrl: './lesson-library.page.html',
      styleUrl: './lesson-library.page.scss',
    })
    export class LessonLibraryPage implements OnInit {
      private api = inject(CONTENT_API);
      private title = inject(Title);
      lessons = signal<Array<{slug:string;title:string;deck:string;difficulty:DifficultyKey;estimatedMinutes:number;publishedAt:string}>>([]);
      sortedLessons = computed(() =>
        [...this.lessons()].sort((a, b) => b.publishedAt.localeCompare(a.publishedAt))
      );
      labelDifficulty = (d: DifficultyKey) => DIFFICULTY_LABELS_UK[d];
      labelMinutes = (n: number) => formatNumberUk(n);
      async ngOnInit() {
        const ls = await this.api.listLessons();
        this.lessons.set(ls);
        this.title.setTitle('Уроки — Arduino UA');
      }
    }
    ```

    Step 2 — lesson-library.page.html:
    ```html
    <ui-page-shell>
      <app-site-header header></app-site-header>
      <main main class="lesson-library-page">
        <header class="lesson-library-page__title-block">
          <ui-heading [level]="1">Уроки</ui-heading>
          <ui-lede>Послідовний шлях через мікроконтролери Arduino — українською мовою. Кожен урок калібрований так, щоб його можна було прочитати ввечері за чашкою чаю.</ui-lede>
        </header>

        @if (sortedLessons().length === 0) {
          <ui-aside variant="note" class="lesson-library-page__empty">
            <h2>Уроки готуються</h2>
            <p>Перші публікації з'являться у наступних фазах. Зазирай знову.</p>
          </ui-aside>
        } @else {
          <ol class="lesson-library-page__toc">
            @for (lesson of sortedLessons(); track lesson.slug; let i = $index) {
              <li class="lesson-library-page__row">
                <span class="lesson-library-page__num">{{ (i + 1).toString().padStart(2, '0') }}.</span>
                <a class="lesson-library-page__title" [routerLink]="['/lessons', lesson.slug]">{{ lesson.title }}</a>
                <p class="lesson-library-page__meta">{{ labelDifficulty(lesson.difficulty) }} · ≈ {{ labelMinutes(lesson.estimatedMinutes) }} хв</p>
              </li>
            }
          </ol>
        }
      </main>
      <app-site-footer footer></app-site-footer>
    </ui-page-shell>
    ```

    Step 3 — lesson-library.page.scss:
    - `.lesson-library-page` — outer wrapper stair.
    - `.lesson-library-page__title-block` — margin-block-end: var(--page-section-gap).
    - `.lesson-library-page__toc` — list-style: none; padding: 0; margin: 0; display: flex; flex-direction: column; gap: var(--lesson-row-gap).
    - `.lesson-library-page__row` — display: grid; grid-template-areas: "num title" ". meta"; grid-template-columns: auto 1fr; gap: var(--space-2) var(--space-3); padding-block-start: var(--space-3); border-block-start: 1px solid var(--color-rule).
    - `.lesson-library-page__row:first-child` — border-block-start: none; padding-block-start: 0.
    - `.lesson-library-page__num` — grid-area: num; color: var(--color-accent); font-family: var(--font-ui); font-weight: 600; font-size: var(--text-h3); line-height: 1;
    - `.lesson-library-page__title` — grid-area: title; font-size: var(--text-h3); color: var(--color-ink); font-family: var(--font-body); font-weight: 600; text-decoration: underline solid var(--color-accent) 0.04em; text-underline-offset: 0.18em.
    - `.lesson-library-page__title:hover` — text-decoration-thickness: 0.12em.
    - `.lesson-library-page__meta` — grid-area: meta; font-size: var(--text-caption); color: var(--color-ink-muted); margin: 0;
    - NO border-radius, NO box-shadow, NO background-color on rows (UI-SPEC: typographic, not card grid).

    Constraints:
    - Strings verbatim from UI-SPEC §Copywriting.
    - Mock data is empty? — verify pinout fixtures still included; if listLessons returns 3+, real rows render.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit &amp;&amp; pnpm exec stylelint "src/app/pages/lesson-library/*.scss" &amp;&amp; pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "Уроки" src/app/pages/lesson-library/lesson-library.page.html` returns ≥ 1 match.
    - `grep -F "Послідовний шлях через мікроконтролери" src/app/pages/lesson-library/lesson-library.page.html` returns one match.
    - `grep -F "Уроки готуються" src/app/pages/lesson-library/lesson-library.page.html` returns one match.
    - `grep -E "border-radius|box-shadow" src/app/pages/lesson-library/lesson-library.page.scss` returns NO matches.
    - `grep -F "padStart(2, '0')" src/app/pages/lesson-library/lesson-library.page.html` returns one match.
    - `pnpm exec tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>LessonLibraryPage renders typographic ToC with accent number + title + meta + hairline rule + empty state; no card surfaces.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: HomePage (reshape existing) + AboutPage (with hand-authored prose) + NotFoundPage</name>
  <files>src/app/pages/home/home.component.ts, src/app/pages/home/home.component.html, src/app/pages/home/home.component.scss, src/app/pages/about/about.page.ts, src/app/pages/about/about.page.html, src/app/pages/about/about.page.scss, src/app/pages/about/about-prose.const.ts, src/app/pages/not-found/not-found.page.ts, src/app/pages/not-found/not-found.page.html, src/app/pages/not-found/not-found.page.scss</files>
  <read_first>
    - src/app/pages/home/home.component.ts (existing; understand current shape before reshaping)
    - .planning/phases/03-page-templates-routing-static-build/03-UI-SPEC.md §HomePage, §AboutPage, §NotFoundPage (full sections)
    - .planning/phases/03-page-templates-routing-static-build/03-UI-SPEC.md §Copywriting (Home h1, Home lede, Recent headings, Усі уроки, About h1, About lede, About body rules, 404 numeral/lede/body)
    - .planning/phases/02-primitives-two-column-layout-page-model-contract/02-UI-SPEC.md §Copywriting (P2 punctuation rules — apply to About prose)
    - src/app/pages/glyph-audit/glyph-audit.component.ts (analog for static page pattern)
  </read_first>
  <behavior>
    - HomeComponent: listLessons + listArticles in parallel, sort desc, slice to 3 lessons + 2 articles, render hero + two mini-TOCs + entry-points paragraph.
    - AboutPage: zero data deps; renders title block + 4-6 inline-constant paragraphs from ABOUT_PROSE; sets <title>Про проєкт — Arduino UA</title>.
    - NotFoundPage: zero data deps; renders 404 numeral + 2px accent hairline + lede + body with link to /lessons; centered vertically; sets <title>Сторінку не знайдено — Arduino UA</title>.
  </behavior>
  <action>
    Step 1 — Reshape src/app/pages/home/home.component.ts:
    - Replace existing placeholder body with HomePage structure.
    - Inject CONTENT_API; on init, await Promise.all([listLessons(), listArticles()]); sort by publishedAt desc; recentLessons = first 3; recentArticles = first 2.
    - Set title to 'Arduino UA — українська онлайн-книга' (per UI-SPEC §Copywriting `<title>` home).
    - Imports: PageShellComponent, HeadingComponent, LedeComponent, RouterLink, SiteHeaderComponent, SiteFooterComponent.

    Step 2 — home.component.html structure (UI-SPEC §HomePage):
    - Hero: <ui-heading [level]="1">Arduino UA</ui-heading> + <ui-lede>Українська онлайн-книга про Arduino. Лекції, статті, схеми та довідники — у книжковій верстці.</ui-lede>.
    - Section Останні уроки: <h2>Останні уроки</h2> + same row visual as LessonLibraryPage (number prefix + h3 title link + difficulty · read-time meta) for the 3 recent. Trailing inline link `Усі уроки →` linked to /lessons in --color-accent --text-caption.
    - Section Статті: <h2>Статті</h2> + 2 article rows (number prefix + title link + date meta — no difficulty/read-time). Per UI-SPEC: NO trailing "all articles" link in P3 (article library deferred).
    - Entry points paragraph: a single <p> with the locked copy `Почни з <a routerLink="/lessons">Уроки</a> або прочитай <a routerLink="/about">про проєкт</a>.`

    Step 3 — home.component.scss: reuse the row visual conventions from LessonLibraryPage (1px rule between rows, accent number prefix, no card). Add hero rules: hero h1 = --text-h1; hero lede = --text-lede italic.

    Step 4 — Create src/app/pages/about/about-prose.const.ts:
    ```ts
    export const ABOUT_PROSE: ReadonlyArray<{ html: string }> = [
      { html: 'Цей сайт — повільна книжкова відповідь на швидкий «навчальний» інтернет. Тут ідуть від першого блимання LED до власного проєкту, без поспіху, без поглинання уваги, без бічних трендів.' },
      { html: 'Дизайн — головний інструмент. Натхнення — паперова книга <em>Arduino Starter Kit</em> з її спокійною двоколонковою версткою, бічними нотатками, навмисно стриманою палітрою.' },
      { html: 'Стек: Angular 21 на фронті (статична генерація, без Node SSR), <code>Wagtail</code> 7.4 LTS на беку, MinIO для медіа, Traefik для TLS. Усе в одному Docker Compose на одному VPS.' },
      { html: 'Один автор. Одна тема. Одна верстка. Якщо знайдеш помилку — пиши на пошту, відповідаю особисто.' },
      { html: 'Поки що — три уроки, одна стаття, два довідники, одна схема. Далі — більше про PWM, переривання, I²C, SPI, керування дисплеями. Про календарного плану наперед нема — пишу те, що зараз під рукою.' },
    ];
    ```
    (Real Ukrainian, hand-authored per UI-SPEC §AboutPage rules. No Lorem.)

    Step 5 — about.page.ts:
    ```ts
    @Component({ selector: 'app-about-page', standalone: true, changeDetection: ChangeDetectionStrategy.OnPush, imports: [PageShellComponent, HeadingComponent, LedeComponent, BodyComponent, SiteHeaderComponent, SiteFooterComponent], templateUrl: ..., styleUrl: ... })
    export class AboutPage implements OnInit {
      readonly prose = ABOUT_PROSE;
      private title = inject(Title);
      ngOnInit() { this.title.setTitle('Про проєкт — Arduino UA'); }
    }
    ```

    Step 6 — about.page.html:
    ```html
    <ui-page-shell>
      <app-site-header header></app-site-header>
      <article main class="about-page">
        <header class="about-page__title-block">
          <ui-heading [level]="1">Про проєкт</ui-heading>
          <ui-lede>Чому існує цей сайт, як він зроблений і куди рухається.</ui-lede>
        </header>
        <div class="about-page__body">
          @for (p of prose; track $index) {
            <ui-body [innerHTML]="p.html"></ui-body>
          }
        </div>
      </article>
      <app-site-footer footer></app-site-footer>
    </ui-page-shell>
    ```

    Step 7 — about.page.scss: outer wrapper stair; .about-page__title-block { margin-block-end: var(--page-section-gap) }; .about-page__body { display: flex; flex-direction: column; gap: var(--space-5) }.

    Step 8 — not-found.page.ts:
    ```ts
    @Component({ selector: 'app-not-found-page', standalone: true, changeDetection: ChangeDetectionStrategy.OnPush, imports: [RouterLink, PageShellComponent, HeadingComponent, LedeComponent, BodyComponent, SiteHeaderComponent, SiteFooterComponent], templateUrl: ..., styleUrl: ... })
    export class NotFoundPage implements OnInit {
      private title = inject(Title);
      ngOnInit() { this.title.setTitle('Сторінку не знайдено — Arduino UA'); }
    }
    ```

    Step 9 — not-found.page.html:
    ```html
    <ui-page-shell>
      <app-site-header header></app-site-header>
      <main main class="not-found-page">
        <div class="not-found-page__inner">
          <ui-heading [level]="1" class="not-found-page__numeral">404</ui-heading>
          <hr class="not-found-page__rule" aria-hidden="true" />
          <ui-lede>Цієї сторінки немає. Можливо, її ще не написано — або адреса змінилась.</ui-lede>
          <ui-body>
            Спробуй <a routerLink="/lessons">повернутися до уроків</a> або <a routerLink="/">на головну</a>.
          </ui-body>
        </div>
      </main>
      <app-site-footer footer></app-site-footer>
    </ui-page-shell>
    ```

    Step 10 — not-found.page.scss:
    - `.not-found-page` — min-height: calc(100vh - 200px); display: grid; place-content: center; padding-inline stair.
    - `.not-found-page__inner` — max-width: 60ch; margin-inline: auto.
    - `.not-found-page__rule` — border: 0; border-block-start: 2px solid var(--color-accent); margin-block: var(--space-3) var(--space-5); width: 100%.

    Constraints:
    - All Ukrainian per UI-SPEC §Copywriting verbatim.
    - 404 page is the ONLY P3 use of accent as a graphic rule (UI-SPEC §Color §addition 15).
    - About prose preserves typographic punctuation rules from P1+P2.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit &amp;&amp; pnpm exec stylelint "src/app/pages/home/*.scss" "src/app/pages/about/*.scss" "src/app/pages/not-found/*.scss" &amp;&amp; pnpm build &amp;&amp; node scripts/lint-fixtures.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "Останні уроки" src/app/pages/home/home.component.html` returns one match.
    - `grep -F "Усі уроки →" src/app/pages/home/home.component.html` returns one match.
    - `grep -F "Українська онлайн-книга про Arduino" src/app/pages/home/home.component.html` returns one match.
    - `grep -F "Про проєкт" src/app/pages/about/about.page.html` returns one match.
    - `grep -F "Чому існує цей сайт" src/app/pages/about/about.page.html` returns one match.
    - `wc -l src/app/pages/about/about-prose.const.ts` shows > 5 prose entries (4-6 paragraphs per UI-SPEC).
    - `grep -F "404" src/app/pages/not-found/not-found.page.html` returns one match.
    - `grep -F "Цієї сторінки немає" src/app/pages/not-found/not-found.page.html` returns one match.
    - `grep -E "border-block-start: 2px solid var\(--color-accent\)" src/app/pages/not-found/not-found.page.scss` returns one match.
    - `pnpm exec tsc --noEmit` exits 0.
    - `pnpm build` succeeds.
  </acceptance_criteria>
  <done>HomeComponent reshaped to UI-SPEC HomePage; AboutPage shipped with 4-6 hand-authored Ukrainian paragraphs; NotFoundPage shipped with accent hairline rule.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ABOUT_PROSE strings → DOM via [innerHTML] | Hand-authored constant in repo; trusted source |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06-01 | Tampering | ABOUT_PROSE [innerHTML] | mitigate | Constant lives in repo; fixed at build time; Angular DomSanitizer applied |
| T-03-06-02 | Information disclosure | wildcard route exposes 404 with internal links | accept | 404 page is intentionally a known surface |
</threat_model>

<verification>
- `pnpm exec tsc --noEmit` clean.
- `pnpm exec stylelint` clean.
- `pnpm build` succeeds.
- `node scripts/lint-fixtures.mjs` still clean (no fixture changes).
- About prose hand-checked: no Lorem, no English fragments, includes <em>Arduino Starter Kit</em> reference per UI-SPEC.
</verification>

<success_criteria>
- LessonLibraryPage renders typographic ToC + empty state.
- HomePage shows hero + 3 recent lessons + 2 recent articles + entry-points paragraph.
- AboutPage renders 4-6 paragraphs of real Ukrainian prose; no fixture dep.
- NotFoundPage renders 404 numeral + 2px accent hairline rule + body with internal links; centered vertically.
- All component classes typed, OnPush, standalone, separate template/style files.
</success_criteria>

<output>
After completion, create `.planning/phases/03-page-templates-routing-static-build/03-06-SUMMARY.md`.
</output>
